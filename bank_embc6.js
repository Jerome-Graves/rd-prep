BANKS["Embedded C"] = BANKS["Embedded C"].concat([

// ---------------------------------------------------------- emb-adc-fw
{ t: "emb-adc-fw", d: 2, q: "Readings are consistently low, and get worse when you fit a higher-value sensor resistor. What is wrong?", o: ["The reference is drifting", "Acquisition time is too short for the source impedance", "The ADC clock is too slow", "Wrong result alignment"], a: 1, why: "<p>Before converting, the ADC connects an internal sampling capacitor to your pin and it charges through your source resistance. Convert too early and you digitise a partly charged capacitor.</p><p>Settling to within half an LSB at 12 bits takes roughly nine time constants of (R_source + R_switch) x C_sample. A 10k source is under a microsecond; a 1M source is tens of microseconds and the default setting is nowhere near.</p><p>The signature is exactly this: low readings that move when you change the source impedance or the channel order. Lengthen the acquisition time or buffer the source with an op-amp.</p>" },
{ t: "emb-adc-fw", d: 3, q: "In scan mode, each channel's reading is pulled toward the previous channel's value. Why?", o: ["PCB crosstalk", "Acquisition time: the sampling capacitor still holds charge from the previous channel", "DMA is misconfigured", "The reference is shared"], a: 1, why: "<p>Same root cause as readings being low, but it presents as contamination rather than an offset because the capacitor's starting charge is the last channel's voltage rather than zero.</p><p>The acquisition time in scan mode has to suit the <b>worst</b> source impedance among the channels, not the average and certainly not the best.</p><p>It is a good fault to recognise because it looks like a layout or crosstalk problem, and people go looking at the board.</p>" },
{ t: "emb-adc-fw", d: 2, q: "Why is timer-triggered conversion into DMA better than one interrupt per conversion?", o: ["It is more accurate per sample", "Sample timing comes from hardware, the CPU can sleep, and the interrupt rate falls by the buffer size", "It needs no calibration", "It uses less flash"], a: 1, why: "<p>Three arguments at once, and the timing one is the one people miss.</p><p>A loop or an interrupt handler samples when it gets round to it, so the interval varies with system load. A timer samples on time regardless of what the CPU is doing, which is what everything downstream assumes.</p><p>At 10 kHz, an interrupt per conversion is 10,000 entries and exits per second to move two bytes. With DMA and a half-buffer interrupt that becomes a handful, and the core can sleep in between.</p>" },
{ t: "emb-adc-fw", d: 2, q: "Your 12-bit result is sixteen times larger than expected. What is the likely cause?", o: ["Wrong reference", "The result is left-aligned in a 16-bit register", "Acquisition time", "Gain error"], a: 1, why: "<p>Many ADCs left-align, so a 12-bit value occupies the top bits of a 16-bit register and needs shifting right by four.</p><p>It is worth checking early in bring-up because it presents as a scaling error and sends you looking at your conversion maths.</p><p>The related case is a value four times out, which is usually a resolution setting different from the one your maths assumes.</p>" },
{ t: "emb-adc-fw", d: 3, q: "Why can switching from VDD to a precision internal reference make a measurement worse?", o: ["It never does", "If the sensor is a divider powered from VDD, the measurement was ratiometric and VDD variation was cancelling", "Internal references are noisier", "It changes the resolution"], a: 1, why: "<p>With a resistive divider across VDD and the ADC referencing VDD, the reading depends only on the resistance ratio and VDD disappears from the equation entirely.</p><p>Switch to a fixed internal reference and VDD variation now appears directly in the result, so you have added an error source in the name of precision.</p><p>The first question about any analogue input is whether it is ratiometric, because that decides the reference before anything else does.</p>" },
{ t: "emb-adc-fw", d: 1, q: "The ADC returns all zeros or all full scale on a new board. What do you check first?", o: ["Acquisition time", "Clock enabled, pin in analogue mode, and the right channel selected", "The reference voltage", "The DMA configuration"], a: 1, why: "<p>Those three account for the overwhelming majority of first-day faults, and all are configuration rather than analogue.</p><p>A pin left in its default digital mode is the classic: everything else in the code is correct and the converter is looking at a driven output.</p><p>Working the bring-up list in order, configuration first and analogue last, costs minutes and saves an afternoon of scoping something that was never the problem.</p>" },
{ t: "emb-adc-fw", d: 2, q: "Why do many ADCs need a self-calibration run after reset?", o: ["To set the reference", "It removes internal offset error, and skipping it degrades accuracy silently", "To enable the clock", "It is optional and cosmetic"], a: 1, why: "<p>The converter has its own offset and capacitor mismatch, and the calibration routine measures and compensates for them.</p><p>Skipping it is silent: readings look plausible and are simply less accurate than the datasheet claims, so nothing draws attention to it.</p><p>Some parts also want it re-run after a significant temperature change, which is worth checking if accuracy drifts over a long run.</p>" },
{ t: "emb-adc-fw", d: 2, q: "What determines how long the ADC must sample before converting?", o: ["The ADC clock only", "Your source resistance and the ADC's sampling capacitor, together", "The resolution setting", "The reference voltage"], a: 1, why: "<p>It is a property of your circuit as much as of the converter, which is why the datasheet cannot simply give you a number.</p><p>The capacitor charges through R_source plus the internal switch resistance, and you need roughly nine time constants for 12-bit settling.</p><p>That is why an op-amp buffer in front of a high-impedance sensor is often the right answer: it makes the source resistance negligible and the acquisition time stops mattering.</p>" },
{ t: "emb-adc-fw", d: 3, q: "Why does running the ADC clock above its maximum look like noise rather than an obvious failure?", o: ["It does not", "The converter has insufficient settling time per bit decision, so results scatter rather than failing outright", "The clock is ignored", "It only affects the reference"], a: 1, why: "<p>A SAR converter makes one bit decision per clock and each needs the comparator to settle. Rush it and the decisions become marginal rather than wrong.</p><p>So the output is centred roughly correctly but scattered, which is indistinguishable from analogue noise unless you know to check the clock.</p><p>It is worth verifying against the datasheet maximum during bring-up, because chasing it as a noise problem can take a long time.</p>" },
{ t: "emb-adc-fw", d: 1, q: "What does the ADC actually measure?", o: ["Absolute voltage", "The input as a fraction of its reference voltage", "Current", "Charge"], a: 1, why: "<p>Everything is a ratio against the reference, which is why the reference is as much part of your accuracy as the converter is.</p><p>V_in = (count / 4095) x V_ref for a 12-bit part, so an error in V_ref is an error in every reading.</p><p>It also explains the ratiometric trick: make the sensor's excitation and the ADC's reference the same thing, and the reference error cancels instead of accumulating.</p>" },

// ---------------------------------------------------------- emb-sampling
{ t: "emb-sampling", d: 1, q: "A loop reads the ADC then calls delay_ms(1). Why is that not 1 kHz sampling?", o: ["delay_ms is inaccurate", "The interval is 1 ms plus the work plus any interrupt, so it varies sample to sample", "The ADC is too slow", "It is 1 kHz"], a: 1, why: "<p>The average rate may well be close to 1 kHz. Every individual interval is different, and the variation grows with system load.</p><p>Everything downstream assumes a constant interval: FFT bins, filter cutoffs, derivative terms, time-of-flight calculations.</p><p>The fix is to let a timer trigger conversions so the sample instant comes from hardware and no amount of CPU load can move it.</p>" },
{ t: "emb-sampling", d: 3, q: "How does sample timing jitter appear in the data?", o: ["As a constant offset", "As noise proportional to the signal's slew rate", "As a gain error", "As aliasing"], a: 1, why: "<p>Sampling at the wrong instant gives you the value the signal had at that instant, so the error is the timing error multiplied by how fast the signal is moving.</p><p>The consequence is that the same jitter is harmless on a slow signal and severe on a fast one, which is why a system can work fine until someone increases the bandwidth.</p><p>It also means the error is not calibratable: it is different every sample.</p>" },
{ t: "emb-sampling", d: 2, q: "An event cannot be sampled periodically. Where should the timestamp be taken?", o: ["In the task that processes it", "In the first line of the handler, or by hardware input capture", "After reading the sensor", "At the end of the handler"], a: 1, why: "<p>Reading the counter first bounds the error to the interrupt latency. Timestamping in the processing task records when you got round to it, which is a different and useless quantity.</p><p>Input capture is better still: the timer latches its count in hardware at the edge, so the error is zero regardless of when your handler runs.</p><p>This is the difference between a time-of-flight measurement that works and one whose error varies with system load.</p>" },
{ t: "emb-sampling", d: 2, q: "Where must an anti-alias filter be?", o: ["Anywhere in the chain", "In hardware, before the ADC", "In firmware, after sampling", "In the DMA setup"], a: 1, why: "<p>Once a component has folded down, it is indistinguishable from a genuine signal at the aliased frequency, so nothing downstream can separate them.</p><p>Two cases worth recognising: mains at 50 or 60 Hz folding to something slow, which looks like genuine drift; and a switching regulator's ripple aliasing near DC, which looks like an offset that changes with load.</p><p>The fixes are a filter on the board, or a higher sample rate with decimation in firmware. Cleverness after the fact is not one of them.</p>" },
{ t: "emb-sampling", d: 2, q: "Two systems sample at the same average rate but one is timer-triggered and one loop-polled. What differs?", o: ["Nothing", "The spectrum: jitter in the polled one becomes broadband noise", "The average value", "The resolution"], a: 1, why: "<p>Average rate says nothing about the interval between any two samples, and it is the interval that every downstream assumption rests on.</p><p>Jitter spreads energy across the spectrum, so an FFT smears, a designed filter cutoff moves, and a derivative term gets noisy.</p><p>This is why 'we sample at 1 kHz' is not a sufficient specification without saying how the sampling is triggered.</p>" },
{ t: "emb-sampling", d: 3, q: "A slow drift appears in logged data that is not present in the physical quantity. What should you suspect?", o: ["Calibration", "Mains hum aliasing down to a low frequency", "The ADC reference", "Temperature"], a: 1, why: "<p>If the sample rate is close to a multiple of the mains frequency, 50 or 60 Hz folds to a low beat frequency that looks exactly like genuine slow variation.</p><p>The giveaway is that the apparent drift period changes if you alter the sample rate slightly, which no real physical drift would do.</p><p>It also cannot be filtered out afterwards, so the answer is an anti-alias filter or a rate change plus decimation.</p>" },
{ t: "emb-sampling", d: 2, q: "How would you measure sampling jitter cheaply?", o: ["An FFT of the data", "Toggle a GPIO at each sample and look at the spread on a scope", "Count interrupts", "Read the timer in software"], a: 1, why: "<p>A pin toggle costs a couple of instructions and shows both the average rate and, crucially, the spread, with sub-microsecond resolution and no tooling beyond a scope.</p><p>Persistence mode on the scope makes the jitter visible directly as the width of the edge distribution.</p><p>Reading a timer in software measures partly your own measurement code, and an FFT tells you there is a problem without telling you it is timing.</p>" },
{ t: "emb-sampling", d: 2, q: "Why does a control loop care about sampling jitter specifically?", o: ["It does not", "The derivative term amplifies it, and the loop's timing assumptions are built on a fixed timestep", "It changes the setpoint", "It affects the integral only"], a: 1, why: "<p>Most discrete control maths assumes a fixed interval, so a varying one means the coefficients no longer correspond to the time constants they were designed for.</p><p>The derivative term is the visible casualty because it divides by the interval, so a short interval inflates it.</p><p>A consistent delay can be designed around; a varying one cannot, which is why jitter often matters more than latency.</p>" },
{ t: "emb-sampling", d: 1, q: "What is the firmware-relevant consequence of the Nyquist limit?", o: ["Sample at least twice the highest frequency present, not just the highest of interest", "Sample as fast as possible", "Filter after sampling", "Use more bits"], a: 1, why: "<p>The distinction between 'present' and 'of interest' is the whole practical point. Interference well above your band still folds down.</p><p>So the sample rate has to be chosen together with the hardware filter that bounds what is present, not from the signal you care about alone.</p><p>Raising the rate and decimating in firmware is a legitimate alternative, because it moves the folding frequency above the interference.</p>" },
{ t: "emb-sampling", d: 3, q: "Why is DMA relevant to sampling accuracy, not just to CPU load?", o: ["It is not", "It removes software from the path between the trigger and the data being stored, so no handler latency affects it", "It is faster", "It filters the data"], a: 1, why: "<p>With an interrupt per conversion, the data is read when the handler runs, and the handler runs when nothing more urgent is in the way.</p><p>The conversion itself was still timer-triggered, so the sample instant is fine, but any variable delay in servicing risks an overrun where a result is overwritten before it is read.</p><p>DMA removes that failure mode entirely: the transfer happens in hardware and the CPU only has to keep up with whole buffers.</p>" },

// ---------------------------------------------------------- emb-units
{ t: "emb-units", d: 2, q: "What makes a measurement ratiometric?", o: ["Using a precision reference", "The sensor's excitation and the ADC's reference are the same voltage, so it cancels", "Averaging many samples", "Using a differential input"], a: 1, why: "<p>With a resistive divider across VDD and the ADC referencing VDD, the reading depends only on the resistance ratio. VDD disappears from the equation.</p><p>That turns supply variation, which you cannot control, into something that does not matter.</p><p>The converse case is a sensor with its own voltage output, which is not ratiometric, and there VDD as a reference puts every millivolt of supply variation straight into your reading.</p>" },
{ t: "emb-units", d: 2, q: "Where should the conversion from counts to engineering units happen?", o: ["Inside the driver", "Above it, in pure functions, with the driver returning raw counts", "In the ISR", "In the display code"], a: 1, why: "<p>Counts are the only thing that is a fact about the hardware. Everything after that is interpretation, and interpretation belongs where you can test it.</p><p>Pure conversion functions can be swept across their whole input range on a host in milliseconds, including the edges where the arithmetic overflows.</p><p>It also keeps the driver a thing that reads a register, which is what makes it portable to the next project.</p>" },
{ t: "emb-units", d: 1, q: "Why name a variable temp_mc rather than temp?", o: ["Convention", "The scaling is invisible in the type, so the unit must live in the name", "It is shorter", "For the debugger"], a: 1, why: "<p>Nothing in <code>int32_t</code> distinguishes celsius from millicelsius from raw counts, so a reader and a caller both have to guess.</p><p>It is the same argument as naming a Q16.16 value <code>angle_q16</code>: fixed-point scaling has no type-system representation in C.</p><p>The everyday failure is a timeout passed in milliseconds to a function expecting ticks. The famous one lost the Mars Climate Orbiter.</p>" },
{ t: "emb-units", d: 3, q: "Which expression converts a 12-bit count to millivolts correctly?", o: ["(count / 4095) * vref_mv", "(count * vref_mv) / 4095 in int16_t", "(int32_t)(((int64_t)count * vref_mv) / 4095)", "count * (vref_mv / 4095)"], a: 2, why: "<p>Two rules apply together: multiply before dividing to preserve precision, and widen first so the multiply cannot overflow.</p><p>Options 1 and 4 divide first, so the integer division discards everything below one unit and the result is coarse or zero.</p><p>Option 2 has the right order but the wrong width, and it is the interesting failure because it is correct for these particular numbers and breaks the moment someone reuses the pattern for a 24-bit ADC.</p>" },
{ t: "emb-units", d: 2, q: "A thermocouple is disconnected. What should the reading be?", o: ["Zero degrees", "A status indicating the fault, separate from the value", "The last good reading", "Minus one"], a: 1, why: "<p>Returning a plausible number for a failed measurement is silent degradation: the data is wrong and nothing distinguishes it from correct.</p><p>A device reporting that a sensor is absent is a support call. One reporting plausible wrong readings is a recall, discovered long after every measurement since became suspect.</p><p>Return the status separately, in the same way a driver returns an error code rather than a magic value.</p>" },
{ t: "emb-units", d: 2, q: "Why work in millicelsius rather than celsius as a float?", o: ["Floats are inaccurate", "Integer maths in the smallest useful unit avoids floating point while keeping the sensor's real resolution", "It uses less RAM", "Celsius is ambiguous"], a: 1, why: "<p>Many parts have no FPU, and on those that do, using it in an interrupt handler may require saving FPU context, which adds cycles to every such interrupt.</p><p>Choosing the unit so that an integer holds the resolution you actually have removes the question entirely.</p><p>The trap that comes with it is intermediate overflow, which is why widening before a multiply matters so much here.</p>" },
{ t: "emb-units", d: 3, q: "Why write the conversion chain out in steps rather than as one composite constant?", o: ["It is faster", "The composite constant hides every assumption in the chain, and each step is a place a mistake can hide", "The compiler requires it", "For coverage"], a: 1, why: "<p>A single magic number like 0.0806 encodes the reference, the resolution, the divider ratio and the sensor's slope all at once, and nobody can check any of them.</p><p>Written as steps, count to fraction to volts to sensor quantity to application units, each conversion is separately reviewable and separately testable.</p><p>The compiler folds the constants anyway, so the runtime cost of clarity here is zero.</p>" },
{ t: "emb-units", d: 2, q: "Which part of the analogue chain is easiest to test on a host?", o: ["The ADC driver", "The unit conversion and calibration functions", "The DMA setup", "The timer trigger"], a: 1, why: "<p>They are pure functions of integers, so they need no hardware and no doubles at all.</p><p>That makes them cheap to sweep across the full input range, which is where the interesting failures are: overflow at the top of the range, truncation at the bottom, and sign handling in between.</p><p>It is also the argument for keeping them out of the driver, since anything mixed in with register access becomes untestable by association.</p>" },
{ t: "emb-units", d: 1, q: "What is the first question to ask about any new analogue input?", o: ["What resolution do I need", "Is it ratiometric", "How fast must I sample", "What is the noise floor"], a: 1, why: "<p>Whether the sensor's excitation and the ADC's reference are the same thing decides the reference choice, and the reference choice affects every reading.</p><p>Get it wrong in one direction and you add supply variation to a measurement that did not have it. Get it wrong in the other and you pay for a precision reference that buys nothing.</p><p>It costs one look at the schematic and it constrains everything that follows.</p>" },
{ t: "emb-units", d: 2, q: "A composite scaling constant is correct but a reviewer cannot verify it. What does that indicate?", o: ["Nothing, it works", "The assumptions are hidden, so a later change to any of them will silently invalidate it", "It should be a float", "It needs a comment"], a: 1, why: "<p>Correct today is not the property that matters. When the divider resistors change, or the reference changes, someone has to find and update that constant, and nothing in it says what it was made of.</p><p>Written as explicit steps with named constants, the change is localised and obvious.</p><p>A comment helps but is not enforced; separate named constants are checked by the fact that they appear in the arithmetic.</p>" },

// ---------------------------------------------------------- emb-calib
{ t: "emb-calib", d: 1, q: "What does two-point calibration determine that one-point does not?", o: ["Nonlinearity", "Gain as well as offset", "Temperature drift", "Noise"], a: 1, why: "<p>One point measures a single known value and solves for offset, assuming the gain is nominal. That is adequate where the gain is well controlled and the offset is not.</p><p>Two points, ideally near each end of the range, solve for both.</p><p>More points than that address a different problem entirely: linearity, which needs a polynomial or an interpolated lookup table rather than a scale and a shift.</p>" },
{ t: "emb-calib", d: 2, q: "Why does a stored calibration record need a CRC?", o: ["To save space", "Corrupted coefficients produce plausible wrong readings, which is the worst available failure mode", "Flash requires it", "For versioning"], a: 1, why: "<p>A corrupt record that still parses gives you data that is wrong but entirely believable, and nothing distinguishes it from correct.</p><p>That is silent degradation: it starts at some unknown point and is discovered long afterwards, by which time every measurement since is suspect.</p><p>A CRC turns it into a detectable condition you can act on at boot, which is the whole point.</p>" },
{ t: "emb-calib", d: 2, q: "Calibration data fails its CRC at boot. Which response is never acceptable?", o: ["Refuse to operate and report why", "Run on nominal values and report degraded", "Silently substitute nominal values", "Refuse to operate"], a: 2, why: "<p>The first two are both defensible depending on whether wrong data is worse than no data, which is a product decision rather than a technical one.</p><p>Silently substituting nominal values is how a product ships subtly wrong measurements for a year with nobody aware.</p><p>Whichever you choose, a diagnostic command should report calibration validity and date, because otherwise that support question has no answer.</p>" },
{ t: "emb-calib", d: 2, q: "Why store a format or version field alongside the coefficients?", o: ["For the CRC", "You will change the record's layout, and devices in the field will still hold the old one", "It identifies the unit", "It is required by MISRA"], a: 1, why: "<p>A firmware update that assumes the new layout will misread every existing device, turning good calibration into nonsense that still passes a CRC computed over the wrong fields.</p><p>A version field lets new firmware recognise an old record and either read it correctly or reject it deliberately.</p><p>It is two bytes, and it is the difference between an update being safe and being a recall.</p>" },
{ t: "emb-calib", d: 3, q: "Why are temperature coefficients usually characterised per design rather than per unit?", o: ["They do not vary between units", "Per-unit characterisation means a thermal soak per unit at manufacture, which is expensive", "Firmware cannot store them", "It is more accurate"], a: 1, why: "<p>It is a cost trade rather than a technical preference. Characterising a sample of units over temperature captures most of the systematic drift for a fraction of the manufacturing cost.</p><p>Per-unit temperature calibration exists on genuinely high-accuracy instruments, and the price reflects it.</p><p>Either way it corrects only the systematic part: hysteresis and long-term ageing are not fixed this way, which is why recalibration intervals exist.</p>" },
{ t: "emb-calib", d: 2, q: "Why keep factory and field calibration in separate records?", o: ["For space", "So a user zeroing the instrument can never overwrite the traceable factory gain", "The CRC differs", "Field calibration is optional"], a: 1, why: "<p>They answer different questions and have different provenance. Factory calibration is traceable and rarely changes; a field zero is routine and untraceable.</p><p>Merging them means one user action can destroy the reference against which the instrument was certified, and there is no way back without returning the unit.</p><p>The related rule: a firmware update must never discard the factory record, which has happened to real products and is a recall.</p>" },
{ t: "emb-calib", d: 2, q: "Averaging does not remove a systematic offset. What does?", o: ["More samples", "Calibration", "A median filter", "Oversampling"], a: 1, why: "<p>Averaging reduces random noise as the square root of N, and does nothing at all to a bias: you get the bias more precisely.</p><p>Offset is exactly what the offset term in a calibration exists to remove, measured against a known reference.</p><p>Recognising which errors are random and which are systematic is what decides whether the answer is more samples or a better reference.</p>" },
{ t: "emb-calib", d: 3, q: "What should a calibration record carry beyond offset and gain?", o: ["Nothing else", "A CRC, a format version, and the date and equipment used", "The serial number only", "The firmware version"], a: 1, why: "<p>The CRC guards against plausible wrong readings, the version guards against a future firmware misreading the layout, and the date and equipment are what an auditor asks for.</p><p>The date has a second use that is easy to miss: it tells you whether a unit was calibrated before or after a known problem with a reference or a procedure, which is how you scope a recall.</p><p>All of it costs a few bytes in a record you are writing anyway.</p>" },
{ t: "emb-calib", d: 2, q: "Why does calibration data belong in a log-structured or wear-aware store rather than a plain erase-and-write?", o: ["It does not", "Because an erase-then-write has a window where neither old nor new data exists, and repeated erases wear the sector", "For speed", "To save flash"], a: 1, why: "<p>A power loss between the erase and the write loses the calibration entirely, which for a measurement instrument is a return to the factory.</p><p>Even without that, endurance is per sector, so anything that erases frequently kills one sector while the rest of the flash is untouched.</p><p>Writing the data before its validity marker is what makes a partial record invisible rather than corrupt, and it is why an existing NVS is usually a better choice than a hand-rolled scheme.</p>" },
{ t: "emb-calib", d: 1, q: "What does the offset term in (raw - offset) * gain remove?", o: ["Proportional error", "The constant error present even at zero input", "Noise", "Nonlinearity"], a: 1, why: "<p>Offset is the reading you get for a known zero: amplifier input offset, ADC offset, a stray thermocouple junction.</p><p>Gain corrects the proportional part, the error that grows with the reading.</p><p>Both are per unit, which is why they cannot be compiled in and have to live in non-volatile storage with everything that implies.</p>" },

// ---------------------------------------------------------- emb-pwm
{ t: "emb-pwm", d: 2, q: "Timer clock 168 MHz, PWM 20 kHz. Roughly how many bits of duty resolution?", o: ["8", "13", "16", "20"], a: 1, why: "<p>Levels equal the timer clock divided by the PWM frequency, so 168 MHz over 20 kHz is 8400 levels, which is between 2^13 and 2^14.</p><p>Raise the PWM frequency to 200 kHz for a smaller inductor and it becomes 840 levels, under 10 bits.</p><p>Switching frequency, magnetic component size, audible noise and control resolution are all the same knob, and being able to state that relationship is what lets you have the conversation with whoever is choosing the inductor.</p>" },
{ t: "emb-pwm", d: 2, q: "What does enabling preload on the compare register prevent?", o: ["Overflow", "A partial cycle of the wrong width when duty changes mid-period", "Dead time violations", "Clock jitter"], a: 1, why: "<p>Writing the compare register directly can change the threshold after the counter has already passed the old value, so that one period comes out the wrong length.</p><p>On a motor that is a current spike; on an LED it is a visible flicker.</p><p>Preload buffers the write until the next update event. The default is often off, and the resulting glitch is intermittent and load dependent, which makes it unpleasant to chase.</p>" },
{ t: "emb-pwm", d: 3, q: "Why must half-bridge dead time be generated in hardware?", o: ["It is more precise", "A software gap depends on interrupt timing, and one badly placed interrupt causes shoot-through", "Software cannot measure it", "To save code space"], a: 1, why: "<p>Shoot-through is both transistors conducting simultaneously, shorting the supply through the bridge. It destroys parts.</p><p>The gap has to be guaranteed, and nothing about software timing is guaranteed once interrupts exist. Advanced timers insert it in hardware.</p><p>The value comes from the transistor's turn-off time plus gate driver propagation delay, with margin.</p>" },
{ t: "emb-pwm", d: 3, q: "What does centre-aligned PWM buy over edge-aligned?", o: ["More resolution", "Lower harmonic content, and an update event at a predictable point that makes a good ADC trigger", "Simpler configuration", "Higher frequency"], a: 1, why: "<p>It costs half the resolution for the same clock and frequency, so it has to buy something.</p><p>The symmetric pulse has lower harmonic content, which matters for EMC and for audible noise in motor drive.</p><p>The second benefit is the one worth naming: triggering the ADC from the timer so motor current is sampled mid-pulse, when the switching noise is quiet, is standard practice in motor control.</p>" },
{ t: "emb-pwm", d: 2, q: "You filter PWM with a single RC to make an analogue output. Why can you not get both low ripple and fast settling?", o: ["You can", "Ripple falls with larger RC, but settling time is several times RC, so they oppose each other", "The PWM frequency is fixed", "RC filters are lossy"], a: 1, why: "<p>One component value, two requirements pulling in opposite directions.</p><p>The escapes are a second RC stage, which improves the trade at the cost of more settling, or a higher PWM frequency, which costs duty resolution as levels equal clock over frequency.</p><p>The honest summary: PWM plus RC is excellent for a slowly changing bias or reference and a poor choice for a signal that has to move quickly. For that, use a real DAC.</p>" },
{ t: "emb-pwm", d: 2, q: "Setting the compare register equal to the period does not give a fully-on output. Why?", o: ["A hardware fault", "The comparison usually still leaves one clock of off time", "The preload is disabled", "The period is wrong"], a: 1, why: "<p>The output is driven by comparing a counter against a threshold, and at equality there is typically one clock where the condition is not yet met.</p><p>Many timers need the compare value set above the period, or the channel forced on, to reach a true 100 per cent.</p><p>It matters where full output is a real operating point, such as a heater or a buck converter at maximum duty, and it presents as a small unexplained deficit at the top of the range.</p>" },
{ t: "emb-pwm", d: 2, q: "Duty jumps to full output when you meant to command zero. What is the likely cause?", o: ["Preload disabled", "An unsigned duty variable underflowing and wrapping to a very large value", "Dead time", "The timer clock"], a: 1, why: "<p>A subtraction that takes an unsigned value below zero wraps to near the type's maximum, and the timer accepts it without complaint.</p><p>Full output when zero was intended is dangerous on anything that moves or heats, which makes this worth a clamp rather than a comment.</p><p>It is the same class as the negative length passed to memcpy: signed reasoning meeting unsigned use.</p>" },
{ t: "emb-pwm", d: 1, q: "What is PWM, mechanically?", o: ["A DAC", "A timer comparing a counter against a threshold", "A filter", "A frequency synthesiser"], a: 1, why: "<p>Almost everything about PWM follows from that one sentence: the resolution is how many counts fit in a period, the glitch on update is the counter having passed the old threshold, and full-on is an equality that never quite occurs.</p><p>Holding that model means you can reason about a new part's timer without reading the whole chapter.</p>" },
{ t: "emb-pwm", d: 3, q: "Why is the timer update event a good moment to trigger an ADC in motor control?", o: ["The CPU is idle", "It lands at a predictable point where switching noise is quiet, so current is sampled cleanly", "It is the fastest point", "The DMA is free then"], a: 1, why: "<p>Current measurement in a switching bridge is dominated by the switching transients, so when you sample matters more than how well you sample.</p><p>Centre-aligned PWM puts the update event mid-pulse, away from the edges, and triggering the ADC from it gets the measurement in the quiet window automatically.</p><p>Doing it in software would put the sample wherever the handler happened to run, which is exactly the jitter problem from the sampling lesson.</p>" },
{ t: "emb-pwm", d: 2, q: "You need finer duty resolution but the PWM frequency is fixed by the magnetics. What is the option?", o: ["Nothing", "Increase the timer clock", "Use centre alignment", "Enable preload"], a: 1, why: "<p>Levels equal timer clock divided by PWM frequency, so with the frequency fixed the only remaining term is the clock.</p><p>Many parts allow the timer to run from a PLL output faster than the peripheral bus specifically for this reason.</p><p>Centre alignment goes the wrong way, halving the resolution, and preload addresses glitching rather than resolution.</p>" },

// ---------------------------------------------------------- emb-filter-fw
{ t: "emb-filter-fw", d: 3, q: "Why does y += (x - y) >> K stop tracking a slowly changing input?", o: ["Overflow", "When (x - y) is smaller than 2^K, the shift truncates to zero and the state stops moving", "The gain is wrong", "It does not"], a: 1, why: "<p>The increment becomes zero while the error is still non-zero, so the output plateaus permanently below the input.</p><p>Truncation towards negative infinity also biases negative values downward, so the filter is asymmetric.</p><p>The fix is to keep the accumulator scaled up by 2^K and shift only on output: <code>acc += x - (acc &gt;&gt; K); return acc &gt;&gt; K;</code>. Almost everyone writes the first version, and most do not notice why it sticks.</p>" },
{ t: "emb-filter-fw", d: 2, q: "What is the appeal of a one-pole EMA in firmware?", o: ["Linear phase", "One shift, one state variable, and a single tuning knob", "It removes spikes", "It has no delay"], a: 1, why: "<p>No coefficient table, no history buffer, and no multiply needed on parts without one. The time constant is roughly 2^K samples, so K is the only thing to choose and each increment halves the bandwidth.</p><p>What it does not give you is linear phase, so different frequencies are delayed differently.</p><p>And it does not remove spikes: like any linear filter it smears them across its impulse response.</p>" },
{ t: "emb-filter-fw", d: 3, q: "A moving average of N samples has nulls where?", o: ["At DC", "At multiples of the sample rate divided by N", "At the Nyquist frequency", "Nowhere"], a: 1, why: "<p>That is a property you can aim. Average exactly one mains period's worth of samples and the nulls land on 50 Hz and its harmonics, removing hum outright.</p><p>It is often a better answer than a notch filter, and it costs nothing beyond choosing N deliberately.</p><p>The same reasoning applies to averaging over an integer number of interference periods generally.</p>" },
{ t: "emb-filter-fw", d: 2, q: "Which filter removes an occasional wild sample rather than spreading it?", o: ["EMA", "Median", "Moving average", "Any of them"], a: 1, why: "<p>Every linear filter spreads a spike across its impulse response, turning one bad sample into several slightly bad ones.</p><p>A median of three or five discards it entirely, because a single outlier cannot be the middle value.</p><p>Use it where the noise is impulsive: a mechanical contact, a channel disturbed by switching, a sensor with occasional bad frames. Follow with an EMA if you also want smoothing.</p>" },
{ t: "emb-filter-fw", d: 2, q: "In what order should despiking, calibration and smoothing be applied?", o: ["Any order", "Median, then calibration, then EMA", "Calibration, then median, then EMA", "EMA, then median"], a: 1, why: "<p>Despiking first means a wild value cannot be scaled by the calibration into something enormous.</p><p>Smoothing after calibration means the filter operates on a linear quantity, which matters when the calibration is nonlinear: averaging before a nonlinear transform gives a different answer from averaging after.</p><p>Order is a real decision here rather than a preference, and it is worth writing the pipeline out explicitly.</p>" },
{ t: "emb-filter-fw", d: 3, q: "Why is filtering a control loop's feedback to make it look nicer dangerous?", o: ["It is slow", "The added group delay spends phase margin and can destabilise the loop", "It changes the gain", "It uses RAM"], a: 1, why: "<p>Delay is what turns a stable loop into an oscillating one, and every filter adds some. An EMA with a long time constant adds a lot.</p><p>So the smoother signal is bought with stability, and the loop then has to be detuned to tolerate the filter, which loses performance you were not trying to give up.</p><p>The honest options for a noisy loop are to fix the noise at source, sample faster, or accept it.</p>" },
{ t: "emb-filter-fw", d: 2, q: "What does a moving average have that an EMA does not?", o: ["Lower cost", "Exactly linear phase, delaying every frequency by the same amount", "Spike rejection", "A shorter delay"], a: 1, why: "<p>Linear phase means the shape of a waveform is preserved and only shifted, which matters if you are comparing or subtracting two filtered signals.</p><p>The delay is exactly (N-1)/2 samples, so it is known and can be compensated.</p><p>With a running sum it also costs one add, one subtract and one shift per sample regardless of N, provided N is a power of two.</p>" },
{ t: "emb-filter-fw", d: 1, q: "What is the general argument about filters in firmware?", o: ["They are always best", "It is nearly always the cheapest place to put one and nearly never the best place", "They should be avoided entirely", "Hardware filters are obsolete"], a: 1, why: "<p>A firmware filter is where you end up when the hardware filter was left off the board or the noise turned up late in the project.</p><p>It cannot undo aliasing, it adds delay you may not be able to afford, and it consumes cycles in the loop that has the least headroom.</p><p>None of that makes it wrong, but it does mean reaching for one should follow the questions about what the noise actually is and where it entered.</p>" },
{ t: "emb-filter-fw", d: 2, q: "How is the time constant of y += (x - y) >> K related to K?", o: ["K samples", "Roughly 2^K samples", "K squared samples", "Independent of K"], a: 1, why: "<p>The shift makes the effective alpha 1/2^K, so each increment of K halves the bandwidth and doubles the settling time.</p><p>That coarse granularity is the main limitation of the shift-based form: you can have 16 samples or 32, but nothing between.</p><p>Where you need finer control, use a multiply with a Q-format coefficient, at the cost of needing a multiplier.</p>" },
{ t: "emb-filter-fw", d: 3, q: "Why keep a moving average's window a power of two?", o: ["For accuracy", "The divide becomes a shift, so the cost is one add, one subtract and one shift per sample", "It improves the nulls", "It is required"], a: 1, why: "<p>With a running sum the only per-sample work is adding the new value, subtracting the one leaving the window, and dividing by N.</p><p>A power-of-two N turns that division into a shift, and the index wrap into a mask, so the whole filter is a handful of instructions regardless of window length.</p><p>The tension is with placing nulls on mains, which wants N to match an interference period rather than a power of two, and you sometimes have to choose.</p>" },

// ---------------------------------------------------------- emb-averaging
{ t: "emb-averaging", d: 1, q: "Averaging 100 independent samples reduces random noise by what factor?", o: ["100", "10", "50", "2"], a: 1, why: "<p>The square root of N, so 100 samples give a factor of 10.</p><p>The practical consequence is diminishing returns: going from 1 to 16 samples buys a factor of 4, and going from 16 to 256 buys another factor of 4 for sixteen times the work.</p><p>There is usually a sensible stopping point well before the number people reach for.</p>" },
{ t: "emb-averaging", d: 2, q: "Why does averaging 16 samples do almost nothing against mains hum?", o: ["Mains is too strong", "The samples are not independent: over a fraction of a cycle you are averaging a nearly constant offset", "16 is too few", "Mains is above Nyquist"], a: 1, why: "<p>The square root rule requires uncorrelated noise, and a slow periodic interference is highly correlated across a short window.</p><p>The fix is not more samples but the right number: average over exactly one interference period, or an integer number of them, and it cancels rather than merely reducing.</p><p>That is the same observation as placing a moving average's nulls on 50 Hz.</p>" },
{ t: "emb-averaging", d: 2, q: "How many samples does oversampling need to gain 2 extra bits?", o: ["4", "16", "32", "256"], a: 1, why: "<p>It is 4^n samples for n extra bits, so two bits costs sixteen and four bits costs 256.</p><p>Sum them and shift right by n, not by the number of samples: sixteen samples summed and shifted by 2 gives a 14-bit result from a 12-bit converter.</p><p>Many ADCs implement this in hardware, summing and shifting for you, so it is worth checking before writing the loop.</p>" },
{ t: "emb-averaging", d: 3, q: "Oversampling a very quiet, perfectly steady input gains nothing. Why?", o: ["The ADC is too slow", "There must be about one LSB of uncorrelated noise, or every sample returns the identical count", "The reference drifts", "The sum overflows"], a: 1, why: "<p>Without noise, sixteen conversions of a steady input give sixteen identical numbers, the sum is exactly sixteen times one of them, and the shift returns you to where you started with no information added.</p><p>The noise is what makes the input land on different sides of a decision threshold on different conversions, and that distribution is what carries the extra resolution.</p><p>So a quiet input is precisely where the technique fails, which is the opposite of the intuition. This is why it is sometimes described as requiring dither.</p>" },
{ t: "emb-averaging", d: 2, q: "Averaging a systematic offset gives you what?", o: ["Zero", "The offset, more precisely", "Half the offset", "Random noise"], a: 1, why: "<p>Averaging only reduces the random component. A bias is present in every sample identically, so it survives untouched.</p><p>Only calibration against a known reference removes it.</p><p>Distinguishing random from systematic error is what decides whether the answer is more samples or a better reference, and it is a distinction worth making explicitly rather than reaching for averaging by reflex.</p>" },
{ t: "emb-averaging", d: 2, q: "What is wrong with summing 16 twelve-bit readings into a uint16_t?", o: ["Nothing", "It reaches 65,520, so it just fits and breaks the moment someone changes 16 to 32", "It is too slow", "The shift is wrong"], a: 1, why: "<p>Sixteen readings of 4095 sum to 65,520 against a maximum of 65,535, so the code is correct and has almost no margin.</p><p>Anyone increasing the sample count, or moving to a 14-bit ADC, gets silent wraparound and nonsense readings.</p><p>Using uint32_t costs nothing and removes the trap entirely, which is the right trade for a variable nobody will think about again.</p>" },
{ t: "emb-averaging", d: 3, q: "Why report min and max alongside an average?", o: ["For display", "Peak excursion is what a safety threshold cares about, and the mean hides it entirely", "It is faster", "For coverage"], a: 1, why: "<p>A mechanical limit or a safety threshold is crossed by a peak, not by an average, so a system that only reports the mean cannot see the event that matters.</p><p>The spread is also a health indicator: a channel whose noise has doubled is telling you something about a connection, while its mean looks perfectly normal.</p><p>It is the same argument as counting bus retries. Anything the firmware smooths away is information you have chosen to discard.</p>" },
{ t: "emb-averaging", d: 2, q: "The quantity being measured drifts during the averaging window. What do you actually get?", o: ["The current value", "The mean of a changing quantity, which is not the value at any particular instant", "The initial value", "The final value"], a: 1, why: "<p>The number is the average over the window, so it corresponds to the middle of the window at best, and to nothing at all if the drift is not linear.</p><p>That matters when something downstream timestamps the result, because the timestamp and the value refer to different moments.</p><p>It is a reason to keep windows short relative to the rate of change, and to timestamp the middle of the window rather than its end if precision matters.</p>" },
{ t: "emb-averaging", d: 1, q: "What is the difference between averaging for noise and oversampling for resolution?", o: ["None", "Averaging reduces noise by root N; oversampling gains bits, needs 4^n samples and requires dither", "Oversampling is faster", "Averaging needs hardware support"], a: 1, why: "<p>The arithmetic looks similar and the requirements are different, which is why conflating them causes trouble.</p><p>Averaging works on any noisy signal and gives a cleaner version of the same resolution. Oversampling adds resolution, but only when there is at least about one LSB of uncorrelated noise to dither the decision.</p><p>Knowing which one you are doing tells you whether a quiet input is good news or the reason it is not working.</p>" },
{ t: "emb-averaging", d: 2, q: "Where does averaging cost you in a control loop?", o: ["Nowhere", "It adds delay, which spends phase margin", "It changes the setpoint", "It increases noise"], a: 1, why: "<p>An average of N samples delays the signal by roughly (N-1)/2 samples, and delay in feedback is what turns a stable loop into an oscillating one.</p><p>So the cleaner measurement is paid for in stability, and the loop then has to be detuned to tolerate it.</p><p>This is why averaging is a much safer choice for logging and reporting than for feedback, and why the decision should be made per consumer rather than once at the driver.</p>" }

]);
