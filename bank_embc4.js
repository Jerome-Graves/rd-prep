BANKS["Embedded C"] = BANKS["Embedded C"].concat([

// ---------------------------------------------------------- emb-cm-model
{ t: "emb-cm-model", d: 1, q: "Which stack pointer does handler mode always use?", o: ["PSP", "MSP", "Whichever thread mode used", "It depends on CONTROL"], a: 1, why: "<p>Handler mode is always privileged and always uses MSP. There is no configuration for it.</p><p>Thread mode can use either, selected by a bit in CONTROL. Bare metal firmware usually leaves everything on MSP; an RTOS gives each task its own stack on PSP.</p><p>The practical value of that split is isolation: a task stack overflow corrupts that task's stack rather than immediately taking down interrupt handling, because handlers are on a different stack entirely.</p>" },
{ t: "emb-cm-model", d: 2, q: "Why must the address in a function pointer have bit 0 set?", o: ["For the alignment of the target address", "It signals Thumb state, the only state", "It marks the end of the vector table", "It is a parity bit over the address"], a: 1, why: "<p>The encoding is inherited from cores that could switch between ARM and Thumb state, where bit 0 of a branch target selected which. Cortex-M only executes Thumb, so the bit must always be 1.</p><p>The compiler and linker set it for you, so it never matters until you construct an address by hand. A bootloader computing an entry point as <code>APP_BASE + 0x200</code> gets an even address and faults.</p><p>The symptom is a UsageFault with INVSTATE set. Reading the reset vector out of the application's own vector table avoids it, because that word already has the bit set.</p>" },
{ t: "emb-cm-model", d: 2, q: "What distinguishes thread mode from handler mode?", o: ["Handler mode runs at a higher clock", "Handler mode is always privileged, on MSP", "Handler mode has extra banked registers", "Thread mode cannot be interrupted at all, ever"], a: 1, why: "<p>That is the whole distinction, and everything else follows from it. The core is in handler mode whenever an exception is executing and thread mode otherwise.</p><p>IPSR, part of xPSR, holds the number of the exception currently executing, and reads as zero in thread mode. That is how code can tell which mode it is in at runtime, which matters for an assert that a function is not being called from an ISR.</p>" },
{ t: "emb-cm-model", d: 2, q: "Under the ARM calling convention, where do the first four arguments go?", o: ["On the stack", "R0 to R3", "R4 to R7", "In memory via R12"], a: 1, why: "<p>R0 to R3 carry the first four arguments, further ones go on the stack, and the return value comes back in R0.</p><p>This is why a fault register dump is more useful than it looks: those registers are in the exception stack frame, so you often get the arguments that caused the fault handed to you.</p><p>It is also why R0 to R3 are in the automatically stacked set. They are caller-saved, meaning a called function may destroy them, so stacking them is exactly what makes a plain C function a legal handler.</p>" },
{ t: "emb-cm-model", d: 1, q: "Which register holds the return address during an ordinary function call?", o: ["R12", "R13", "R14", "R15"], a: 2, why: "<p>R14 is the link register. A <code>bl</code> instruction puts the return address there, and <code>bx lr</code> returns.</p><p>R13 is the stack pointer, R15 the program counter, R12 a scratch register the linker may use for veneers.</p><p>On exception entry LR holds something quite different: EXC_RETURN, a magic value encoding which mode and stack to return to, rather than any real address.</p>" },
{ t: "emb-cm-model", d: 3, q: "What does the CONTROL register select?", o: ["How the interrupt priorities are grouped", "Thread mode's stack, and its privilege", "The clock source feeding the core", "Where the vector table base sits"], a: 1, why: "<p>Two bits that matter: one chooses MSP or PSP for thread mode, the other drops thread mode to unprivileged.</p><p>Both are what an RTOS uses to run tasks on their own stacks and, optionally, without access to system registers.</p><p>Changing CONTROL is a context-changing operation, so it needs an ISB afterwards or the change may not be in effect for the next instruction.</p>" },
{ t: "emb-cm-model", d: 2, q: "Unprivileged code writes to an NVIC register. What happens?", o: ["It works: the NVIC is always writable", "It faults, rather than doing nothing", "It is silently ignored by the bus", "It resets the whole part immediately"], a: 1, why: "<p>The system regions are privileged-access only, so the attempt raises a fault rather than being quietly dropped. That is the useful behaviour: a violation is loud and located.</p><p>Most small firmware runs entirely privileged and never encounters this. It becomes relevant when tasks are dropped to unprivileged, which is the point of combining CONTROL with the MPU.</p>" },
{ t: "emb-cm-model", d: 3, q: "A fault dump shows a faulting address of 0x0000002C. What is the most likely cause?", o: ["A stack overflow into low memory", "A null pointer plus a member offset", "The vector table has been corrupted somehow", "A DMA transfer to a bad descriptor"], a: 1, why: "<p><code>p-&gt;field</code> on a null pointer computes 0 plus the member's offset, so the address is small but not zero. 0x2C is a plausible offset into a struct.</p><p>Recognising the shape saves time: a small non-zero faulting address almost always means null plus an offset rather than a wild pointer.</p><p>On many parts address zero is valid flash, so the access can even succeed and return garbage. An MPU region with no access at address zero is what turns that into an immediate fault.</p>" },
{ t: "emb-cm-model", d: 1, q: "Which instruction set does Cortex-M execute?", o: ["ARM only", "Thumb only", "Both, switchable", "Thumb-1 only"], a: 1, why: "<p>Thumb only, and on M3 and above that means Thumb-2, which mixes 16-bit and 32-bit instruction encodings for a good balance of density and capability.</p><p>Because there is no other state to switch to, the state bit in a branch target is always 1, which is the origin of the odd-address rule for function pointers.</p>" },
{ t: "emb-cm-model", d: 2, q: "Where does IPSR read as zero?", o: ["In handler mode", "In thread mode", "Only after reset", "Never"], a: 1, why: "<p>IPSR holds the number of the exception currently executing, so it is zero exactly when no exception is executing, meaning thread mode.</p><p>That gives you a cheap runtime check for whether code is running in interrupt context, which is worth an assert in any function that must not be called from an ISR.</p><p>CMSIS exposes it as <code>__get_IPSR()</code>.</p>" },

// ---------------------------------------------------------- emb-cm-exceptions
{ t: "emb-cm-exceptions", d: 2, q: "How many registers does the core stack automatically on exception entry?", o: ["Four", "Eight", "Sixteen", "None"], a: 1, why: "<p>Eight: R0, R1, R2, R3, R12, LR, PC and xPSR, with R0 at the lowest address.</p><p>Those are exactly the caller-saved registers under the ARM calling convention, which is the point. A C function is already allowed to destroy them, so a plain C function is a legal handler with no assembly wrapper and no interrupt attribute.</p><p>Anything else the handler uses, R4 to R11, the compiler saves itself as it would in any function.</p>" },
{ t: "emb-cm-exceptions", d: 2, q: "Where in the stacked frame is the faulting instruction address?", o: ["At SP + 0x00", "At SP + 0x18", "At SP + 0x1C", "It is not stacked"], a: 1, why: "<p>The frame runs R0, R1, R2, R3, R12, LR, PC, xPSR from low address upward, so PC sits at offset 0x18, which is <code>frame[6]</code> if you treat the pointer as a uint32_t array.</p><p>That is the single most useful number in a fault handler. Decoded against the ELF from that exact build, it gives you the file and line.</p><p>xPSR is at 0x1C and the stacked LR at 0x14, which together give you some idea of the call path.</p>" },
{ t: "emb-cm-exceptions", d: 3, q: "What is in LR when a handler begins?", o: ["The address the handler returns to", "EXC_RETURN, encoding mode and stack", "The exception number being served", "A copy of the stacked program counter"], a: 1, why: "<p>Not an address at all. The top bits are all ones and the low bits encode the return: 0xFFFFFFF9 means thread mode on MSP, 0xFFFFFFFD means thread mode on PSP, 0xFFFFFFF1 means handler mode.</p><p>Branching to that value is what triggers unstacking, which is why a handler can return with an ordinary <code>bx lr</code>.</p><p>The practical use is bit 2, which tells a fault handler which stack the frame is on. Get that wrong and you decode a PC out of the wrong stack entirely.</p>" },
{ t: "emb-cm-exceptions", d: 2, q: "What does tail-chaining do?", o: ["Runs two handlers at the same time", "Skips the unstack and immediate restack", "Reorders the pending priorities", "Merges two sources onto one vector"], a: 1, why: "<p>If another exception is pending when a handler returns, the core does not pop the eight registers only to push the same eight again. It goes straight into the next handler.</p><p>Roughly six cycles instead of about twenty-four, and entirely invisible to your code.</p><p>It matters when you are working out whether an interrupt load is sustainable, because the naive sum of per-interrupt entry costs overestimates significantly under back-to-back conditions.</p>" },
{ t: "emb-cm-exceptions", d: 3, q: "A higher priority interrupt arrives while the core is still stacking for a lower one. What happens?", o: ["It waits until the first one returns", "The core switches, reusing the stacking", "Both exceptions are lost entirely", "The lower priority one is discarded"], a: 1, why: "<p>The stacked frame is the same regardless of which handler runs, so there is nothing to redo. The core simply changes which vector it fetches.</p><p>The lower priority exception stays pending and runs afterwards, usually by tail-chaining, so nothing is lost.</p><p>Like tail-chaining, this is free and invisible, and it is part of why Cortex-M interrupt latency is low and predictable enough to put in a timing analysis.</p>" },
{ t: "emb-cm-exceptions", d: 3, q: "What is lazy FPU stacking?", o: ["Delaying the interrupt until the FPU is idle", "Reserving the space, filling it on demand", "Disabling the FPU inside every handler", "Saving the FPU registers in software"], a: 1, why: "<p>There are seventeen more registers to preserve on a part with an FPU, and stacking them on every interrupt would be expensive when most handlers never touch a float.</p><p>So the core reserves the space and sets a flag, and only actually saves them if the handler executes a floating point instruction.</p><p>Two consequences. The frame is 104 bytes rather than 32 whenever FPU context is active, which matters when sizing stacks. And the first handler to touch a float pays a one-off cost, which appears as jitter rather than a constant overhead.</p>" },
{ t: "emb-cm-exceptions", d: 2, q: "The FORCED bit in HFSR is set. What does that mean?", o: ["A genuine HardFault from the start", "A configurable fault that escalated", "The debugger forced the core to halt", "The vector table entry was invalid"], a: 1, why: "<p>A MemManage, BusFault or UsageFault escalates to HardFault when its own handler is disabled, or when it occurs inside a handler of equal or higher priority.</p><p>So most HardFaults are really something more specific with nowhere to go, and the actual cause is still sitting in CFSR.</p><p>Enabling the individual fault handlers in SHCSR during development gives you a much more precise answer than the catch-all.</p>" },
{ t: "emb-cm-exceptions", d: 2, q: "Why does a Cortex-M handler need no special compiler attribute?", o: ["The compiler detects handlers by name", "Hardware stacks exactly what C clobbers", "Handlers run in a separate address space entirely", "The attribute is optional but advised"], a: 1, why: "<p>On many other architectures an interrupt handler must save extra state that a normal function would not, so the compiler needs to know it is a handler.</p><p>On Cortex-M the hardware has already saved the caller-saved set, and the callee-saved set is the compiler's normal responsibility in any function. So the two halves cover everything.</p><p>That is a genuinely nice piece of architecture design, and it is why the vector table can just hold ordinary C function pointers.</p>" },
{ t: "emb-cm-exceptions", d: 3, q: "Your fault handler reads BFAR and gets a plausible-looking address, but it points somewhere unrelated. What did you forget?", o: ["To enable the BusFault handler first", "To check BFARVALID in CFSR first", "To clear the pending bit before reading", "A memory barrier before the read"], a: 1, why: "<p>BFAR and MMFAR are only meaningful when the corresponding valid bit is set. Otherwise they hold whatever was left from a previous fault, which reads as a perfectly plausible address.</p><p>Checking BFARVALID or MMARVALID first is the difference between a real answer and confidently chasing the wrong memory.</p><p>The valid bits live in CFSR alongside the category bits, so you are reading that register anyway.</p>" },
{ t: "emb-cm-exceptions", d: 1, q: "In the stacked frame, which register is at the lowest address?", o: ["xPSR", "PC", "R0", "LR"], a: 2, why: "<p>R0 sits at SP + 0x00 and xPSR at SP + 0x1C, so the frame reads R0, R1, R2, R3, R12, LR, PC, xPSR going up in memory.</p><p>Knowing the order lets you index the frame directly as an array in a C fault handler, which is how you extract the faulting PC as <code>frame[6]</code>.</p>" },

// ---------------------------------------------------------- emb-cm-nvic
{ t: "emb-cm-nvic", d: 1, q: "Which Cortex-M interrupt priority preempts the other?", o: ["Priority 7 preempts priority 2", "Priority 2 preempts priority 7", "Neither preempts the other", "Whichever one was enabled first"], a: 1, why: "<p>Lower number means more urgent. Priority 0 is the highest configurable priority.</p><p>This is the opposite of most people's intuition and it is worth stating explicitly whenever a team designs a priority scheme, because the mistake is silent: everything still runs, just not in the order anyone intended.</p>" },
{ t: "emb-cm-nvic", d: 2, q: "The priority register is 8 bits wide. How many does silicon usually implement?", o: ["All eight, on every Cortex-M part", "Only the upper few, often 3 or 4", "Only the lower three bits", "It varies from interrupt to interrupt"], a: 1, why: "<p>Only the upper bits are implemented, and the rest read as zero. Three bits is common, giving eight distinct levels; four gives sixteen.</p><p>Design a scheme around sixteen levels on a part with eight and pairs of priorities silently collapse into one, so things you expected to preempt simply do not.</p><p><code>__NVIC_PRIO_BITS</code> in the device header tells you how many you have. Use the CMSIS setter, which shifts your value into the implemented bits; writing the register directly does not.</p>" },
{ t: "emb-cm-nvic", d: 3, q: "What does the subpriority field actually do?", o: ["It causes preemption within a group", "Breaks ties between pending exceptions", "It sets the handler's vector address", "It selects which stack pointer will be used"], a: 1, why: "<p>Only the preempt priority field decides preemption. Subpriority never lets one interrupt interrupt another.</p><p>AIRCR.PRIGROUP decides where the split between the two fields falls within the implemented bits.</p><p>This catches people who configure many distinct priorities, observe that nothing preempts anything, and cannot see why. Under an RTOS, leave PRIGROUP alone: FreeRTOS expects all bits to be preempt priority and its configASSERT checks will tell you if you changed it.</p>" },
{ t: "emb-cm-nvic", d: 2, q: "What does __disable_irq() actually set?", o: ["BASEPRI", "PRIMASK", "FAULTMASK", "The NVIC enable registers"], a: 1, why: "<p>PRIMASK, which blocks every configurable-priority interrupt at once. NMI and HardFault still get through.</p><p>It is the blunt instrument. BASEPRI is the finer one: it masks only interrupts whose priority number is greater than or equal to the value written, so urgent interrupts keep running.</p><p>FAULTMASK additionally blocks HardFault, which you almost never want, because it turns a diagnosable fault into a lockup.</p>" },
{ t: "emb-cm-nvic", d: 3, q: "Why does an RTOS define a maximum syscall interrupt priority?", o: ["To cap how many interrupts you may use", "Above it, never masked, so latency holds", "To reserve vectors for the kernel", "To let the kernel manage power states"], a: 1, why: "<p>The kernel protects its data structures with BASEPRI rather than PRIMASK, masking only up to that level.</p><p>So anything more urgent is never masked at all, which gives it low, bounded latency regardless of what the kernel is doing. That is exactly what a motor control or timing-critical handler needs.</p><p>The price is that such a handler cannot call any RTOS function, because the kernel's data structures are not protected against it. It has to communicate through something lock-free instead.</p>" },
{ t: "emb-cm-nvic", d: 2, q: "What is the difference between a pending and an active interrupt?", o: ["Nothing: they are the same state", "Pending is asserted; active is running", "Pending means the interrupt is disabled", "Active means the handler has finished"], a: 1, why: "<p>Both states are visible in NVIC registers and the distinction is diagnostic gold.</p><p>Pending but never active means something is masking it: its enable bit, PRIMASK, BASEPRI, or a higher priority handler that never returns.</p><p>Repeatedly active and immediately re-entered means the peripheral condition was never cleared, which is the classic forever-reasserting interrupt.</p>" },
{ t: "emb-cm-nvic", d: 3, q: "You clear the interrupt source in the peripheral, and the handler runs once more anyway. Why?", o: ["A compiler bug in the handler code", "The NVIC pending bit had already latched", "The interrupt priority is set wrong", "The vector table points to the wrong slot"], a: 1, why: "<p>The peripheral's request and the NVIC's pending bit are separate pieces of state. Once the NVIC has latched the request, clearing the peripheral flag does not retract it.</p><p>So you get one spurious entry into the handler, which a well-written handler tolerates because it checks the peripheral flags and finds nothing to do.</p><p>If you genuinely need to suppress it, clear the NVIC pending bit explicitly as well, and note the related trap that clearing an enable bit needs DSB and ISB or an already-pending interrupt can still be taken.</p>" },
{ t: "emb-cm-nvic", d: 2, q: "Why pass 0 to 7 to NVIC_SetPriority rather than writing the register directly?", o: ["It is faster than a direct write", "CMSIS shifts it into the implemented bits", "It validates that the range is legal", "It disables interrupts while it writes"], a: 1, why: "<p>The implemented bits are the upper ones, so a raw value of 1 written directly lands in an unimplemented bit and is ignored, while CMSIS turns it into 0x20 on a three-bit part.</p><p>Using the setter also means the same source works on parts with different numbers of priority bits, which is the portability argument.</p><p>Writing the register directly is a genuine and quiet source of priority schemes that do not behave as designed.</p>" },
{ t: "emb-cm-nvic", d: 1, q: "Which exceptions can PRIMASK not block?", o: ["None, it blocks everything", "NMI and HardFault", "SysTick", "PendSV"], a: 1, why: "<p>PRIMASK blocks configurable-priority exceptions. NMI and HardFault have fixed negative priorities and are always taken.</p><p>That is deliberate: a fault that cannot be reported is a lockup with no information, so the architecture keeps the reporting path open.</p><p>FAULTMASK does block HardFault, which is why it is almost never the right tool.</p>" },
{ t: "emb-cm-nvic", d: 2, q: "An interrupt is enabled and pending but its handler never runs. What is the first thing to check?", o: ["That the vector table is correct", "What is masking it: BASEPRI or PRIMASK", "That the peripheral clock is enabled", "That the stack has room for a frame"], a: 1, why: "<p>Pending-but-never-active is specifically a masking or starvation symptom, which is why reading both NVIC state bits is worth doing before anything else.</p><p>The usual culprits are a critical section that was entered and never left, a BASEPRI left set, or a higher priority handler stuck in a loop, often because its own peripheral flag was never cleared.</p><p>A wrong vector table would show a different symptom: the interrupt goes active and dispatches into the wrong code, typically a default handler that loops.</p>" },

// ---------------------------------------------------------- emb-cm-systick
{ t: "emb-cm-systick", d: 1, q: "How wide is the SysTick counter?", o: ["16 bits", "24 bits", "32 bits", "Vendor dependent"], a: 1, why: "<p>24 bits, which at typical clock rates caps the period at roughly 100 ms. Anything longer needs a vendor timer.</p><p>It lives in the core rather than in the vendor's peripheral block, at a fixed architectural address, which is exactly what makes RTOS ports portable across vendors.</p>" },
{ t: "emb-cm-systick", d: 3, q: "Why is PendSV given the lowest priority in the system?", o: ["The context switch is slow to run", "So a switch cannot run inside another", "It lets the core enter sleep sooner", "CMSIS requires that specific value"], a: 1, why: "<p>A switch that ran inside a device handler could be interrupted partway, and returning from that interrupt would land in a half-switched context.</p><p>Lowest priority guarantees every other pending handler has finished first, so the switch happens at a clean point.</p><p>It costs nothing, because tail-chaining means going from the tick handler straight into PendSV is around six cycles rather than a full unstack and restack.</p>" },
{ t: "emb-cm-systick", d: 2, q: "What does the SysTick handler do when a higher priority task becomes ready?", o: ["Switches to it there and then, in SysTick", "Sets PendSV pending, so it happens later", "Calls the scheduler function directly", "Disables interrupts until it switches"], a: 1, why: "<p>It decides, then defers. Writing PENDSVSET in SCB-&gt;ICSR marks PendSV pending, and because PendSV is the lowest priority it runs only once everything else has finished.</p><p>Separating the decision from the switch is the whole design. The tick handler stays short and the switch happens where nothing else is in flight.</p><p>portYIELD_FROM_ISR is the same mechanism used from a device handler, so a switch happens on the way out of your interrupt rather than waiting for the next tick.</p>" },
{ t: "emb-cm-systick", d: 2, q: "In a context switch, which registers does the PendSV handler save?", o: ["All sixteen", "R4 to R11", "R0 to R3 and R12", "None"], a: 1, why: "<p>The hardware has already stacked R0 to R3, R12, LR, PC and xPSR. PendSV saves the callee-saved half, R4 to R11, then swaps stack pointers, pops the new task's R4 to R11, and returns.</p><p>The hardware then unstacks the remaining eight into the new context.</p><p>That neat split is why an RTOS PendSV handler on Cortex-M is around twenty instructions of assembly rather than a large piece of machinery.</p>" },
{ t: "emb-cm-systick", d: 2, q: "What is the SVC instruction for?", o: ["Saving the caller's context to the stack", "A synchronous call into privileged code", "Setting the priority of an exception", "Entering a low power sleep state"], a: 1, why: "<p>It raises a synchronous exception, exactly like a system call on a desktop operating system. The immediate value in the instruction says which service is wanted.</p><p>The handler recovers that number by reading the instruction back out of the stream using the stacked PC, which is a neat use of the exception frame.</p><p>Most bare metal firmware never uses it. It becomes relevant the moment tasks run unprivileged and need to ask the kernel to do something they cannot.</p>" },
{ t: "emb-cm-systick", d: 3, q: "Why does tickless idle need a timer other than SysTick?", o: ["SysTick ticks too fast for long sleeps", "SysTick stops in most sleep modes", "SysTick is only 24 bits wide", "It does not: SysTick is enough"], a: 1, why: "<p>The width is a limitation, but stopping in sleep is the blocker. To sleep across a hundred ticks you need something always-on, typically an RTC.</p><p>You then have to correct the tick count on wake, because the kernel's idea of elapsed time came from a counter that was not running.</p><p>Getting that correction wrong produces timing that is subtly wrong only after a sleep, which is an unpleasant class of bug to chase.</p>" },
{ t: "emb-cm-systick", d: 2, q: "Why is PendSV described as having no hardware source?", o: ["It is not really an exception at all", "Software sets its pending bit, nothing else", "It is triggered by the SysTick hardware", "It only ever runs at reset time"], a: 1, why: "<p>It exists purely so software can request an exception that runs later, at a controlled priority. Nothing in the peripheral space asserts it.</p><p>That is what makes it the right vehicle for a context switch: you get to choose when it becomes pending, and its priority decides when it actually runs.</p><p>SVC is its synchronous counterpart, raised immediately by an instruction rather than deferred.</p>" },
{ t: "emb-cm-systick", d: 3, q: "What is the risk of reading the SysTick current value register while debugging?", o: ["There is no risk in reading it", "Reading clears COUNTFLAG, losing an overflow", "It stops the counter until written", "It resets the reload value to zero"], a: 1, why: "<p>COUNTFLAG indicates that the counter reached zero since it was last read, and the read clears it.</p><p>So a debugger watch expression, or a stray diagnostic read, can consume the flag that your code was about to test, and a tick goes missing.</p><p>It is the same class of hazard as a read-to-clear peripheral register, and it is a reason to be careful about what you leave in a watch window.</p>" },
{ t: "emb-cm-systick", d: 2, q: "Why does portYIELD_FROM_ISR exist rather than waiting for the next tick?", o: ["It is shorter to type than the alternative", "The readied task runs on the way out", "It disables interrupts while switching", "It clears the interrupt's pending bit"], a: 1, why: "<p>If an interrupt readies a high priority task, waiting for the next tick can add up to a full tick period of latency, which on a 1 ms tick is an eternity for a control loop.</p><p>Pending PendSV from the handler means the switch happens as the interrupt returns, via tail-chaining.</p><p>That is often the difference between meeting and missing a deadline, and it is why the FromISR APIs return a woken flag at all.</p>" },
{ t: "emb-cm-systick", d: 1, q: "Where does SysTick live?", o: ["In the vendor's peripheral block", "In the core, at a fixed address", "In external memory-mapped space", "Inside the NVIC's register block"], a: 1, why: "<p>It is part of the Cortex-M core itself, in the Private Peripheral Bus region at 0xE0000000, so it is at the same address on every vendor's part.</p><p>That is precisely why an RTOS can provide one port per architecture rather than one per chip. The tick source, the context switch mechanism and the interrupt controller are all architectural.</p>" },

// ---------------------------------------------------------- emb-cm-memory
{ t: "emb-cm-memory", d: 2, q: "What lives in the region starting at 0xE0000000?", o: ["The vendor peripheral register block area", "The Private Peripheral Bus: NVIC, SCB", "Externally attached SRAM or SDRAM", "The internal flash memory array"], a: 1, why: "<p>Everything architectural, at the same address on every Cortex-M. That is what lets CMSIS define <code>SCB-&gt;CFSR</code> once for all vendors.</p><p>A faulting address in that range means code touched a core register it should not have, which usually means unprivileged code reaching for a system register.</p><p>Worth memorising alongside 0x20000000 for SRAM and 0x40000000 for vendor peripherals, because recognising the region tells you a lot about a fault address immediately.</p>" },
{ t: "emb-cm-memory", d: 3, q: "You have just written SCB->VTOR. What must follow?", o: ["Nothing", "DSB then ISB", "ISB only", "A critical section"], a: 1, why: "<p>DSB waits until the write has actually completed. ISB flushes the pipeline so instructions fetched after it see the new state.</p><p>Without them the change may not be in effect when the next instruction runs, and you get a fault a few instructions later that appears to have nothing to do with the write.</p><p>The same DSB-then-ISB rule applies to enabling or disabling the MPU, changing CONTROL, and cache operations on an M7.</p>" },
{ t: "emb-cm-memory", d: 2, q: "What is the difference between Normal and Device memory?", o: ["Device memory is simply slower to reach", "Normal may be reordered; Device may not", "Device memory is always read-only", "Normal is cached, Device is not, and no more"], a: 1, why: "<p>Reading RAM twice has no side effect, so the hardware is free to optimise. Reading a peripheral register twice may pop a FIFO or clear a flag, so it must not be.</p><p>The peripheral region is Device by default, which is why straightforward register writes on Cortex-M usually behave exactly as written.</p><p>It is also why <code>volatile</code> alone is sufficient far more often here than on a large application processor, and why that habit quietly stops being sufficient when you move to something with a write buffer and a data cache.</p>" },
{ t: "emb-cm-memory", d: 3, q: "What is the difference between DMB and DSB?", o: ["There is no practical difference", "DMB orders; DSB also waits for completion", "DSB applies to instruction fetch only", "DMB applies only to peripheral accesses"], a: 1, why: "<p>DMB is an ordering guarantee: accesses before it are observed before those after it. It does not stall for completion.</p><p>DSB is stronger. Execution halts until every preceding memory access has actually completed, which is what you need before an operation whose effect depends on that write having landed.</p><p>ISB is a third thing entirely: it flushes the pipeline so subsequent instructions are refetched, which is why context-changing operations need DSB then ISB together.</p>" },
{ t: "emb-cm-memory", d: 3, q: "Which cores implement bit-banding?", o: ["All Cortex-M", "Cortex-M3 and M4 only", "Cortex-M0 and M0+ only", "Cortex-M7 only"], a: 1, why: "<p>M3 and M4 map the lowest megabyte of SRAM and of the peripheral region into alias regions where each word corresponds to one bit. M0, M0+ and M7 do not have it.</p><p>The appeal is atomicity: a single store rather than the read-modify-write of <code>reg |= bit</code>, so an interrupt cannot land in the middle.</p><p>The reasons to avoid it are portability and readability. Prefer the peripheral's own set and clear registers where they exist, and a short critical section otherwise.</p>" },
{ t: "emb-cm-memory", d: 2, q: "Cortex-M4 supports unaligned access. Which instructions still require alignment?", o: ["None: all accesses may be unaligned", "LDM, STM, LDRD and STRD", "Byte loads and stores only", "Every load, but not stores"], a: 1, why: "<p>The multi-word instructions always require alignment, on every Cortex-M, regardless of the unaligned support for single word and halfword accesses.</p><p>The compiler emits them whenever it moves more than one word, which it does routinely for struct copies and register saves.</p><p>That is the real reason <code>__packed</code> on a struct is a correctness risk rather than merely a performance one: the compiler can generate a multi-word access to something the packing has left unaligned.</p>" },
{ t: "emb-cm-memory", d: 3, q: "You disable an interrupt and it fires anyway, once. What is the fix?", o: ["Clear the NVIC pending bit as well", "DSB then ISB after the disable", "Raise the interrupt's priority", "Use PRIMASK rather than the NVIC"], a: 1, why: "<p>The write disabling the interrupt may not have propagated before execution continues, so an already-pending interrupt can still be taken.</p><p>DSB ensures the write has landed and ISB ensures subsequent execution sees the new state.</p><p>It is an obscure bug and it presents as an interrupt firing after you switched it off, which is exactly the kind of thing that makes people distrust the hardware rather than the sequence.</p>" },
{ t: "emb-cm-memory", d: 1, q: "What is at 0x20000000 on a typical Cortex-M?", o: ["Flash", "SRAM", "Peripherals", "The vector table"], a: 1, why: "<p>SRAM. Code starts at 0x00000000, SRAM at 0x20000000, vendor peripherals at 0x40000000, and the Private Peripheral Bus at 0xE0000000.</p><p>Recognising these on sight makes a fault address immediately informative: a small number is a null dereference, 0x2xxxxxxx is RAM, 0x4xxxxxxx is a peripheral, and 0xExxxxxxx is a core register you should not have touched.</p>" },
{ t: "emb-cm-memory", d: 2, q: "Why is the fixed memory map an architectural decision rather than a vendor one?", o: ["It is not; each vendor chooses it", "Core facilities sit at one address", "It saves address space on small parts", "It is a security requirement of the core"], a: 1, why: "<p>If every vendor put the NVIC somewhere different, every piece of core-level software would need per-chip configuration.</p><p>Fixing it means one CMSIS header, one RTOS port per architecture, and one set of debug tools work everywhere.</p><p>Vendors still get 0x40000000 for their own peripherals, which is where all the genuine variation lives, and that is what the device header describes.</p>" },
{ t: "emb-cm-memory", d: 3, q: "Why is volatile more often sufficient on Cortex-M than on a large application processor?", o: ["The compiler behaves quite differently there", "The peripheral region is Device memory", "Cortex-M compilers do not reorder", "It is not; the same rules apply"], a: 1, why: "<p>volatile only ever addresses the compiler. It stops reads being cached in registers and writes being elided, and does nothing about hardware.</p><p>On Cortex-M the peripheral region is Device memory by default and there is typically no data cache in the way, so the compiler was the only problem and volatile solves it.</p><p>Move to something with a write buffer, a data cache or an out-of-order core and the same code needs barriers and cache maintenance as well. This is why 'volatile is enough' is a habit that quietly stops being true.</p>" },

// ---------------------------------------------------------- emb-cm-mpu
{ t: "emb-cm-mpu", d: 1, q: "What does the Cortex-M MPU do?", o: ["Translates virtual to physical addresses", "Permits or denies access, faulting", "Caches memory regions for speed", "Arbitrates DMA access to memory"], a: 1, why: "<p>Permission checking only. There is no address translation, so no virtual memory and no paging.</p><p>A violation raises a MemManage fault at the instruction that caused it, which is the entire value: silent corruption becomes a located error.</p><p>That distinction matters in interviews, because people conflate an MPU with an MMU and the capabilities are quite different.</p>" },
{ t: "emb-cm-mpu", d: 2, q: "On ARMv7-M, what constrains an MPU region's base address?", o: ["Nothing constrains the base address", "It aligns to the region's own size", "It must be four-byte aligned", "It must start inside the SRAM region"], a: 1, why: "<p>Size is a power of two with a 32-byte minimum, and the base must be aligned to that size. A 4 kB region must start on a 4 kB boundary.</p><p>The consequence is that the MPU configuration and the linker script have to be designed together. You cannot protect a stack that the linker placed at an arbitrary address.</p><p>ARMv8-M, meaning Cortex-M23 and M33, uses base and limit registers instead and largely removes this restriction.</p>" },
{ t: "emb-cm-mpu", d: 2, q: "Two MPU regions overlap. Which takes effect?", o: ["The lower numbered", "The higher numbered", "The more restrictive", "Undefined"], a: 1, why: "<p>The highest numbered region wins, and this is deliberately useful rather than an accident.</p><p>It lets you lay down a broad restrictive region and then carve permitted exceptions out of it using higher numbers, which is how you grant a task access to one shared buffer inside an otherwise no-access area.</p>" },
{ t: "emb-cm-mpu", d: 2, q: "What is the cheapest genuinely useful MPU configuration?", o: ["Full isolation between every task", "No-access at zero, to catch null", "Marking the flash region read-only", "Disabling the cache on peripherals"], a: 1, why: "<p>On many parts address zero is a valid flash mapping, so a null pointer dereference currently succeeds and returns garbage that propagates.</p><p>A no-access region there turns it into an immediate MemManage fault at the offending instruction, and it costs a handful of register writes.</p><p>Stack guard regions are the other high-value, low-effort use. Both are a fraction of the work of full task isolation and catch a large share of the bugs that are otherwise hardest to locate.</p>" },
{ t: "emb-cm-mpu", d: 3, q: "How does an MPU stack guard compare with a high water mark check?", o: ["They tell you exactly the same thing", "The guard faults at the overflow itself", "The high water mark is more accurate", "The guard costs more RAM to set up"], a: 1, why: "<p>A high water mark is a measurement taken after the fact. It tells you the minimum free space ever seen, which is useful for sizing but says nothing at the moment of failure.</p><p>A pattern-based overflow check is better but only notices at the next context switch, by which time the corruption has happened.</p><p>A no-access MPU region immediately below the stack faults at the instruction that ran off the end, which is both immediate and located. Use the high water mark for sizing and the guard for catching.</p>" },
{ t: "emb-cm-mpu", d: 2, q: "What must you enable so an MPU violation does not simply become a HardFault?", o: ["The MPU itself, via its enable bit", "The MemManage handler, via MEMFAULTENA", "BASEPRI, set below the fault priority", "The DWT, which reports the address"], a: 1, why: "<p>Configurable faults escalate to HardFault when their own handler is disabled, and they are disabled by default.</p><p>Enabling MEMFAULTENA gives you a MemManage handler with a specific cause in CFSR rather than the catch-all.</p><p>Then read MMFAR for the offending address, but only when MMARVALID is set, because otherwise it holds a stale value that reads as perfectly plausible.</p>" },
{ t: "emb-cm-mpu", d: 3, q: "What must follow enabling or disabling the MPU?", o: ["Nothing", "DSB then ISB", "A context switch", "Clearing CFSR"], a: 1, why: "<p>It is a context-changing operation, so the same rule applies as for VTOR and CONTROL: DSB so the write completes, ISB so subsequent instructions are fetched with the new configuration in effect.</p><p>Without them the very next access may be checked against the old settings, producing a fault that looks unrelated to the change.</p>" },
{ t: "emb-cm-mpu", d: 2, q: "Roughly how many regions does a typical ARMv7-M MPU provide?", o: ["Two", "Eight", "Sixty-four", "Unlimited"], a: 1, why: "<p>Eight is typical, with some Cortex-M7 implementations offering sixteen.</p><p>That scarcity is why you spend them deliberately. A null-catcher and a per-task stack guard already account for two, and full task isolation with separate code, data and peripheral regions consumes most of the rest.</p><p>The overlap rule, where the highest number wins, is partly a response to that scarcity: it lets one broad region plus a few exceptions do the work of many.</p>" },
{ t: "emb-cm-mpu", d: 3, q: "What is the real cost of full task isolation with the MPU?", o: ["Nothing beyond the initial setup time", "Reprogramming regions on every switch", "It requires a full MMU, not an MPU", "It disables interrupts during a switch"], a: 1, why: "<p>Each task needs its own region set, so the switch has to load them, which adds to context switch time.</p><p>Beyond that, every shared buffer and every peripheral a task touches has to be granted explicitly, so drivers and ownership need designing rather than assuming.</p><p>It is real work and it wants a reason: third party code, a safety requirement, or a genuine history of memory corruption. Saying which subset is worth the cost on a given project is a better interview answer than claiming you always use it.</p>" },
{ t: "emb-cm-mpu", d: 1, q: "What is the minimum MPU region size on ARMv7-M?", o: ["4 bytes", "32 bytes", "1 kB", "4 kB"], a: 1, why: "<p>32 bytes, and every size above that is a power of two.</p><p>The small minimum is what makes stack guard regions practical: you do not need to sacrifice a kilobyte to catch an overflow, just a small barrier immediately below the stack.</p><p>Remember it must also be aligned to its own size, so the linker has to place the stack accordingly.</p>" },

// ---------------------------------------------------------- emb-cm-cmsis
{ t: "emb-cm-cmsis", d: 1, q: "What is CMSIS, mostly?", o: ["A full hardware abstraction framework layer", "Headers for the core, plus intrinsics", "A small real-time operating system", "A build system for ARM projects"], a: 1, why: "<p>Chiefly <code>core_cm4.h</code> and friends, defining SCB, NVIC, SysTick, MPU and DWT as structs at the addresses the architecture fixes.</p><p>That is why <code>SCB-&gt;CFSR</code> means the same thing on every vendor's part, and why one RTOS port covers an architecture rather than a chip.</p><p>The vendor adds a device header with their own peripherals and a startup file containing the vector table and reset handler. None of it is a framework in the sense that a HAL is.</p>" },
{ t: "emb-cm-cmsis", d: 2, q: "What does __CLZ compile to, and what is it good for?", o: ["A loop that shifts until it finds one", "One instruction; finds the top set bit", "A library call into libgcc", "A memory barrier before the count"], a: 1, why: "<p>One instruction, one cycle. It turns 'which is the highest priority ready task' from a search loop into a constant-time operation.</p><p>That is exactly how a scheduler with a ready-task bitmap picks the next task, and it is why such schedulers are O(1) rather than O(number of priorities).</p><p><code>__REV</code> is the other one worth knowing: a byte-reverse in a single instruction, so an endian swap costs one operation rather than several shifts and ORs.</p>" },
{ t: "emb-cm-cmsis", d: 3, q: "In a disassembled polling loop, the LDR is above the loop label and the body is a bare branch. What does that show?", o: ["Correct and entirely expected optimisation", "The read was hoisted: no volatile", "The loop has been fully unrolled", "An interrupt is pending on entry"], a: 1, why: "<p>The compiler concluded nothing in the loop could change that memory, read it once, and left an unconditional branch. The loop spins forever on a stale value.</p><p>Looking at the disassembly is the fastest possible confirmation of a missing volatile, and it takes about ten seconds against however long you would otherwise spend reasoning about it.</p><p>The healthy version has the LDR inside the loop, between the label and the conditional branch.</p>" },
{ t: "emb-cm-cmsis", d: 2, q: "You see bl __aeabi_dadd in a hot function on a Cortex-M4F. What is happening?", o: ["A 64-bit integer addition in software", "Software double precision, from a literal", "A DMA transfer being set up", "An atomic read-modify-write pair"], a: 1, why: "<p>The Cortex-M4 FPU is single precision only, so doubles are emulated by library routines that are dramatically slower.</p><p>The usual cause is a literal written <code>1.5</code> rather than <code>1.5f</code>, which promotes the whole expression to double even when every variable in it is a float.</p><p>It is completely invisible in the source and can dominate a hot path. Enabling -Wdouble-promotion catches it at compile time.</p>" },
{ t: "emb-cm-cmsis", d: 2, q: "What does bl __aeabi_uidiv indicate?", o: ["A floating point divide done in software", "Integer division the part cannot do", "A DMA descriptor being built", "A stack overflow check on entry"], a: 1, why: "<p>Cortex-M0 and M0+ have no hardware divide instruction, so every integer division becomes a library call costing tens of cycles rather than one.</p><p>M3 and above do have one, so seeing this on such a part usually means a 64-bit division, which is still a library call.</p><p>It matters in an interrupt handler or a control loop, where replacing a division by a shift or a reciprocal multiply can be the difference between fitting the deadline and not.</p>" },
{ t: "emb-cm-cmsis", d: 2, q: "Which tool turns a faulting address into a file and line?", o: ["objdump, with the disassembly", "addr2line, against that exact ELF", "readelf, reading out the symbol table", "nm, listing every symbol"], a: 1, why: "<p><code>arm-none-eabi-addr2line -e firmware.elf 0x080012a4</code> resolves the address using the debug information in the ELF.</p><p>The critical part is 'that exact build'. Optimisation moves code, so decoding against a slightly different binary produces a confident answer pointing at the wrong function, which is worse than no answer.</p><p><code>objdump -d --source</code> is the companion, interleaving source with instructions so you can read what surrounded the fault.</p>" },
{ t: "emb-cm-cmsis", d: 1, q: "What does __WFI() do?", o: ["Waits for a flag to be set in RAM", "Sleeps the core until an interrupt", "Flushes the instruction pipeline", "Disables interrupts until cleared"], a: 1, why: "<p>Wait For Interrupt: it halts the core until an exception occurs, which is the basic building block of an idle loop.</p><p>On a battery device this is the difference between an idle loop burning full active current and one drawing very little, and since duty cycle dominates average current it is one of the highest value single lines you can add.</p><p><code>__WFE()</code> is the related one, waiting on an event rather than an interrupt, used for inter-core signalling and some spin-wait constructs.</p>" },
{ t: "emb-cm-cmsis", d: 2, q: "Which instruction is a function call in Thumb?", o: ["B", "BL", "BX", "MOV pc, lr"], a: 1, why: "<p>BL branches and puts the return address in LR. BX branches to the address in a register, and <code>bx lr</code> is the corresponding return.</p><p>Plain B is a branch with no link, used for loops and gotos, which is why a polling loop shows up as a bare <code>b .loop</code>.</p><p><code>pop {pc}</code> is also a return, and a very common one, because a function that pushed LR on entry pops it straight into PC on exit.</p>" },
{ t: "emb-cm-cmsis", d: 3, q: "Why do MOVW and MOVT often appear as a pair?", o: ["To keep the constant pool properly aligned", "A 32-bit constant needs two halves", "They swap two register contents", "Together they act as a barrier"], a: 1, why: "<p>Thumb instructions are 16 or 32 bits, so a full 32-bit immediate cannot be encoded directly. MOVW loads the lower halfword and MOVT the upper.</p><p>Recognising the pair is useful when reading disassembly around a peripheral access: the pair is building a register address, and the value tells you which peripheral is being touched.</p><p>The alternative the compiler sometimes picks is a PC-relative load from a literal pool, which appears as <code>ldr r0, [pc, #n]</code>.</p>" },
{ t: "emb-cm-cmsis", d: 2, q: "What does the vendor's device header add on top of CMSIS core headers?", o: ["Nothing: it just includes CMSIS", "Peripherals, IRQ numbers, PRIO_BITS", "The RTOS kernel and its ports", "The linker script for that part"], a: 1, why: "<p>The core header covers what the architecture fixes. The device header covers everything at 0x40000000, plus the IRQ number enumeration and how many priority bits the part implements.</p><p>That split is exactly the portability boundary: code touching SCB or NVIC moves between vendors unchanged, code touching a UART does not.</p><p>The startup file is the third piece, holding the vector table and the reset handler that copies .data, zeroes .bss and calls main.</p>" }

]);
