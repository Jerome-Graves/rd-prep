BANKS["Embedded C"] = BANKS["Embedded C"].concat([

// ---------------------------------------------------------- emb-tdd-loop
{ t: "emb-tdd-loop", d: 1, q: "In the TDD micro cycle, what happens immediately after adding a test?", o: ["Write the implementation for it", "Run every test and watch it fail", "Refactor what is already there", "Commit before going any further"], a: 1, why: "<p>The red step is the one people skip and the one that carries the evidence.</p><p>A passing test means either the code is correct or the test checks nothing, and those look identical from outside. Inverted assertions, bodies that never run because of a typo, and fixtures returning unconditional success all pass happily.</p><p>Watching it fail for the reason you expected, then pass after the change, is the only cheap proof that the test is wired to the behaviour it claims to check.</p>" },
{ t: "emb-tdd-loop", d: 2, q: "Why write a test list before writing any test?", o: ["It documents the module's behaviour", "You notice error cases while they are cheap", "Most frameworks require a written manifest first", "It lets you estimate the effort"], a: 1, why: "<p>Enumerating the cases separates deciding what to test from working out how to test it, and doing both at once is what makes a cycle drag.</p><p>It also surfaces the error cases before the code exists, which is the cheapest moment to notice that a caller cannot distinguish two failures.</p><p>When a new case occurs to you mid-cycle it goes on the list rather than derailing the one in progress.</p>" },
{ t: "emb-tdd-loop", d: 2, q: "In FIRST, what does Repeatable mean?", o: ["The test can safely be run more than twice", "Same result every run, any machine", "It loops over its inputs", "It runs unattended inside CI"], a: 1, why: "<p>Anything non-deterministic makes a failure ambiguous, and an ambiguous failure gets rerun rather than read.</p><p>The usual offenders in firmware are a real delay, the wall clock, uninitialised memory, and shared hardware.</p><p>That is why the transport struct injects a delay function: the double advances time instantly, so a timeout test runs in microseconds and gives the same answer every time.</p>" },
{ t: "emb-tdd-loop", d: 3, q: "What does 'as the tests get more specific, the code gets more generic' describe?", o: ["The refactoring half of the cycle, done properly", "Each case makes a hardcoded answer untenable", "How coverage grows over the suite", "What mutation testing measures"], a: 1, why: "<p>Grenning's phrasing for why TDD is a design practice rather than a verification one.</p><p>You are not checking a design you already settled on. The sequence of tests does some of the choosing, because each new case rules out the shortcut that satisfied the previous one.</p><p>The corollary is that a test which is painful to write is telling you about the design, and the correct response is to change the design rather than to work around it in the test.</p>" },
{ t: "emb-tdd-loop", d: 2, q: "A TDD cycle has taken half an hour. What is the usual fix?", o: ["Push on to the end", "Find a smaller test", "Skip the refactor step", "Add more assertions"], a: 1, why: "<p>The discipline is in the size of the step. Cycles are meant to be minutes, and a long one means too much was attempted at once.</p><p>Backing up and picking a smaller case restores the property that when a test fails, the cause is in the small amount of code you just wrote.</p><p>That property is the whole reason the debugging cost stays low, and it evaporates as soon as the steps get large.</p>" },
{ t: "emb-tdd-loop", d: 2, q: "Why does an init test assert that the out parameter is untouched on failure, as well as checking the return code?", o: ["It raises the branch coverage figure", "The header promises it, so verify it", "It slows the test down deliberately", "Unity requires two assertions per test"], a: 1, why: "<p>Documented behaviour that no test checks drifts silently the first time someone restructures the error paths.</p><p>This particular promise matters because a caller that checks the return value but keeps a stale handle on failure has a bug that only appears on an error path nobody exercises.</p><p>The general habit: every guarantee in the header comment should have a test naming it.</p>" },
{ t: "emb-tdd-loop", d: 1, q: "What shape should the body of a test have?", o: ["Whatever is shortest", "Arrange, act, assert", "Assert, act, arrange", "One line"], a: 1, why: "<p>Set up the world, perform the single operation under test, then check the outcome. A reader should never have to work out which part is which.</p><p>Keeping the act to one call is what makes a failure unambiguous: if the assertion fails, exactly one operation could have caused it.</p><p>Tests with several acts interleaved with assertions are usually several tests that have been run together.</p>" },
{ t: "emb-tdd-loop", d: 3, q: "Why is TDD described as a design practice rather than a testing one?", o: ["Because the tests themselves come second", "Difficulty writing a test is design feedback", "Because it produces documentation as a byproduct", "It is not; it is purely about testing"], a: 1, why: "<p>Two reasons. The sequence of increasingly specific tests pushes the implementation toward generality, so the tests participate in the design.</p><p>And every friction point in a test maps to a specific structural problem: an enormous setup means too many dependencies, an unprovokable error path means the failure source is not substitutable, a needed real delay means time was not injected.</p><p>The tests left behind are valuable, but they are a by-product of a design conversation with the code.</p>" },
{ t: "emb-tdd-loop", d: 2, q: "Why does the red step survive even if you write tests after the code?", o: ["It does not survive that ordering at all", "You can still break the code to check", "CI requires a failing run first", "Coverage tools need the red run"], a: 1, why: "<p>The value is the evidence, not the order. Temporarily invert a condition or return the wrong value, confirm the test fails, and restore it.</p><p>Without that, you have a test that has never failed and therefore no evidence it is connected to anything.</p><p>It is essentially mutation testing done once by hand, and it takes seconds.</p>" },
{ t: "emb-tdd-loop", d: 2, q: "What is the strongest cost argument for testing on a contract firmware project?", o: ["It is simply what everyone expects now", "Client hardware is scarce and temporary", "It measurably reduces binary size", "It removes the need for any code review at all"], a: 1, why: "<p>The defect-cost argument is real but familiar. The scheduling one is specific to contract work and lands harder.</p><p>There may be one board, shared between engineers, on loan for a fixed period. Logic that can only be exercised with it attached stops being maintainable the day it leaves.</p><p>When the client returns eighteen months later with a bug, a host suite still runs on a laptop and everything else does not.</p>" },

// ---------------------------------------------------------- emb-tdd-doubles
{ t: "emb-tdd-doubles", d: 2, q: "Which double records calls, arguments and order for the test to check afterwards?", o: ["Stub", "Spy", "Fake", "Dummy"], a: 1, why: "<p>A stub answers questions and has no memory. A spy keeps a log.</p><p>That distinction matters because interaction is often what you care about in a driver: not what init returned, but that it wrote CTRL1 after the reset completed.</p><p>A mock goes further still, programmed with expectations up front and failing during execution when they are violated, which is what CMock generates.</p>" },
{ t: "emb-tdd-doubles", d: 2, q: "In the test-double taxonomy, what distinguishes a fake from a stub?", o: ["A test that is written so as to always pass", "A working but simplified implementation", "A stub that also counts its calls", "A mocked interrupt handler"], a: 1, why: "<p>Write 0x38 to a fake register file and reading it back gives 0x38, so sequences where a later step depends on an earlier write actually exercise real logic.</p><p>A stub returning canned answers cannot do that, and adding enough state to make it work turns it into a fake anyway.</p><p>Fakes can also do what hardware will not: report the wrong chip ID, NAK the third write, refuse to leave reset.</p>" },
{ t: "emb-tdd-doubles", d: 3, q: "You are asserting on the final state of the device after a sequence of operations. Which double?", o: ["A mock", "A fake", "A dummy", "A spy"], a: 1, why: "<p>Final state requires the double to behave coherently across the whole sequence, which is exactly what a fake does and a stub does not.</p><p>A spy records what happened but does not model the device, so it cannot answer questions about resulting state.</p><p>A mock would work but binds the test to the exact call sequence, so a refactor that changes the order without changing the outcome breaks it.</p>" },
{ t: "emb-tdd-doubles", d: 2, q: "Which substitution mechanism requires no change to the production source?", o: ["Function pointer", "Link-time", "Preprocessor", "None"], a: 1, why: "<p>You compile the same source and link a different object file that provides the same symbols. The code under test is byte-for-byte what ships.</p><p>That makes it the only option for code you cannot modify, which is why it is the workhorse for legacy code and how CMock operates.</p><p>The limitation is that substitution is per executable, so one test cannot use the real implementation while another uses the double without separate binaries.</p>" },
{ t: "emb-tdd-doubles", d: 3, q: "What is the advantage of function pointer substitution over link-time?", o: ["It executes noticeably faster at run time too", "Per instance, and switchable at run time", "It needs no change to the design", "It works on code you cannot modify"], a: 1, why: "<p>Link-time substitution replaces a symbol for the whole executable. A function pointer belongs to an instance, so two devices in one test can behave differently.</p><p>The cost is an indirect call and, more importantly, that the production design has to accommodate it.</p><p>That requirement is not really a cost: it is the design pressure that produces a driver with a narrow, explicit dependency on its platform.</p>" },
{ t: "emb-tdd-doubles", d: 2, q: "Why is preprocessor substitution the last resort?", o: ["It is slow to compile on every run", "The test build compiles different source", "It cannot be automated by a build script", "It only works where macros are used"], a: 1, why: "<p>The value of the other two mechanisms is that the code under test is exactly the code you ship. Redefining a name breaks that.</p><p>It stays genuinely useful for wrapping something with no other seam, most obviously a vendor macro, since a macro has no symbol to substitute at link time.</p><p>Keep it for cases where nothing else reaches, and be aware that the test is now proving something slightly different from what runs on the product.</p>" },
{ t: "emb-tdd-doubles", d: 2, q: "You have to fake an entire vendor I2C API to test a driver. What does that tell you?", o: ["The test you wrote is far too ambitious", "The driver is coupled to the platform", "You need CMock to generate it", "Nothing: that is what drivers need"], a: 1, why: "<p>The amount you must replace is a direct measurement of coupling. A properly layered driver needs three functions: read, write and delay.</p><p>Having to model handles, init structures and status enums means the driver is talking to the platform rather than to an abstraction it defined.</p><p>The productive response is to treat that as a design signal. An elaborate stub is a maintenance liability that also fails to catch what it was meant to protect against.</p>" },
{ t: "emb-tdd-doubles", d: 1, q: "What is a dummy?", o: ["A test that does nothing at all useful", "An argument passed but never used", "A stub that always returns zero", "A placeholder name for a test"], a: 1, why: "<p>The simplest of the five. A null callback pointer where the code under test does not invoke it, or an unused context pointer.</p><p>It exists in the vocabulary mainly so you can say precisely what a given double is doing, which matters when discussing a test design with someone else.</p>" },
{ t: "emb-tdd-doubles", d: 3, q: "Why can a fake not verify that init wrote CTRL1 after the reset rather than before?", o: ["It can, provided that it records the writes", "Ordering is a property of interaction", "Fakes are not allowed to record", "That register is read-only anyway"], a: 1, why: "<p>Run the writes in either order and the register array ends up identical, so there is nothing in the final state to distinguish them.</p><p>Ordering has to be recorded as it happens, which is what a spy or a mock does.</p><p>This matters in real drivers because many devices require configuration in a specific order, and a driver that happens to work on your bench can fail on a slower part where the ordering becomes observable.</p>" },
{ t: "emb-tdd-doubles", d: 2, q: "What is the shared property of all three substitution mechanisms?", o: ["They all require a full test framework", "The driver source is identical either way", "They only work in a host build", "They all rely on function pointers somewhere"], a: 1, why: "<p>Only what the code is linked against, pointed at, or compiled with changes. The logic itself is the same text in both builds.</p><p>That is what makes the test meaningful: you are exercising the code you ship rather than a copy that has drifted.</p><p>Preprocessor substitution weakens this slightly, which is precisely why it ranks last.</p>" },

// ---------------------------------------------------------- emb-tdd-unity
{ t: "emb-tdd-unity", d: 2, q: "Why does Unity ship a script to generate the test runner?", o: ["It speeds up the compilation step", "C has no reflection: a forgotten test hides", "It generates the mocks as well", "It is needed for C++ compatibility"], a: 1, why: "<p>Nothing in C can enumerate the functions in a file, so the RUN_TEST lines have to exist somewhere.</p><p>Written by hand, a test you added but forgot to register does not run, and because it does not run it does not fail. Nothing tells you.</p><p>The generator parses the file and emits the runner, which removes the whole class of silent omission. It is the main reason to adopt it even on a small project.</p>" },
{ t: "emb-tdd-unity", d: 2, q: "How does CMock substitute the real implementation?", o: ["Function pointers set up in setUp", "Link-time: the mock provides the symbols", "The preprocessor, redefining the names", "It rewrites the source before it is built"], a: 1, why: "<p>It parses a header and generates a .c providing every function in it, plus an expectation API. Linking that instead of the real object is the substitution.</p><p>The strength is that it works on a C API you cannot modify and needs no change to the code under test.</p><p>The limitation is that the resulting tests bind to an exact call sequence, so refactoring the implementation without changing observable behaviour can still break them.</p>" },
{ t: "emb-tdd-unity", d: 2, q: "What does CppUTest provide that Unity does not?", o: ["Support for plain C as well as C++", "Memory leak detection that fails a test", "Noticeably faster test execution", "Generation of mocks directly from headers"], a: 1, why: "<p>A test that allocates and does not free fails, with the location reported.</p><p>For driver code with allocation and several error paths that must each free correctly, that is a genuinely valuable property, and those paths are exactly the ones that rarely run.</p><p>It is written in C++ but tests C happily, and it is what Grenning's book uses for most of its examples.</p>" },
{ t: "emb-tdd-unity", d: 1, q: "What runs before every individual test in Unity?", o: ["main", "setUp", "RUN_TEST", "UNITY_BEGIN"], a: 1, why: "<p><code>setUp</code> before each and <code>tearDown</code> after each. Both are called by the framework, not by you.</p><p>Resetting your fakes in setUp is what gives you the Isolated property from FIRST. Without it, state leaks between tests and you get failures that depend on execution order.</p><p>Order-dependent failures are among the most confusing to debug, which is why this is worth being disciplined about.</p>" },
{ t: "emb-tdd-unity", d: 2, q: "Why prefer TEST_ASSERT_EQUAL_HEX8 for a register value?", o: ["It compares faster than the decimal form", "Mismatches are reported in hex", "It checks the type as well as value", "It is more precise about the width"], a: 1, why: "<p>'Expected 0x38 was 0x30' tells you a bit is wrong and which one. 'Expected 56 was 48' needs conversion before it means anything.</p><p>The failure message is read at the worst possible moment, often by someone who did not write the test, so making it self-explanatory pays.</p><p>The _MESSAGE variants take a string as well, which is worth using wherever the assertion alone does not say why it mattered.</p>" },
{ t: "emb-tdd-unity", d: 2, q: "Why start with plain assertions in a main() rather than adopting Ceedling first?", o: ["Ceedling is unreliable on Windows", "Proving it builds off-target takes ten minutes", "Ceedling does not really support C", "It is required before adding tools"], a: 1, why: "<p>If the code turns out to be too coupled to the vendor SDK to compile on a host, you learn that in ten minutes rather than after a day of build configuration.</p><p>Setting up a framework, a mock generator and a runner generator before anything has been proven is how a day disappears with nothing to show.</p><p>Migration afterwards is easy. The order matters more than the choice of tool.</p>" },
{ t: "emb-tdd-unity", d: 3, q: "When is a CMock-style mock the right choice over a hand-written fake?", o: ["Always: mocks are strictly better", "When the interaction is the specification", "When the fake makes the test slow", "When you need coverage of a specific branch"], a: 1, why: "<p>If the requirement is literally 'these calls happen in this order with these arguments', a mock expresses it directly and fails at the point of violation.</p><p>If the requirement is about resulting behaviour, a fake is better, because it lets the implementation change freely as long as the outcome holds.</p><p>Mock-heavy suites tend to break on refactors that changed nothing observable, which is the fastest way to make a team resent its tests.</p>" },
{ t: "emb-tdd-unity", d: 1, q: "What is Ceedling?", o: ["A mocking library for use with C code", "Build glue over Unity, CMock and rake", "An assertion library for embedded C", "A coverage tool built on gcov"], a: 1, why: "<p>Point it at a directory and <code>test_foo.c</code> is compiled against <code>foo.c</code>, with mocks generated automatically for any header included as <code>mock_bar.h</code>.</p><p>The appeal is not writing a makefile per test. The cost is a Ruby dependency and a certain amount of implicit behaviour, which is a real consideration if the rest of the build is CMake.</p>" },
{ t: "emb-tdd-unity", d: 2, q: "Why is TEST_ASSERT_FLOAT_WITHIN preferred over comparing floats for equality?", o: ["It compares faster than exact equality", "Results rarely land on an exact value", "It handles NaN, which equality does not", "Unity offers no float equality macro"], a: 1, why: "<p>Rounding means a computed result is almost never bit-identical to the literal you expected, so an equality assertion fails for reasons that have nothing to do with correctness.</p><p>The WITHIN form takes a tolerance, which forces you to state how close is close enough. That is a useful thing to have to decide.</p><p>The same reasoning is why comparing floats with == in production code is a warning sign.</p>" },
{ t: "emb-tdd-unity", d: 2, q: "What is the honest thing to say in an interview about test frameworks?", o: ["Claim working expertise in all of them", "Name what each does; the first test matters", "Say that frameworks are unnecessary", "Mention only the one you have used"], a: 1, why: "<p>Unity is assertions and a runner. CMock generates mocks by link-time substitution. Ceedling is the build glue. CppUTest is the alternative with leak detection.</p><p>Being able to place each one, and to say why you would choose a fake over a mock in a given case, demonstrates more than claiming depth in a specific tool.</p><p>It also invites the more interesting conversation about what you test and how you decide, which is where the real signal is.</p>" },

// ---------------------------------------------------------- emb-tdd-dualtarget
{ t: "emb-tdd-dualtarget", d: 2, q: "What is the most underrated benefit of a host build?", o: ["The tests run in a small fraction of a second", "Host tooling: UBSan, ASan, Valgrind, gcov", "The resulting binaries are smaller", "It proves the timing is acceptable"], a: 1, why: "<p>Speed and availability are the reasons people give. The sanitizers are the reason that pays off unexpectedly.</p><p>Running driver logic under UndefinedBehaviorSanitizer catches signed overflow, out-of-range shifts, misaligned access and null dereference at run time, reporting a file and line.</p><p>There is no on-target equivalent, and it costs one extra build configuration.</p>" },
{ t: "emb-tdd-dualtarget", d: 2, q: "Which of these can a host test suite NOT establish?", o: ["That an error is returned on a NAK", "That the handler meets a 50 us deadline", "That a state machine rejects an event", "That a frame is serialised correctly"], a: 1, why: "<p>Timing needs a real core running real instructions under real load. Nothing about a host build speaks to it.</p><p>The other three are pure logic and are exactly what the host tier is for.</p><p>Being explicit about this boundary is what makes the rest of the argument credible. Overclaiming for host testing is what gives it a bad name among people who have been burned by it.</p>" },
{ t: "emb-tdd-dualtarget", d: 3, q: "Why does Grenning insist the suite also runs on the target?", o: ["Certification bodies insist on it", "Type sizes, alignment, endianness, compiler", "Host tests are inherently unreliable in this area", "Coverage can only be measured there"], a: 1, why: "<p>Dual targeting means both, not host instead of target. The host build cannot tell you that a struct is laid out differently or that an access the host tolerates faults on a Cortex-M0.</p><p>A test that passes on host and fails on target has found a genuine portability defect, and found it in a test rather than in a product.</p><p>It does not need to run on every commit, just often enough that a divergence is caught while it is small.</p>" },
{ t: "emb-tdd-dualtarget", d: 2, q: "Which build configuration do teams most often omit?", o: ["The host debug configuration itself", "Host release, at the shipped -O level", "The target release configuration", "The target debug configuration"], a: 1, why: "<p>Developing at -O0 and shipping at -O2 hides missing volatile and undefined behaviour until the release build, which by then has had the least testing of any configuration.</p><p>Running the suite at the shipped optimisation level surfaces those faults during development, when the code is still fresh.</p><p>-Og is a reasonable everyday setting, but it is not a substitute for exercising the level you actually ship.</p>" },
{ t: "emb-tdd-dualtarget", d: 3, q: "Your host is 64-bit and your target 32-bit. What does that difference give you?", o: ["Tests that run considerably faster", "Free exposure of pointer size assumptions", "Better coverage of exactly the same source lines", "Nothing useful; it is a nuisance"], a: 1, why: "<p>Assumptions such as sizeof(void *) equalling sizeof(int) hold on one build and break on the other, so the mismatch surfaces them.</p><p>Some teams deliberately build the host suite both 32-bit and 64-bit, and occasionally big-endian under an emulator, for exactly this reason.</p><p>It is a portability check you were not paying for, which is a rare thing.</p>" },
{ t: "emb-tdd-dualtarget", d: 2, q: "What makes a host build possible in the first place?", o: ["Having a working cross compiler", "The code compiling without the vendor SDK", "A mocking framework being available", "A large enough stack on the host"], a: 1, why: "<p>This is the layering test from the drivers lesson, and dual targeting is what it buys you.</p><p>The board layer, the bus layer and the adapter stay on target. The driver logic, the state machines, the protocol code and the maths build anywhere.</p><p>If the driver includes a vendor header, none of this works, which is why that single question is the strongest test of whether the layering is right.</p>" },
{ t: "emb-tdd-dualtarget", d: 2, q: "Why does an alignment bug often survive a host test suite?", o: ["The tests simply do not happen to cover it", "x86 tolerates what Cortex-M0 faults on", "Alignment is a linker concern only", "It does not; the host catches it"], a: 1, why: "<p>The host executes the access happily, so nothing fails and the test passes.</p><p>On a Cortex-M0 the same access is a fault, and on M3 and above it is fine for single loads but still faults for LDM, STM, LDRD and STRD, which the compiler emits whenever it moves more than one word.</p><p>This is one of the specific reasons to run the suite on target periodically, and one reason packing a struct is a correctness risk.</p>" },
{ t: "emb-tdd-dualtarget", d: 1, q: "How long should a host unit test suite take to run?", o: ["Under a minute is fine", "Milliseconds to a few seconds", "It does not matter", "As long as the target build"], a: 1, why: "<p>Fast is the F in FIRST, and it is load-bearing rather than aesthetic.</p><p>A suite that takes minutes gets run at commit time instead of continuously, so the feedback arrives after you have moved on, which is precisely when a failure is most expensive to interpret.</p><p>Anything slow in a unit suite is usually a real delay or real I/O that should have been substituted.</p>" },
{ t: "emb-tdd-dualtarget", d: 3, q: "You add UBSan to the host build and a previously passing test now fails. What have you learned?", o: ["UBSan is reporting a false positive here", "It was passing while executing UB", "The test was flaky all along", "The host compiler differs from target"], a: 1, why: "<p>Undefined behaviour frequently produces the expected answer, right up until an optimiser decides the situation cannot occur and deletes the surrounding code.</p><p>So a passing test proved nothing about that path being correct, only that it happened to behave today.</p><p>Finding it in a test run rather than in a release build at -O2 is exactly the trade the extra build configuration buys you.</p>" },
{ t: "emb-tdd-dualtarget", d: 2, q: "Which parts of a well-layered driver stack stay on target and cannot be host-tested?", o: ["All of it needs the real hardware", "The board layer, bus layer and adapter", "The driver's own decision logic", "The protocol and framing code"], a: 1, why: "<p>Those three touch pins, the vendor API and real timing, so they are inherently target-bound.</p><p>Everything above them, the register sequencing, the state machines, the error handling and the serialisation, is ordinary logic that runs anywhere.</p><p>The humble object idea applies: keep the target-bound layer thin and decision-free, so that reading it is sufficient review.</p>" },

// ---------------------------------------------------------- emb-tdd-legacy
{ t: "emb-tdd-legacy", d: 1, q: "How does Feathers define legacy code?", o: ["Code over five years old", "Code without tests", "Code in an obsolete language", "Undocumented code"], a: 1, why: "<p>Deliberately blunt, and useful because it moves the problem from age or style to the missing safety net.</p><p>By this definition code you wrote this morning without tests is legacy, which is uncomfortable and correct.</p><p>It also makes the goal concrete: not to modernise, but to get a net under the part you are about to change.</p>" },
{ t: "emb-tdd-legacy", d: 2, q: "What is a seam?", o: ["The boundary between two separate modules", "Where behaviour changes without editing there", "A fixture shared between tests", "A specially named section in the linker script"], a: 1, why: "<p>C has three: link, preprocessing and object, matching the three substitution mechanisms.</p><p>The first question when facing an untestable function is not how to restructure it but which seam already exists, because the link seam usually does and needs no source change at all.</p><p>That reframing is what makes a first test achievable today rather than after a refactor nobody has approved.</p>" },
{ t: "emb-tdd-legacy", d: 3, q: "What is a characterisation test?", o: ["A specification of correct behaviour", "A record of what the code does now", "A benchmark of the current speed", "A report of which lines are covered"], a: 1, why: "<p>You write an assertion with a guess, run it, and let the failure report the real answer, which you then put in. That feels backwards and is the fastest way to document behaviour nobody remembers.</p><p>Some of what you capture will be wrong. Record it as-is, get the suite green, then fix deliberately as a separate change with the net already in place.</p><p>Mixing characterising and fixing is how you end up unable to tell which change broke something.</p>" },
{ t: "emb-tdd-legacy", d: 2, q: "You must add logic to a 400-line untested function. What is sprouting?", o: ["Rewriting the whole function first", "New logic in a tested function, one call", "Copying the function and editing the copy", "Commenting out the old code path"], a: 1, why: "<p>The new code is fully covered and the risky function changed by exactly one line, which is about as small a change as you can make to something you cannot verify.</p><p>Wrapping is the sibling technique: rename the original and give its old name to a new function that calls it and adds the behaviour, so every existing caller is untouched.</p><p>Neither is elegant. Both let you make progress without a rewrite you cannot justify.</p>" },
{ t: "emb-tdd-legacy", d: 3, q: "A function reads a GPIO register directly, so it will not build on a host. What is the lowest-friction fix?", o: ["Redesign the module properly first", "Extract it to one function, link seam", "Preprocessor-stub that entire source file", "Test that function only on target"], a: 1, why: "<p>One function, one obvious change, and the surrounding logic becomes testable without touching it.</p><p>Redesigning the module is the right long-term answer and the wrong first move, because it is a large change to code with no safety net, which is exactly the situation you are trying to escape.</p><p>Repeat the extraction as you touch things, and the module becomes testable incrementally. That is what actually happens on real projects.</p>" },
{ t: "emb-tdd-legacy", d: 2, q: "Where should you start adding tests to a large untested codebase?", o: ["Whichever is the worst file in the codebase", "Something you are about to change", "The smallest file, to build momentum", "The most recently written code"], a: 1, why: "<p>That is where the tests pay for themselves immediately, and where you need the net regardless of any longer-term goal.</p><p>Starting with the worst file means a large investment with no near-term return, which is how the effort gets deprioritised after two weeks.</p><p>The incremental approach also produces a track record, which is what makes further investment easy to argue for.</p>" },
{ t: "emb-tdd-legacy", d: 2, q: "Why is the link seam the most useful for legacy code specifically?", o: ["It is by far the fastest of the three to set up", "No source change breaks the deadlock", "It also works on macro dependencies", "It can be switched per instance"], a: 1, why: "<p>The dilemma is that changing code safely wants tests, and testing it usually wants a change. Anything that gets a first test running without editing the file breaks that circle.</p><p>Compiling the same source and linking a substitute object achieves exactly that.</p><p>Once one test exists you have a net, and every subsequent structural improvement is a change you can verify rather than a leap.</p>" },
{ t: "emb-tdd-legacy", d: 3, q: "When would you reach for the preprocessing seam?", o: ["By default, in preference to the others", "When the dependency is a macro", "When performance matters most", "Whenever you are using CMock"], a: 1, why: "<p>A macro expands before the linker ever sees anything, so there is no symbol to replace. Redefining it is the only handle you have.</p><p>Vendor headers full of register access macros are the usual case in firmware.</p><p>Accept that the test build is now compiling slightly different source from production, which weakens the guarantee, and prefer it only where no other seam reaches.</p>" },
{ t: "emb-tdd-legacy", d: 2, q: "Why capture a bug in a characterisation test rather than fixing it immediately?", o: ["The bug may well be intentional", "Green first, then fix deliberately", "Coverage tools require it that way", "A bug cannot be captured in a test"], a: 1, why: "<p>If you fix while you are still establishing what the code does, a later failure is ambiguous: it could be your fix or your misunderstanding.</p><p>Getting green first means the net exists before you change behaviour, which is the entire point of the exercise.</p><p>It is also worth checking whether something depends on the buggy behaviour, which happens more often in long-lived firmware than anyone expects.</p>" },
{ t: "emb-tdd-legacy", d: 2, q: "What is wrapping, as distinct from sprouting?", o: ["They are two names for one thing", "Rename the original, new one calls it", "Moving the function into a brand new file", "Adding a mutex around the call"], a: 1, why: "<p>Sprouting adds a call inside the existing function. Wrapping leaves the original untouched and intercepts at its name, so every existing caller gets the new behaviour without being edited.</p><p>Wrapping suits cross-cutting additions such as logging, counting or validation.</p><p>Sprouting suits a change that belongs in the middle of the existing logic.</p>" },

// ---------------------------------------------------------- emb-tdd-design
{ t: "emb-tdd-design", d: 2, q: "Your test needs a real one-second delay. What is the design problem?", o: ["The test itself is simply badly written", "Time is a dependency, not injected", "The RTOS tick is misconfigured", "Nothing: some tests take time"], a: 1, why: "<p>A driver calling vTaskDelay or HAL_Delay depends on that platform, and every timeout test then takes real seconds, so the suite gets slow enough that people stop running it.</p><p>An injected delay function lets the double return immediately while counting, so a timeout test runs in microseconds and is deterministic.</p><p>It is also why the transport struct has three members rather than two.</p>" },
{ t: "emb-tdd-design", d: 3, q: "What is dependency inversion, expressed in C?", o: ["Using a dependency injection framework", "A struct of function pointers it needs", "Wrapping the vendor HAL in your own", "Passing void pointers everywhere"], a: 1, why: "<p>The arrow reverses. Rather than the driver depending on the platform, the platform depends on a definition the driver owns.</p><p>Four struct members, no framework, no container. That is the whole of it in C.</p><p>The test benefit follows automatically, but the portability benefit is the one that pays on the next project.</p>" },
{ t: "emb-tdd-design", d: 2, q: "What does the absence of bus init and chip select from a transport struct demonstrate?", o: ["An oversight in the interface design", "Interface segregation: only what it uses", "Poor encapsulation of the bus", "A clear violation of the project's layering rules"], a: 1, why: "<p>Every member you add is a member every test double must provide, so a fat interface makes testing expensive in direct proportion.</p><p>Keeping it to read, write and delay is what makes the double trivial to write and the driver trivial to port.</p><p>It is also honest: the driver genuinely does not initialise buses or control chip selects, so declaring that it might would be a lie about its responsibilities.</p>" },
{ t: "emb-tdd-design", d: 2, q: "What is a practical single-responsibility test for a firmware module?", o: ["The number of lines in the file", "How many things you must fake", "The count of public functions", "How many headers it includes"], a: 1, why: "<p>Three is a well-factored driver: read, write, delay. Ten means the unit is doing several jobs at once.</p><p>The count is objective and easy to apply during review, unlike arguing about whether something has one reason to change.</p><p>Splitting a module with a high count usually improves the production code as much as the tests, which is the point.</p>" },
{ t: "emb-tdd-design", d: 3, q: "What is the humble object pattern?", o: ["Keeping every single function short", "Thin, decision-free untestable layer", "Avoiding inheritance in C++", "Writing fewer but considerably broader tests"], a: 1, why: "<p>You are not eliminating untestable code, you are shrinking it until reading it is sufficient review.</p><p>The canonical firmware example is an interrupt handler that reads the data register and immediately calls a tested function, with no branching of its own.</p><p>It is an honest position to state in an interview: here is what is covered, here is what is not, and here is why the uncovered part is small enough to reason about by eye.</p>" },
{ t: "emb-tdd-design", d: 2, q: "You cannot construct the unit under test in a known state. What does that indicate?", o: ["A missing constructor", "Hidden global state", "Too many parameters", "A compiler issue"], a: 1, why: "<p>If the behaviour depends on file-scope statics that no test can reach, then no test can establish preconditions, and results depend on what ran before.</p><p>That also means only one instance can ever exist, which is usually a limitation nobody chose deliberately.</p><p>Moving that state into a handle fixes the testability and the instance limit at the same time.</p>" },
{ t: "emb-tdd-design", d: 3, q: "Why does 'we will add tests later' so rarely happen?", o: ["A simple lack of team discipline", "It is architectural: every call site changes", "Tests are expensive to write later", "Management does not allow the time"], a: 1, why: "<p>If a driver calls the HAL directly, adding a seam later touches every call site, and by then other code depends on the structure being what it is.</p><p>So it is not an effort problem that discipline solves; it is a design decision that was made at the start and is expensive to reverse.</p><p>That is the real argument for writing the first test early: not because the test is valuable on day one, but because writing it forces a boundary to exist while creating one is still cheap.</p>" },
{ t: "emb-tdd-design", d: 2, q: "You cannot provoke an error path in a test. What is the design problem?", o: ["The error genuinely cannot happen", "The failure source is not substitutable", "The test framework is inadequate for this", "The error handling itself is wrong"], a: 1, why: "<p>To test that a driver returns a specific error when the bus NAKs, something has to be able to produce a NAK, and real hardware will not on demand.</p><p>If the failure originates inside a call the test cannot replace, that path is unreachable from a test and will ship having never executed.</p><p>Those are precisely the paths that run when something is wrong in the field, which makes them the ones most worth exercising.</p>" },
{ t: "emb-tdd-design", d: 2, q: "How should you frame a review comment about a driver calling the vendor HAL directly?", o: ["As a matter of style preference", "As a consequence: it only runs on silicon", "As a performance issue to address", "As a portability nicety for later"], a: 1, why: "<p>Saying it should use dependency injection sounds like taste and invites disagreement about taste.</p><p>Naming the consequence is concrete and hard to argue with: no host build, no exercising of error paths, and testing stops when the client's board goes back.</p><p>Then propose the specific change, three function pointers and a context, so the comment ends with an action rather than a criticism.</p>" },
{ t: "emb-tdd-design", d: 3, q: "An enormous test setup usually means what?", o: ["That the test is a thorough one", "Too many dependencies: several jobs", "That the framework is slow to start", "That fixtures are missing from it"], a: 1, why: "<p>Setup size tracks dependency count almost exactly, so a page of arrangement is a measurement rather than an inconvenience.</p><p>The tempting response is to extract a helper and carry on, which hides the signal without addressing it.</p><p>The productive response is to split the unit, which usually improves the production code more than it improves the test.</p>" },

// ---------------------------------------------------------- emb-tdd-coverage
{ t: "emb-tdd-coverage", d: 2, q: "What does code coverage actually measure?", o: ["What was verified", "What was executed", "Assertion quality", "Test count"], a: 1, why: "<p>A test that calls a function and asserts nothing produces full coverage of everything it touches, and passes forever regardless of what you do to the code.</p><p>So coverage is best read as a negative signal: uncovered lines definitely are not tested, covered lines merely might be.</p><p>The useful question is not whether coverage is high but what is uncovered and whether that is acceptable. In firmware the answer is usually the error paths.</p>" },
{ t: "emb-tdd-coverage", d: 3, q: "For if (a && b), what does MC/DC require beyond branch coverage?", o: ["Nothing at all beyond branch coverage", "Cases where a and b each decide it", "Full statement coverage as well", "Every combination of the inputs"], a: 1, why: "<p>Branch coverage only requires the whole condition to evaluate both ways, which two tests achieve while leaving one sub-condition never shown to matter.</p><p>MC/DC requires demonstrating that each sub-condition independently flips the result, which catches a condition that is effectively dead.</p><p>It is required for DO-178C Level A avionics, and knowing the distinction is a good signal that you understand what the weaker metrics leave out.</p>" },
{ t: "emb-tdd-coverage", d: 2, q: "What does a surviving mutant tell you?", o: ["That the code definitely contains a bug", "No test checked that behaviour", "That coverage is still too low", "That the test is running slowly"], a: 1, why: "<p>Change a greater-than to a greater-or-equal, or flip a return value, and if the suite still passes then nothing was asserting on it.</p><p>That measures assertion quality, which is exactly what coverage cannot see, since the mutated line was covered both before and after.</p><p>Full tooling for C is heavier than most projects want, but doing it by hand for ten minutes on the most safety-relevant function is cheap and consistently surprising.</p>" },
{ t: "emb-tdd-coverage", d: 2, q: "A test passes alone but fails when the suite runs. What is the most likely cause?", o: ["A bug in the compiler's optimiser", "Shared state: a static not reset", "Coverage is too low to be reliable", "A timing issue between the tests"], a: 1, why: "<p>Order dependence is the signature of state leaking between tests, and it is the most common source of flakiness.</p><p>The fix is to reset every fake and every module-level variable in setUp, which restores the Isolated property from FIRST.</p><p>The wrong response is rerunning until it passes, because a suite with a known-flaky test is one nobody trusts, and the next genuine failure gets dismissed too.</p>" },
{ t: "emb-tdd-coverage", d: 1, q: "Why measure coverage at -O0?", o: ["The instrumented build runs faster", "Optimisation moves and merges code", "gcov will not run at any other level", "It produces a smaller output file"], a: 1, why: "<p>Inlining and reordering break the mapping between generated code and source lines, so the report becomes unreliable.</p><p>This is one of the few places where measuring at -O0 is the right call rather than a habit to avoid.</p><p>It does not conflict with running the suite at the shipped optimisation level as well; those are two configurations answering two different questions.</p>" },
{ t: "emb-tdd-coverage", d: 3, q: "Why do mock-heavy suites tend to break under refactoring?", o: ["The generated mocks contain bugs", "They bind to an exact call sequence", "They are slow to run in a suite", "They need regenerating on every single build"], a: 1, why: "<p>A mock asserts on the interaction. If the specification really is the interaction, that is correct and valuable.</p><p>If the specification is the resulting behaviour, the test is now coupled to an implementation detail, and every internal change costs test maintenance for no gain.</p><p>That is the argument for preferring a fake wherever the behaviour is what matters, and reserving mocks for where the call sequence genuinely is the requirement.</p>" },
{ t: "emb-tdd-coverage", d: 2, q: "What is the correct response to a flaky test?", o: ["Rerun it", "Fix it or delete it", "Mark it as expected to fail", "Increase the timeout"], a: 1, why: "<p>A suite containing a test that sometimes fails trains everyone to rerun rather than read, and that habit is what makes the next real failure get dismissed.</p><p>Deleting is a legitimate option: a test nobody trusts has negative value, since it costs time and provides no signal.</p><p>The usual causes are shared state, real time, uninitialised memory and real hardware, and the first three are all fixable.</p>" },
{ t: "emb-tdd-coverage", d: 2, q: "Which coverage metric is weakest?", o: ["Branch", "Statement", "MC/DC", "Path"], a: 1, why: "<p>Statement coverage asks only whether a line ran. One test through a compound condition gives full statement coverage of the body while proving essentially nothing about the condition.</p><p>Branch coverage additionally requires each decision to go both ways, which is meaningfully stronger and usually the right target for firmware.</p><p>A high statement coverage number quoted without saying which metric it is should prompt the question.</p>" },
{ t: "emb-tdd-coverage", d: 3, q: "Which coverage figure is most worth investigating on a firmware project?", o: ["The overall percentage, tracked over time", "Which lines are uncovered: error paths", "The total number of tests written", "How well the happy path is covered"], a: 1, why: "<p>The aggregate number invites a target, and targets get met by writing assertion-free tests that execute a lot of code.</p><p>The uncovered list is short, specific and actionable, and in firmware it is dominated by error handling: the allocation failure, the third NAK, the timeout.</p><p>Those are the paths that only run when something has already gone wrong, so they are the ones you least want shipping unexercised.</p>" },
{ t: "emb-tdd-coverage", d: 2, q: "Uninitialised memory is causing intermittent test failures. Which tool finds it?", o: ["gcov, from the coverage report", "AddressSanitizer or MemorySanitizer", "objdump on the built object", "A logic analyser attached to the bus"], a: 1, why: "<p>The sanitizers instrument memory access and report the file and line, which is the fastest route to a fault whose symptom varies run to run.</p><p>gcov measures coverage and says nothing about correctness. objdump and an analyser answer entirely different questions.</p><p>This is another instance of the host build's real advantage: this class of tooling has no on-target equivalent.</p>" }

]);
