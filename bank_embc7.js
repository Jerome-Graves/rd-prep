BANKS["Embedded C"] = BANKS["Embedded C"].concat([

// ---------------------------------------------------------- emb-usb
{ t: "emb-usb", d: 2, q: "USB transfers work except those of exactly 64, 128 or 192 bytes, which hang. What is missing?", o: ["A larger buffer on that endpoint", "A zero-length packet to end it", "Double buffering on the endpoint", "A different endpoint type entirely"], a: 1, why: "<p>A transfer ends when the host receives a packet shorter than wMaxPacketSize. An exact multiple of the packet size never produces one, so the host keeps waiting.</p><p>The fix is to send an explicit zero-length packet after such a transfer.</p><p>It is worth knowing by name because the reproduction is so specific: everything works except those sizes, which makes it look like a data-dependent bug rather than a protocol requirement.</p>" },
{ t: "emb-usb", d: 2, q: "What does wTotalLength in a configuration descriptor cover?", o: ["The configuration descriptor alone", "The whole blob, interfaces and endpoints", "The largest endpoint's maximum packet size", "The number of interfaces it declares"], a: 1, why: "<p>The host reads the configuration descriptor first to learn how many bytes to fetch, then fetches that many and expects the interface and endpoint descriptors nested inside.</p><p>Set it to 9, the size of the configuration descriptor itself, and the host truncates the set, so your endpoints never appear and the device enumerates as something useless.</p><p>It is completely silent when wrong, which is why it belongs at the top of the checklist when a device shows up as unknown.</p>" },
{ t: "emb-usb", d: 2, q: "Which USB endpoint type guarantees bandwidth but never retries?", o: ["Bulk", "Isochronous", "Interrupt", "Control"], a: 1, why: "<p>Lost data stays lost, which is the correct trade for audio and video where a late packet is worse than a missing one.</p><p>Bulk is the opposite: guaranteed delivery through retries, but no guaranteed latency, taking whatever bandwidth is left over.</p><p>Interrupt guarantees a maximum polling interval so latency is bounded, but the packets are small: at full speed that is 64 bytes per millisecond, which is 64 kB/s and decides whether HID is viable before you write anything.</p>" },
{ t: "emb-usb", d: 1, q: "Why choose HID despite it being slow?", o: ["It offers the largest packet size", "No driver installation on any host", "It guarantees the lowest latency", "It supports bulk transfers as well as interrupt"], a: 1, why: "<p>That is its entire appeal, and it is a real one for a product that must work when plugged into a machine you do not control.</p><p>The costs are that it is interrupt-only and therefore limited to small periodic packets, and that the report descriptor is its own small language which is easy to get wrong.</p><p>For a custom application the usual better answer is vendor-specific with Microsoft OS descriptors, which binds WinUSB automatically and gives you bulk endpoints with no INF file.</p>" },
{ t: "emb-usb", d: 3, q: "A USB device does nothing at all when plugged in. What are the first candidates?", o: ["The VID and PID pair is set wrong", "D+ pull-up, USB clock, peripheral clock", "A malformed device descriptor", "An unhandled standard request"], a: 1, why: "<p>Nothing at all means the host never saw a device attach, so it is below the descriptor layer entirely.</p><p>The pull-up on D+ is what signals attachment and on most parts it is under firmware control. The USB clock has to be 48 MHz and accurate, and a bare internal RC oscillator usually is not good enough.</p><p>Descriptor faults present differently, as an unknown device, because the host did see it and then could not make sense of it.</p>" },
{ t: "emb-usb", d: 2, q: "Why can a slow control-transfer handler stop a device enumerating?", o: ["It cannot: there is no time limit on it", "Control transfers time out, so the host quits", "It corrupts the descriptor while it is in flight", "It causes the host to reset the whole bus"], a: 1, why: "<p>Enumeration is a sequence of control transfers and each is bounded. Doing real work inside a setup handler, such as reading from flash or waiting on a peripheral, can exceed it.</p><p>The symptom is a device that appears and disappears, or that enumerates intermittently depending on what else the firmware is doing.</p><p>Keep setup handling to copying a prepared response, and defer anything slow to outside the handler.</p>" },
{ t: "emb-usb", d: 2, q: "In USB, which side initiates all traffic?", o: ["Whichever side happens to have data ready", "The host, always; the device answers", "The device, by raising an interrupt", "They negotiate it at enumeration"], a: 1, why: "<p>There is no asynchronous path from device to host. Even an interrupt endpoint is polled by the host at an interval it chooses.</p><p>That is why an interrupt endpoint's latency guarantee is really a promise about how often you will be asked, and why the endpoint type decides your maximum data rate.</p><p>It also explains why almost every enumeration failure is your device giving a wrong answer to a question rather than doing something wrong of its own accord.</p>" },
{ t: "emb-usb", d: 1, q: "What does bMaxPower in a configuration descriptor specify?", o: ["The current draw in milliamps", "The current draw in 2 mA units", "The power draw in whole watts", "The bus voltage the device needs"], a: 1, why: "<p>A value of 50 means 100 mA, not 50 mA. Getting the units wrong either over-requests, which a hub may refuse, or under-declares, which is a compliance problem.</p><p>It is one of a family of silent descriptor faults alongside wTotalLength, bNumInterfaces and every descriptor's own bLength.</p><p>None of them produce an error message; the device simply does not work properly, which is why checking them methodically beats guessing.</p>" },
{ t: "emb-usb", d: 3, q: "A USB device works, then stalls under sustained load. What are the likely causes?", o: ["An error somewhere in the descriptor set", "No double buffering, or a slow handler", "The wrong VID for that class", "A missing zero-length packet"], a: 1, why: "<p>Under load the host sends packets back to back, so the device must be ready for the next one before the previous is fully processed.</p><p>Single buffering means the endpoint is NAKed while you are still working, which the host tolerates until it does not.</p><p>Both fixes are structural: double buffer the endpoint, and move processing out of the interrupt so the handler only moves data.</p>" },
{ t: "emb-usb", d: 2, q: "Which tool turns a USB enumeration failure from guesswork into reading?", o: ["An oscilloscope on D+ and D-", "A protocol analyser, or USBPcap", "A logic analyser on the power rail", "objdump on the built firmware"], a: 1, why: "<p>It shows the actual sequence of control transfers, so you can see exactly which request went unanswered or which descriptor the host rejected.</p><p>Without it you are inferring from the host's behaviour, which is a very coarse signal: unknown device covers a dozen distinct causes.</p><p>This is the same principle as putting a logic analyser on I2C: the value is often in what a clean capture rules out.</p>" },

// ---------------------------------------------------------- emb-ble
{ t: "emb-ble", d: 2, q: "A BLE link is far slower than expected. What is the first thing to check?", o: ["Whether transmit power is too low", "Whether the ATT MTU was negotiated up", "Whether the advertising interval is long", "Whether the service UUID is 128-bit"], a: 1, why: "<p>The default MTU of 23 bytes leaves 20 bytes of payload after ATT overhead. Negotiating to 247 gives 244, more than twelve times as much per packet.</p><p>It is a few lines of code on connection and it is the single most common reason a BLE link is slow.</p><p>Pair it with data length extension, which raises the link-layer payload from 27 to 251 bytes, or your large ATT packet is simply fragmented across several radio packets and you gain much less than you expected.</p>" },
{ t: "emb-ble", d: 2, q: "Why is indicate much slower than notify?", o: ["Indications carry smaller packets", "Acknowledged, one outstanding at a time", "It requires a much larger negotiated MTU", "It cannot use data length extension"], a: 1, why: "<p>Notify is unacknowledged at the ATT layer, so several can go out in a single connection event. An indication has to be confirmed before the next can be sent.</p><p>That makes indicate roughly an order of magnitude slower for continuous data.</p><p>Use indicate where the application genuinely needs per-item confirmation. For streaming, use notify and put sequence numbers in the payload if you need to detect loss.</p>" },
{ t: "emb-ble", d: 3, q: "What does slave latency give you?", o: ["A larger maximum MTU on the link", "The peripheral may skip empty events", "Faster advertising while connected", "More concurrent connections at any one time"], a: 1, why: "<p>It decouples responsiveness from power. A short connection interval keeps latency low when there is data, and slave latency lets the radio stay asleep through most events when there is not.</p><p>The constraint is that the supervision timeout must exceed the interval multiplied by slave latency plus one.</p><p>Get that relationship wrong and the link drops precisely when it goes idle, which is a confusing symptom because activity appears to fix it.</p>" },
{ t: "emb-ble", d: 2, q: "Who decides the final BLE connection parameters?", o: ["The peripheral", "The central", "Fixed by the specification", "Whichever requests first"], a: 1, why: "<p>The peripheral requests and the central decides. Phones in particular have their own policies, may ignore your request, and differ between iOS and Android.</p><p>So firmware has to work acceptably with whatever it is given rather than assume the request was granted, and it should read back what it actually got.</p><p>Designing around an assumed 7.5 ms interval that the phone never grants is a common and avoidable disappointment.</p>" },
{ t: "emb-ble", d: 1, q: "In GATT, what is the unit of data you expose?", o: ["A service", "A characteristic", "A descriptor", "An attribute handle"], a: 1, why: "<p>Services group characteristics; characteristics hold the values with properties such as read, write, notify and indicate.</p><p>The common design mistake is one characteristic per value, which costs a round trip each and wastes the payload you fought to enlarge.</p><p>Packing related values into one characteristic, with a small structure inside it, is usually the better trade once you have the MTU to carry it.</p>" },
{ t: "emb-ble", d: 3, q: "Roughly how does BLE throughput scale?", o: ["Directly with the transmit power", "Packets per event over the interval", "With the number of characteristics", "Inversely with the advertising interval"], a: 1, why: "<p>Three levers, and knowing which one you are short of tells you what to change.</p><p>At 7.5 ms you get 133 opportunities per second; at 100 ms you get 10. At the default MTU each carries 20 bytes; at 247 it carries 244.</p><p>Doing this arithmetic before writing code is what stops you optimising the wrong thing, and it is the same calculation that tells you whether an OTA update over BLE will take twenty seconds or a hundred.</p>" },
{ t: "emb-ble", d: 2, q: "Why should firmware never block inside a BLE stack callback?", o: ["Blocking is not allowed by the C standard", "A missed event costs a whole interval", "It corrupts the GATT attribute table", "Stack callbacks are not reentrant"], a: 1, why: "<p>The stack owns the radio timing and your code runs in the gaps. A callback that takes too long means the radio misses its slot.</p><p>At a 7.5 ms interval, missing one event is 7.5 ms of dead air, and missing enough of them exceeds the supervision timeout and the connection drops.</p><p>The structural answer is to decouple: put data in a queue and let another context drain it into notifications as buffers free up.</p>" },
{ t: "emb-ble", d: 2, q: "Why decouple producing data from sending it over BLE?", o: ["It saves RAM in the application", "Throughput survives a busy radio", "It reduces the radio's average power", "The stack requires a separate thread"], a: 1, why: "<p>Trying to send at the moment data is produced couples your sample rate to the radio's availability, so any hiccup loses data or blocks the producer.</p><p>A queue between them absorbs the variation, and the sender can fill each notification fully rather than sending whatever happened to be ready.</p><p>It also keeps the producing context, often an interrupt or a sensor task, free of any knowledge that BLE exists.</p>" },
{ t: "emb-ble", d: 2, q: "What is the minimum BLE connection interval?", o: ["1 ms", "7.5 ms", "10 ms", "20 ms"], a: 1, why: "<p>7.5 ms, giving at most 133 connection events per second. The maximum is 4 seconds.</p><p>That lower bound is a hard limit on latency: no amount of tuning gets a round trip below roughly one interval, so a BLE link is never going to behave like a wired one.</p><p>It also means the interval is the first number in any throughput or latency calculation.</p>" },
{ t: "emb-ble", d: 3, q: "The link drops only when the device is idle. What should you suspect?", o: ["The transmit power is marginal", "Supervision timeout too short for latency", "MTU negotiation failing on idle", "The advertising interval is set far too long"], a: 1, why: "<p>Slave latency lets the peripheral skip events when it has nothing to send, so the longest gap between packets is roughly the interval times latency plus one.</p><p>If the supervision timeout is shorter than that, the central concludes the link is gone exactly when the peripheral is being well behaved.</p><p>The symptom is distinctive: activity keeps the link up and idleness kills it, which is the opposite of what people expect.</p>" },

// ---------------------------------------------------------- emb-can
{ t: "emb-can", d: 2, q: "Two CAN nodes transmit at once. What happens to the loser?", o: ["Both of the messages are destroyed", "It backs off and retries later", "It corrupts the winner's frame", "The whole bus is reset"], a: 1, why: "<p>Every transmitter watches the bus while sending. A node that sends a recessive 1 and reads back a dominant 0 knows it has lost and stops immediately, before doing any damage.</p><p>Arbitration is therefore non-destructive, and the winner does not even pause.</p><p>The consequence for firmware is that the identifier is the priority, so identifier allocation is a system design decision rather than arbitrary numbering.</p>" },
{ t: "emb-can", d: 2, q: "Why does CAN hardware filter configuration matter?", o: ["It saves a useful amount of bus bandwidth", "Every node sees everything, so filter", "It sets the message's priority", "The CAN standard requires them"], a: 1, why: "<p>CAN has no addressing: messages are identified by content, and every node receives all of them.</p><p>At 500 kbit/s a busy bus can generate thousands of interrupts a second for messages you immediately throw away.</p><p>Filters work as (id &amp; mask) == (match &amp; mask), so grouping related identifiers into a contiguous block lets one filter accept everything you need, which is a reason to think about identifier allocation early.</p>" },
{ t: "emb-can", d: 3, q: "What is bus-off?", o: ["A fault in the physical bus wiring", "A node disconnecting itself past 255", "The recessive idle state of the whole bus", "The controller's low-power mode"], a: 1, why: "<p>It is designed behaviour rather than a defect. A node with a failing transceiver would otherwise flood the bus with error frames and destroy it for everyone, so the standard makes it remove itself.</p><p>Firmware must detect it and recover, typically after 128 occurrences of 11 recessive bits.</p><p>It must also count it, because a node that goes bus-off once a day is reporting a wiring or termination problem long before it fails outright.</p>" },
{ t: "emb-can", d: 3, q: "Your controller has three transmit mailboxes and sends in mailbox order. What is the risk?", o: ["None: the hardware sorts them by identifier", "A low-priority message goes out first", "Messages are lost from the mailbox", "Arbitration fails and the node halts"], a: 1, why: "<p>That is a priority inversion inside your own node, happening before the bus arbitration ever gets a chance.</p><p>Your carefully chosen identifier priorities stop at the edge of your own hardware.</p><p>Many parts have a configuration bit to transmit by identifier priority instead. Set it, or manage mailbox allocation yourself, but know which behaviour you have.</p>" },
{ t: "emb-can", d: 2, q: "A CAN network works at 125 kbit/s and fails at 500 kbit/s. What do you suspect first?", o: ["Something in the software layer", "Bit timing and termination", "The filter configuration", "How the identifiers are allocated"], a: 1, why: "<p>Higher rates leave less margin for propagation delay, so a marginal sample point or missing termination becomes a failure only at speed.</p><p>Termination is 120 ohms at each physical end of the bus and nowhere else. A single terminator, or terminators in the middle, produces exactly this rate-dependent behaviour.</p><p>Suspect the physical and timing layers before anything in software when a fault correlates with rate or cable length.</p>" },
{ t: "emb-can", d: 1, q: "On CAN, which identifier wins arbitration?", o: ["The highest", "The lowest", "The longest", "Whichever started first"], a: 1, why: "<p>The lowest, because 0 is dominant and a wired-AND bus resolves to the smallest value bit by bit from the most significant end.</p><p>So a lower numeric identifier is a higher priority, which is worth stating explicitly because it is the reverse of most numbering schemes people expect.</p><p>It also means identifier allocation is a system-level safety decision: the message that must always get through gets a low identifier.</p>" },
{ t: "emb-can", d: 2, q: "What does error-passive mean?", o: ["The node has stopped transmitting entirely", "Counters above 127: still talking", "A fault in the bus wiring", "The bus is idle and recessive"], a: 1, why: "<p>It is the intermediate state between error-active and bus-off, and it exists so a node that is having trouble cannot dominate the bus with assertive error frames.</p><p>Monitoring the error counters, rather than only reacting to bus-off, gives you a leading indicator: a node drifting toward error-passive is degrading before it disconnects.</p><p>Exposing those counters as a diagnostic turns an eventual field failure into a present observation.</p>" },
{ t: "emb-can", d: 2, q: "Where does CAN termination go?", o: ["120 ohms at every node", "120 ohms at each physical end", "120 ohms at the middle of it", "One terminator, anywhere on it"], a: 1, why: "<p>Two terminators, one at each end. Extra terminators load the bus and a missing one causes reflections.</p><p>Both faults present the same way: fine at low rate on a short harness, failing as rate or length increases.</p><p>It is worth measuring the resistance across a powered-down bus, which should read about 60 ohms if both terminators are present.</p>" },
{ t: "emb-can", d: 3, q: "Why is the sample point within a CAN bit worth configuring deliberately?", o: ["It is what sets the actual bit rate", "Nodes must agree; propagation eats margin", "It selects standard or extended identifiers", "It controls how filtering behaves"], a: 1, why: "<p>The controller samples at a configured fraction through the bit, typically 75 to 87.5 per cent, and every node has to be sampling at a compatible point.</p><p>Longer cables mean a bit arriving later relative to the local clock, so a sample point chosen for a bench harness can fall outside the valid window in a vehicle.</p><p>This is the mechanism behind the classic works-on-the-bench-fails-in-the-loom fault.</p>" },
{ t: "emb-can", d: 2, q: "What does CAN use instead of addresses?", o: ["MAC addresses programmed into each transceiver", "Content identifiers, filtered locally", "Node numbers assigned at startup", "A master that polls each node"], a: 1, why: "<p>The identifier says what the message is, not who it is for. Any node interested in engine speed accepts that identifier and everyone else filters it out.</p><p>That makes adding a listener free, which is genuinely useful, and it makes the identifier do double duty as the priority.</p><p>It also means a message catalogue, listing which identifier carries what at what rate, is the central design document for a CAN system.</p>" },

// ---------------------------------------------------------- emb-net
{ t: "emb-net", d: 2, q: "Throughput collapses under load with no errors reported. What is the likely cause on a small part?", o: ["A fault somewhere in the Ethernet cable", "The pbuf pool is exhausted, so drops", "The MTU is set wrongly for the link", "DNS resolution is failing silently"], a: 1, why: "<p>The buffer pool is sized at build time. When it empties the stack drops packets silently, and TCP's retransmission converts that into a throughput collapse rather than a reported error.</p><p>An application that holds a received buffer while doing slow work makes it worse, because it is consuming the resource the stack needs to receive the next packet.</p><p>The numbers to look at are the pool size, the TCP window, and how promptly the application frees what it is handed.</p>" },
{ t: "emb-net", d: 2, q: "Why is the BSD socket API often the wrong choice on a constrained part?", o: ["The API is not portable across stacks", "Each blocking connection needs a thread", "It has no support for TCP on lwIP", "It is much slower to compile in"], a: 1, why: "<p>It is the most convenient API and the most expensive one, which is why people arriving from Linux reach for it and then run out of memory.</p><p>lwIP's raw callback API runs in the stack's own context with no threads and no copies, giving the smallest footprint and the highest throughput.</p><p>The price is that your code becomes callbacks and you may never block in them, which is the same discipline as an interrupt handler.</p>" },
{ t: "emb-net", d: 3, q: "A request-response protocol over TCP has a reliable 200 ms stall per exchange. What is happening?", o: ["Steady packet loss on the link", "Nagle interacting with delayed ACK", "A DNS lookup timing out each time", "IP fragmentation and reassembly delay"], a: 1, why: "<p>Nagle holds a small send until previously sent data has been acknowledged. Delayed ACK holds that acknowledgement for up to 200 ms hoping to piggyback it on outgoing data.</p><p>With a small request and then a wait, neither side has a reason to act, so the delayed ACK timer decides your latency.</p><p>Disabling Nagle with TCP_NODELAY is usually correct for request-response traffic, and the tell is that the delay is suspiciously constant.</p>" },
{ t: "emb-net", d: 2, q: "When is UDP the better engineering choice on a constrained device?", o: ["Never: TCP is always preferable", "Periodic telemetry, where a loss is fine", "Firmware update over the network", "Transferring a file to the device"], a: 1, why: "<p>It costs almost nothing in state, buffers or timers, and it avoids a stalled TCP connection blocking a device that had nothing important to say.</p><p>For a sensor reporting every second, retransmitting a two-second-old value is worse than useless.</p><p>The middle path people forget is UDP with your own sequence numbers and an application-level acknowledgement only for the messages that actually matter.</p>" },
{ t: "emb-net", d: 3, q: "Checksum offload is misconfigured. What is the symptom?", o: ["Throughput becomes noticeably slow", "The far end discards packets silently", "The link never comes up at all", "CPU usage rises to handle all the fixups"], a: 1, why: "<p>If the MAC computes checksums in hardware and the stack also computes them, or neither does, the resulting packets are invalid and the receiver simply drops them.</p><p>Nothing on your side reports an error, because from your point of view the packet was transmitted successfully.</p><p>It presents as a connectivity fault with a clean link, which is why capturing at the far end is what identifies it.</p>" },
{ t: "emb-net", d: 2, q: "What is the correct bring-up order for Ethernet on a new board?", o: ["Your own protocol first, and then the rest", "Link, MAC counters, ARP, ping, protocol", "Ping first, then work downwards", "DHCP first, then everything else"], a: 1, why: "<p>Each step proves one layer, so a failure tells you where you are.</p><p>Link up at the expected speed and duplex first, because a duplex mismatch works at low rates and collapses under load. Then whether any frames arrive at all. Then ARP, which is the first sign the stack is alive. Then ICMP echo, which proves IP end to end.</p><p>Starting with your own protocol means a failure could be any of five layers.</p>" },
{ t: "emb-net", d: 2, q: "A link works at low rates and collapses under load. What physical-layer fault fits?", o: ["A broken or damaged cable", "A duplex mismatch", "Wrong MTU", "Wrong IP address"], a: 1, why: "<p>Half duplex on one end and full duplex on the other means collisions that only start mattering when both directions are busy.</p><p>The load dependence is the signature, and it is worth checking what the PHY actually negotiated rather than what you configured.</p><p>The other load-dependent candidate on a small part is buffer pool exhaustion, which looks similar from the application but has a completely different fix.</p>" },
{ t: "emb-net", d: 1, q: "Which lwIP API gives the smallest memory footprint?", o: ["The BSD sockets API", "The raw callback API", "The netconn API", "They are all equivalent"], a: 1, why: "<p>Raw runs in the stack's own context: no threads, no copies, no per-connection thread stack.</p><p>Netconn adds a sequential API and needs an RTOS and a copy. Sockets add BSD compatibility and a thread per blocking connection.</p><p>The trade is convenience against memory, and on a part with 128 kB the memory usually wins the argument.</p>" },
{ t: "emb-net", d: 2, q: "What is the single most useful tool when debugging a networked device?", o: ["A logic analyser on the MII bus", "A capture at the far end, Wireshark", "An oscilloscope on the magnetics", "objdump on the firmware image"], a: 1, why: "<p>It separates three quite different problems in one step: the packet never left, it left but was malformed, or it left correctly and no reply came.</p><p>Without it you are inferring from the absence of a response, which is consistent with all three.</p><p>It is the same reasoning as a logic analyser on I2C, where a clean trace tells you the fault is above the transport rather than in it.</p>" },
{ t: "emb-net", d: 3, q: "Why does an application holding a received buffer too long hurt more than it looks?", o: ["It leaks memory over a long enough period", "It starves the pool needed to receive", "It blocks the CPU from other work", "It invalidates the packet checksum"], a: 1, why: "<p>Buffers come from a fixed pool shared with reception. Holding one during slow processing shrinks what the stack has available.</p><p>The result is dropped packets under load, which TCP hides as retransmission and reduced throughput rather than as an error.</p><p>The fix is to copy out what you need and release the buffer immediately, or to process in a context that cannot stall.</p>" },

// ---------------------------------------------------------- emb-protodesign
{ t: "emb-protodesign", d: 2, q: "Why does a version field belong near the front of a frame?", o: ["It keeps the header word-aligned", "The receiver decides before parsing", "It compresses better at the front", "It falls inside the CRC coverage"], a: 1, why: "<p>Place it after any variable-length content and a parser has to understand the frame in order to discover whether it understands the frame.</p><p>The second half of the decision matters as much: write down what a receiver does with a version it does not recognise, before you need it.</p><p>That choice is the difference between a fleet you can upgrade incrementally and one that needs a flag day.</p>" },
{ t: "emb-protodesign", d: 3, q: "A retried command increments a counter twice. What is the protocol missing?", o: ["A CRC computed over the command payload", "A sequence number, to spot duplicates", "Encryption of the command frame", "A larger maximum transfer unit"], a: 1, why: "<p>Every real link loses messages, so senders retry, so receivers see duplicates. That is not an edge case, it is normal operation.</p><p>Design commands to be idempotent where you can: read the temperature is naturally safe to repeat, increment the counter is not.</p><p>Where they cannot be, a remembered sequence number lets a repeat be acknowledged without being executed, and the specification should state plainly whether delivery is at-most-once or at-least-once.</p>" },
{ t: "emb-protodesign", d: 2, q: "What does type-length-value buy over a fixed struct layout?", o: ["Frames come out noticeably smaller on the wire", "A receiver can skip what it does not know", "Parsing is faster than a fixed struct", "It detects errors better than a CRC"], a: 1, why: "<p>It costs a few bytes per field and converts every future field addition from a breaking change into something old firmware tolerates.</p><p>On a constrained link that overhead is often worth paying, because the alternative is coordinating a simultaneous update of both ends.</p><p>A fixed layout is compact and trivial to parse, and is the right choice when both ends genuinely always ship together.</p>" },
{ t: "emb-protodesign", d: 2, q: "Which integrity detail is most often left out of a protocol specification?", o: ["Which polynomial is actually used", "Exactly which bytes it covers", "The width of the check field", "The initial register value"], a: 1, why: "<p>Coverage is not part of the algorithm, so naming the CRC communicates nothing about it. Does it include the header? The length field? The start delimiter?</p><p>The other five parameters can be looked up from a catalogue entry; this one only exists in your document.</p><p>One worked example frame in hex, with its computed check value, settles it permanently and is checkable in a way prose is not.</p>" },
{ t: "emb-protodesign", d: 1, q: "Why put a sequence number in every frame even on a link you trust?", o: ["It seeds the encryption stream", "Both ends can count what they missed", "It pads the frame to a fixed size", "The CRC calculation requires one"], a: 1, why: "<p>It costs two bytes and it is what lets you distinguish loss from corruption during integration: gaps with no CRC failures mean frames were dropped in software rather than damaged on the wire.</p><p>It also makes duplicate detection possible, which retries make necessary.</p><p>Adding it later means changing the frame format, which is exactly the change a version field exists to manage.</p>" },
{ t: "emb-protodesign", d: 2, q: "You need both control commands and continuous telemetry. What is the usual shape?", o: ["One or the other; never both at once", "Request-response for control, streaming", "Publish and subscribe used for both kinds", "Polling for both, on a fixed period"], a: 1, why: "<p>They have genuinely different requirements: control wants confirmation and ordering, telemetry wants throughput and tolerates loss.</p><p>Forcing telemetry into request-response means the round trip bounds your sample rate. Forcing control into streaming means no confirmation that a command took effect.</p><p>Separate frame types on the same link is simple and common, and it keeps each half's failure handling appropriate to what it carries.</p>" },
{ t: "emb-protodesign", d: 3, q: "What must a streaming protocol have that a request-response one does not?", o: ["A CRC on every single frame", "Flow control, or a slow consumer loses", "A version field in each frame", "Sequence numbers on each frame"], a: 1, why: "<p>In request-response the consumer's readiness is implicit: nothing is sent until it asks. In streaming the producer decides, so a consumer that cannot keep up simply overruns.</p><p>Without flow control the loss is silent, which is the worst property a data path can have.</p><p>A credit or window scheme, where the consumer periodically grants permission for N more items, is the usual minimal answer.</p>" },
{ t: "emb-protodesign", d: 2, q: "Why expose protocol counters as a diagnostic message?", o: ["They are useful for benchmarking", "A retrying link still delivers, hiding it", "MISRA requires these diagnostic counters", "They are what actually triggers the retries"], a: 1, why: "<p>Recovery hides the problem. Retries, resynchronisations and CRC failures are all invisible from outside as long as the data eventually arrives correctly.</p><p>Counters make the difference between a healthy unit and a marginal one visible, which is what turns a support call into a diagnosis.</p><p>Frames sent and received, CRC failures, resynchronisations, timeouts and buffer-full drops cost a few bytes each and answer questions nothing else can.</p>" },
{ t: "emb-protodesign", d: 2, q: "Why state the byte order explicitly even when both ends are little-endian today?", o: ["It is faster to parse when stated", "The protocol outlives both ends", "The CRC depends on the byte order", "Wire protocol standards require it"], a: 1, why: "<p>A wire format is a contract, and a contract that relies on an accident of the current hardware is not a contract.</p><p>Reimplementation on a different platform, or in a different language on a host, is exactly when an unstated assumption surfaces, and by then there are devices in the field.</p><p>It costs one line in the document and it is the sort of thing that separates a specification from a description of what the code currently does.</p>" },
{ t: "emb-protodesign", d: 3, q: "What is the strongest single addition to a protocol document?", o: ["A state diagram of the exchange", "One worked frame in hex, interpreted", "A complete list of error codes", "A table of performance figures"], a: 1, why: "<p>It is checkable. Two implementers can each run their code against it and know immediately whether they agree, which no amount of prose achieves.</p><p>It also implicitly pins down byte order, field placement, framing and CRC coverage all at once, which are the four things most often ambiguous.</p><p>It takes ten minutes to produce and it settles arguments that would otherwise consume days of integration.</p>" },

// ---------------------------------------------------------- emb-ota
{ t: "emb-ota", d: 2, q: "Why calculate the transfer time before designing an update protocol?", o: ["It tells you whether to compress it first", "It decides whether resume is essential", "It tells you how much flash to fit", "It sets the signature algorithm"], a: 1, why: "<p>A 256 kB image over BLE at the default 20-byte payload is around 100 seconds, which is a long time to hold a connection to a phone in someone's pocket.</p><p>At that length interruption is normal rather than exceptional, so resume stops being a nicety.</p><p>It also shows that negotiating the MTU up is worth more than anything clever in the protocol, since it can take the same transfer under twenty seconds.</p>" },
{ t: "emb-ota", d: 2, q: "Why is stop-and-wait per chunk usually unusable?", o: ["It is unreliable over a lossy radio link", "Every chunk costs a full round trip", "It cannot detect corruption at all", "It needs far too much buffer RAM"], a: 1, why: "<p>It is trivially correct, which is why people write it, and hopelessly slow on any link with meaningful latency.</p><p>A window of chunks acknowledged as a group keeps the link busy while acknowledgements are in flight.</p><p>Flow control still matters within that, because flash erase stalls the receiver periodically, so the drain rate is not steady and a sender that assumes it is will overrun the buffer.</p>" },
{ t: "emb-ota", d: 3, q: "What must a sender verify before resuming an interrupted transfer?", o: ["That the battery will last the transfer", "That what it holds is this same image", "That the link speed has not changed", "That the flash still has endurance"], a: 1, why: "<p>Otherwise resuming after the user selected a different firmware version splices two images together.</p><p>Tag the transfer with the image's own identifier or hash, and have the receiver report both its progress and which image that progress belongs to.</p><p>The receiver must also persist its progress somewhere that survives a reset, since an interruption that matters is often a reset.</p>" },
{ t: "emb-ota", d: 2, q: "Why verify the whole image after writing rather than as it arrives?", o: ["It is faster to hash it all at once", "Bytes in transit say nothing about flash", "The hash is not available any earlier", "It uses less RAM than streaming it"], a: 1, why: "<p>A flash write can fail silently, particularly on a worn sector, so the only meaningful check reads back what is really stored.</p><p>It also belongs to the bootloader rather than the transport, because the question it answers is whether this image is safe to run.</p><p>And if the concern is origin rather than corruption, it needs a signature: a CRC or plain hash can be recomputed by anyone who modified the image.</p>" },
{ t: "emb-ota", d: 2, q: "Where do per-chunk and whole-image integrity checks each belong?", o: ["Only one of the two is really needed here", "Chunk in the transport, image in the loader", "Both of them belong in the bootloader", "Both of them belong in the transport layer itself"], a: 1, why: "<p>They answer different questions. A per-chunk CRC stops you discovering at the end of a hundred-second transfer that something was corrupted near the start.</p><p>The whole-image check decides whether the code is safe to execute, which is the bootloader's responsibility and happens after the write.</p><p>Keeping them separate also keeps the trust boundary clear: the transport can be sloppy about corruption, but only a signature answers the question of origin.</p>" },
{ t: "emb-ota", d: 3, q: "What does the new image confirming itself after first boot achieve?", o: ["Nothing useful beyond a log entry", "An intact but broken image rolls back", "It makes the next boot faster", "It verifies the image's signature once more"], a: 1, why: "<p>Integrity checks catch corruption; they say nothing about whether the firmware actually works.</p><p>Marking the image pending, booting it, and requiring it to confirm before the next reset means a broken update reverts by itself.</p><p>It is the step most often skipped, and it is what turns a bad release from a field recovery operation into a self-healing event.</p>" },
{ t: "emb-ota", d: 2, q: "Why receive into the inactive slot rather than overwriting the running image?", o: ["It is faster than in-place writing", "The running image is never touched", "It uses less flash than one slot", "It avoids the signature check step entirely"], a: 1, why: "<p>Erasing in place has an interval during which neither a complete old image nor a complete new one exists, and a power cut there is fatal.</p><p>A second slot costs flash and removes that window entirely.</p><p>It also makes rollback possible, because the previous image is still sitting there intact when the new one turns out not to work.</p>" },
{ t: "emb-ota", d: 2, q: "Why does flash erase complicate flow control during an update?", o: ["It does not; erase is quick enough", "It stalls the receiver, so drain varies", "It corrupts the receive buffer", "It resets the link temporarily"], a: 1, why: "<p>On many parts, code fetching from flash cannot execute while the controller is erasing, and an erase can take tens of milliseconds.</p><p>So the receiver periodically stops consuming entirely, and a sender pacing itself on an average rate will overrun the buffer during those pauses.</p><p>Explicit flow control, where the receiver grants credit as it frees space, handles this naturally where a fixed rate does not.</p>" },
{ t: "emb-ota", d: 1, q: "What should be exposed during an update?", o: ["Nothing at all until it has entirely finished", "Progress, state, and a reason on failure", "Only success or failure at the end", "The hash of the incoming image"], a: 1, why: "<p>An update is something the user deliberately started, so an update that simply stops with the device saying nothing is the worst possible outcome.</p><p>Distinguishing downloading from verifying from pending lets a support engineer tell a slow link from a rejected image.</p><p>A reason code on failure is what turns 'the update did not work' into something actionable without returning the unit.</p>" },
{ t: "emb-ota", d: 3, q: "A CRC over the received image passes. What does that establish?", o: ["That the image is genuinely authentic too", "Integrity only: what was sent, not who", "That the image will boot correctly", "That the image is the newest version"], a: 1, why: "<p>A CRC is keyless and public, so anyone able to substitute an image can recompute it. It detects a corrupted download and nothing else.</p><p>Rejecting firmware you did not produce needs a signature, verified against a public key the bootloader holds.</p><p>Nor does either check tell you the image works, which is what the confirm-on-first-boot step exists to establish.</p>" },

// ---------------------------------------------------------- emb-linkdebug
{ t: "emb-linkdebug", d: 2, q: "What is the highest-value change before investigating an intermittent link fault?", o: ["Increase every buffer size in the path", "Counters at each layer, exposed", "Lower the bit rate for margin", "Add more retries at the link"], a: 1, why: "<p>They cost a few bytes and turn an unmeasurable complaint into a number you can compare between a healthy unit and a misbehaving one.</p><p>They also reveal something invisible from outside: a link that is retrying constantly still delivers correct data, and is much closer to failing than one that is not.</p><p>Without them everything that follows is guesswork about a system you cannot observe.</p>" },
{ t: "emb-linkdebug", d: 3, q: "Sequence numbers show gaps but there are no CRC failures. What does that indicate?", o: ["Electrical interference on the signal line", "Loss: a buffer overflowing in software", "Congestion somewhere in the path", "A clock mismatch between the ends"], a: 1, why: "<p>Corruption announces itself as CRC failures. Frames that never arrive at all, with no integrity errors, were dropped by something rather than damaged.</p><p>The usual culprit is a receive buffer or pool exhausted under load, which is a software sizing problem rather than a physical one.</p><p>That distinction is only possible because the sequence numbers are there, which is why they earn their two bytes on any link.</p>" },
{ t: "emb-linkdebug", d: 2, q: "Why capture at both ends simultaneously?", o: ["Redundancy, in case one capture fails", "Never left, left malformed, or no reply", "It lets you measure the throughput", "It synchronises the two clocks"], a: 1, why: "<p>Those are three quite different problems with three different owners, and from one end they are indistinguishable: all you see is no response.</p><p>One capture at each end resolves it in a single step.</p><p>It is the same reasoning as a logic analyser on I2C, where the value of a clean trace is what it rules out rather than what it shows.</p>" },
{ t: "emb-linkdebug", d: 2, q: "Latency is poor but raw bandwidth is fine. What do you change?", o: ["The raw bit rate of the link", "The number of round trips", "The size of the buffers", "The CRC polynomial used"], a: 1, why: "<p>Latency is dominated by round trips, so making the link faster barely helps: you are waiting, not transferring.</p><p>The fix is protocol shape, batching several operations into one exchange or moving to a streaming form.</p><p>Throughput is a different lever entirely, governed by packet size and how much is in flight, which is why measuring the two separately matters before changing anything.</p>" },
{ t: "emb-linkdebug", d: 3, q: "How do you distinguish congestion from loss and corruption?", o: ["You cannot really tell them apart", "Latency rises before loss, with load", "Congestion causes CRC failures", "Congestion appears at random"], a: 1, why: "<p>Queues fill before they overflow, so latency climbs first and only then do frames start being dropped.</p><p>Corruption is roughly independent of load and rises with cable length, interference or rate. Loss without CRC failures points at a buffer.</p><p>Load-correlating the symptom is often the fastest single discriminator, which is why it is worth asking whether the fault tracks traffic before instrumenting anything.</p>" },
{ t: "emb-linkdebug", d: 2, q: "How do you make an intermittent link fault easier to fix?", o: ["Wait for it to happen again on its own", "Raise the rate: more load, longer cable", "Add more logging and nothing more", "Lower the bit rate to be on the safe side"], a: 1, why: "<p>If it is a race or a buffer limit, raising the rate at which the window is hit turns a daily event into a per-minute one.</p><p>Reproduction is most of the work, because without a reliable trigger you cannot distinguish a fix from a coincidence.</p><p>That matters especially here, since many changes alter timing incidentally and can suppress a race without fixing it, so it returns when something else changes.</p>" },
{ t: "emb-linkdebug", d: 1, q: "Which question about an intermittent fault is worth asking first?", o: ["What bit rate is the link running", "Always or sometimes, and what correlates", "Which CRC polynomial is in use", "How old is the hardware involved"], a: 1, why: "<p>The adjective in the complaint is evidence. Consistent failure means something systematic; occasional failure means timing, load or environment.</p><p>Correlation with load, temperature, cable length or a specific unit each eliminates a class of cause, and all of it is free.</p><p>Asking these before reaching for an instrument is what separates a short investigation from a long one.</p>" },
{ t: "emb-linkdebug", d: 2, q: "The fault occurs in one direction only. Why does that matter?", o: ["It does not narrow anything down", "It halves the search immediately", "It points to corruption on the wire", "It points to congestion downstream"], a: 1, why: "<p>A bidirectional link has four independent pieces: two transmit paths and two receive paths. A one-directional fault clears half of them at once.</p><p>It also tends to rule out shared causes such as termination or a common clock, and point at buffering or handling on one side.</p><p>It is one of the cheapest discriminating questions available and it is often not asked.</p>" },
{ t: "emb-linkdebug", d: 3, q: "Why is jitter often the quantity that actually breaks things?", o: ["It is by far the largest of the delays", "A constant delay can be designed around", "It is what causes the CRC errors", "It scales directly with the load"], a: 1, why: "<p>Anything downstream that assumes a regular arrival, a control loop, a timestamped stream, a watchdog on message age, is built on the interval rather than on the average rate.</p><p>A fixed delay is just an offset and can be compensated. A varying one cannot be, because you do not know it in advance.</p><p>It also frequently originates in your own firmware, in scheduling or buffering, rather than in the link at all.</p>" },
{ t: "emb-linkdebug", d: 2, q: "A link delivers correct data but the retry counter is high. What is the situation?", o: ["Everything is fine as it stands", "Working, but close to failing", "The counters themselves are wrong", "The CRC is too weak for the link"], a: 1, why: "<p>Recovery hides degradation. A link retrying on one frame in ten produces the same output as one retrying on one in ten thousand, right up until conditions worsen slightly.</p><p>This is exactly what the counters exist to reveal, and why comparing a suspect unit against a known-good one is such an effective technique.</p><p>Treat a rising retry rate as a fault to investigate rather than as the system working as intended.</p>" }

]);
