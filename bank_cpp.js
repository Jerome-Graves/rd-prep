BANKS["C++"] = [
{ t: "raii", d: 1, q: "What is the core idea of RAII?", o: ["Resources are freed by a background garbage collector", "A central manager object tracks and releases all resources", "A resource is acquired in a constructor and released in the destructor", "Resources must be released manually in reverse order of acquisition"], a: 2, why: "RAII ties a resource's lifetime to an object's scope: the destructor releases it deterministically, even when exceptions unwind the stack." },
{ t: "raii", d: 1, q: "Why can std::unique_ptr not be copied?", o: ["Copying would create two owners that would each try to delete the same object", "Its copy constructor is private for historical reasons", "It can be copied, but only within the same scope", "Copying is forbidden only when a custom deleter is present"], a: 0, why: "unique_ptr models exclusive ownership; its copy operations are deleted so a double delete cannot happen. Ownership transfer is done by moving." },
{ t: "raii", d: 2, q: "What does std::lock_guard provide over calling lock and unlock manually?", o: ["It makes the mutex recursive", "It is faster because it avoids a system call", "It lets multiple readers hold the mutex at once", "Its destructor releases the mutex even if the protected code throws"], a: 3, why: "lock_guard is RAII for a mutex: unlock happens in the destructor on every exit path, including exceptions, preventing a permanently stuck lock." },
{ t: "raii", d: 2, q: "Why should a destructor not let an exception escape?", o: ["Destructors cannot contain try blocks", "If it throws during stack unwinding, std::terminate is called", "The exception is silently swallowed, hiding bugs", "The object's memory would never be reclaimed"], a: 1, why: "If unwinding from one exception runs a destructor that throws another, the program terminates. Destructors are implicitly noexcept since C++11." },
{ t: "raii", d: 1, q: "A std::ofstream with buffered output goes out of scope. What happens to the open file?", o: ["It stays open until the program exits", "The destructor flushes pending output and closes the file", "It closes, but unwritten buffered data is lost", "The program aborts with an I/O error"], a: 1, why: "File streams are RAII types: the destructor flushes and closes. It swallows errors, though, so critical code should flush and check explicitly." },
{ t: "raii", d: 2, q: "Why is std::make_unique preferred over constructing unique_ptr from a raw new expression?", o: ["It allocates the object and control block together", "It is the only way to pass constructor arguments", "It allocates from a faster pool", "It avoids a naked new, so the object is owned by a smart pointer immediately"], a: 3, why: "make_unique removes raw new from user code and is safer in complex expressions. The single-allocation optimization belongs to make_shared, not make_unique." },
{ t: "raii", d: 3, q: "In what order are the non-static data members of a class destroyed?", o: ["Reverse of their declaration order in the class", "Reverse of the constructor initializer list order", "Unspecified; the compiler chooses", "Declaration order, before the destructor body runs"], a: 0, why: "Members are constructed in declaration order and destroyed in exactly the reverse order, after the destructor body runs. Initializer list order is irrelevant." },
{ t: "raii", d: 2, q: "With shared_ptr and weak_ptr, when are the managed object and the control block freed?", o: ["Both when the last shared_ptr is destroyed", "Both when the last weak_ptr is destroyed", "Object with the last shared_ptr; control block once the last weak_ptr also goes", "Object on first reset; control block at program exit"], a: 2, why: "The strong count reaching zero destroys the object; the control block survives until the weak count also reaches zero so weak_ptr can still report expiry." },
{ t: "raii", d: 2, q: "A constructor throws halfway through. What cleanup does the language perform?", o: ["Destructors run for bases and members already fully constructed, not the object itself", "The object's own destructor runs and cleans everything", "No cleanup at all; everything leaks", "Only members with virtual destructors are cleaned up"], a: 0, why: "The whole object's lifetime never began, so its destructor is not called, but every fully constructed subobject is destroyed. RAII members therefore prevent leaks." },
{ t: "raii", d: 3, q: "Which statement about unique_ptr custom deleters is correct?", o: ["The deleter is type-erased, so pointer size never changes", "Deleters must be function pointers", "A stateful deleter can make the unique_ptr bigger than a raw pointer", "The deleter also runs when the unique_ptr is null"], a: 2, why: "The deleter is part of the unique_ptr type and stored inline; only empty stateless deleters keep it pointer-sized. Type erasure is shared_ptr. A null unique_ptr never invokes its deleter." },
{ t: "raii", d: 2, q: "You receive a handle from a C library that must be released with lib_close. Idiomatic modern C++?", o: ["Call lib_close manually on every return path", "Wrap the handle in unique_ptr with a custom deleter calling lib_close", "Store it globally and release at exit", "Rely on the OS to reclaim it when the process ends"], a: 1, why: "A unique_ptr or small RAII wrapper with a custom deleter guarantees release on every path, including exceptions, with zero manual bookkeeping." },
{ t: "raii", d: 3, q: "A class derives from enable_shared_from_this. Calling shared_from_this inside its constructor does what in C++17?", o: ["Returns a shared_ptr with use count 1", "Returns an empty shared_ptr", "It is undefined behavior", "Throws std::bad_weak_ptr because no shared_ptr owns the object yet"], a: 3, why: "The internal weak reference is set only after a shared_ptr takes ownership, which happens after construction; C++17 specifies the call throws bad_weak_ptr before then." },
{ t: "move", d: 1, q: "What does std::move(x) do by itself?", o: ["Moves x's resources into a temporary", "Destroys x and frees its resources", "Copies x then marks the original invalid", "Nothing at runtime; it casts x to an rvalue reference"], a: 3, why: "std::move is just a cast that enables move overloads to be selected. Any actual resource transfer happens in the move constructor or assignment that receives the result." },
{ t: "move", d: 2, q: "What state is a std::string in after being moved from?", o: ["Guaranteed empty", "Valid but unspecified; safe to assign to or destroy", "Destroyed; any further use is undefined behavior", "Unchanged, because strings always copy"], a: 1, why: "Standard library moved-from objects are valid with unspecified value: operations without preconditions, like assignment or clear, are fine; assuming emptiness is not portable." },
{ t: "move", d: 2, q: "Why is writing return std::move(local_vec); usually worse than return local_vec; ?", o: ["It creates a dangling reference", "It copies the vector twice", "It blocks NRVO, forcing a move where elision could construct in place", "It fails to compile for standard containers"], a: 2, why: "Returning the plain name of a local enables copy elision (and implicit move as fallback); wrapping it in std::move changes the expression and disqualifies NRVO." },
{ t: "move", d: 2, q: "Overloads f taking a const lvalue reference and f taking an rvalue reference exist. Which runs for f(std::move(w))?", o: ["The rvalue reference overload", "The const lvalue reference overload", "Neither; the call is ambiguous", "Whichever is declared first"], a: 0, why: "std::move yields an xvalue, and an rvalue reference parameter is a better match for rvalues than a const lvalue reference." },
{ t: "move", d: 3, q: "Why mark a move constructor noexcept?", o: ["Moves that may throw do not compile", "The compiler can then omit the destructor call", "std::vector reallocation moves elements only if the move cannot throw, else it copies", "noexcept functions are inlined by mandate"], a: 2, why: "vector preserves the strong guarantee during reallocation via move_if_noexcept: a throwing move demotes to copies, and you silently lose move performance." },
{ t: "move", d: 1, q: "The main purpose of move semantics is to...", o: ["run resource transfers on a background thread", "compact objects into contiguous storage", "let the compiler garbage-collect temporaries", "transfer ownership of resources cheaply instead of deep copying"], a: 3, why: "A move pilfers the source's internal resources, such as a heap buffer, leaving the source valid but emptied, and avoids an expensive deep copy." },
{ t: "move", d: 2, q: "You std::move a const std::string into a new string. What actually happens?", o: ["It is copied; a const rvalue cannot bind to the move constructor", "It is moved as normal", "It fails to compile", "It is undefined behavior"], a: 0, why: "The move constructor takes a non-const rvalue reference, which cannot bind to a const argument, so overload resolution silently falls back to the copy constructor." },
{ t: "move", d: 3, q: "Inside a function template with a forwarding reference parameter, what does std::forward do?", o: ["Unconditionally casts to an rvalue like std::move", "Restores the argument's value category: lvalues stay lvalues, rvalues can move", "Copies the argument to protect the caller", "Disables template argument deduction"], a: 1, why: "Perfect forwarding uses the deduced template argument to cast back to the caller's value category, so wrappers pass arguments through without spurious copies or moves." },
{ t: "move", d: 2, q: "After moving from a std::unique_ptr, what does the source contain?", o: ["A valid but unspecified pointer", "The old address, until scope exit", "It is guaranteed to be null", "Touching it is undefined behavior"], a: 2, why: "unique_ptr is one of the few types with a fully specified moved-from state: the source is left equal to nullptr, so it can be safely tested and reused." },
{ t: "move", d: 3, q: "How does the generic std::swap(a, b) work since C++11?", o: ["Move-constructs a temporary from a, then move-assigns a from b and b from the temporary", "Copies both objects and destroys the originals", "Exchanges the raw bytes of the two objects", "It requires the type to provide a member swap"], a: 0, why: "Generic swap is one move construction plus two move assignments, which is why cheap noexcept moves matter; types may still provide a faster specialized swap." },
{ t: "stl", d: 1, q: "What is the amortized complexity of std::vector::push_back?", o: ["Logarithmic in size", "Always linear, because elements may be copied", "Quadratic in the worst case", "Amortized constant time"], a: 3, why: "Capacity grows geometrically, so the occasional linear reallocation averages out to constant time per insertion." },
{ t: "stl", d: 1, q: "Lookup complexity of std::map versus std::unordered_map?", o: ["Both are constant time", "map is logarithmic; unordered_map is constant on average", "map is constant; unordered_map is logarithmic", "Both are logarithmic"], a: 1, why: "map is a balanced tree with logarithmic lookups; unordered_map is a hash table with average constant lookups but a linear worst case." },
{ t: "stl", d: 2, q: "push_back triggers a reallocation of a std::vector. Which stay valid?", o: ["Iterators before the insertion point", "Nothing: all iterators, pointers, and references to its elements are invalidated", "References, but not iterators", "Everything except the end iterator"], a: 1, why: "Reallocation moves the elements to new storage and frees the old buffer, so every iterator, pointer, and reference into the old buffer becomes invalid." },
{ t: "stl", d: 2, q: "What problem does the erase-remove idiom address?", o: ["std::remove throws when the value is missing", "erase alone reorders the container", "It preserves the begin iterator across deletions", "std::remove only shifts elements and returns a new logical end; erase trims the tail"], a: 3, why: "Algorithms cannot resize containers. remove compacts the kept elements and returns the new logical end; the container's erase then physically removes the leftover tail." },
{ t: "stl", d: 1, q: "Difference between vector reserve(n) and resize(n)?", o: ["reserve changes capacity only; resize changes the number of elements", "They are interchangeable", "reserve default-constructs n elements; resize only allocates", "resize changes capacity only; reserve fills with zeros"], a: 0, why: "reserve preallocates storage without creating elements; resize creates value-initialized elements or destroys existing ones so size becomes exactly n." },
{ t: "stl", d: 2, q: "Why does std::sort not work on std::list?", o: ["List elements are stored const", "std::sort would deadlock on the list's internal locks", "std::sort needs random access iterators; list only offers bidirectional ones", "Lists maintain sorted order automatically"], a: 2, why: "std::sort requires random access iterators for its partitioning jumps; std::list provides bidirectional iterators and supplies its own member sort instead." },
{ t: "stl", d: 2, q: "For std::map m, what does the expression m[k] do when k is not present?", o: ["Inserts k with a value-initialized mapped value and returns a reference to it", "Throws std::out_of_range", "Returns a default value without changing the map", "It is undefined behavior"], a: 0, why: "The bracket operator default-inserts the mapped value for a missing key, which also means it cannot be used on a const map; use find or at for pure lookup." },
{ t: "stl", d: 3, q: "After push_front on a std::deque, what remains valid?", o: ["All iterators and references", "Nothing", "References and pointers to existing elements, but no iterators", "Only iterators into the middle"], a: 2, why: "Insertion at either end of a deque invalidates all iterators but leaves references and pointers to existing elements valid, unlike vector reallocation." },
{ t: "stl", d: 2, q: "What does std::array offer over a built-in C array?", o: ["Heap storage with bounds growth", "Value semantics, a size method, and no decay to a pointer", "Faster indexing than raw arrays", "Automatic sorting of contents"], a: 1, why: "std::array is a thin aggregate wrapper: it copies and compares as a value, knows its size, works with algorithms, and costs nothing over a raw array." },
{ t: "stl", d: 1, q: "What does emplace_back do that push_back cannot?", o: ["Insert at the front", "Reserve capacity before inserting", "Skip destructors of existing elements", "Construct the element in place directly from constructor arguments"], a: 3, why: "emplace_back forwards its arguments to the element's constructor inside the container, avoiding a separate temporary object and its move or copy." },
{ t: "stl", d: 1, q: "Iterating a std::set from begin to end visits elements in...", o: ["insertion order", "unspecified hash order", "reverse insertion order", "sorted order defined by the comparator"], a: 3, why: "set is an ordered associative container, typically a red-black tree; in-order traversal yields keys sorted by the comparator, less-than by default." },
{ t: "stl", d: 3, q: "When std::unordered_map rehashes, what is invalidated?", o: ["Nothing", "All iterators, but references and pointers to elements stay valid", "Iterators and references both", "Only iterators into the bucket that grew"], a: 1, why: "Rehashing redistributes nodes among new buckets: iterators die, but the nodes themselves do not move in memory, so element references and pointers survive." },
{ t: "stl", d: 2, q: "Fastest standard approach to test whether a value exists in a large sorted std::vector?", o: ["std::find, scanning linearly", "std::count, which stops at the first hit", "std::binary_search or lower_bound, logarithmic on random access iterators", "Copy into a std::set and call count"], a: 2, why: "Sorted plus random access means binary search with logarithmic probes. find is linear, count scans everything, and building a set costs far more than it saves." },
{ t: "stl", d: 2, q: "Which is true of the small string optimization (SSO)?", o: ["An implementation technique storing short strings inline; not mandated by the standard", "Guaranteed for strings under 16 characters", "It places every string entirely on the stack", "It makes moving any string a pure pointer swap"], a: 0, why: "Most implementations keep short strings in an internal buffer to skip heap allocation; the standard permits but does not require it. It also means short-string moves copy bytes." },
{ t: "const", d: 1, q: "What does the trailing const in: int size() const; mean?", o: ["The return value is const", "The function may be evaluated at compile time", "The function cannot modify non-mutable members and is callable on const objects", "Parameters are implicitly const"], a: 2, why: "A const member function sees this as pointer-to-const, so it cannot mutate ordinary members and is the only kind callable through a const object or reference." },
{ t: "const", d: 2, q: "What does the mutable keyword on a data member allow?", o: ["The member becomes atomic", "Derived classes may modify the private member", "The member skips construction", "Modification even inside const member functions, e.g. a cache or a mutex"], a: 3, why: "mutable exempts a member from the class's logical constness, letting const functions update internal details like memoized values or a guarding mutex." },
{ t: "const", d: 1, q: "Given the declaration const int* p, which is true?", o: ["p may be repointed, but the int cannot be modified through p", "Neither p nor the pointee can change", "The pointee can change but p cannot be reassigned", "Both can change freely"], a: 0, why: "const binds to the int: this is a pointer to const int. A pointer that itself cannot change would put const after the asterisk instead." },
{ t: "const", d: 2, q: "What does constexpr on a function actually guarantee?", o: ["Every call is evaluated at compile time", "It is usable in constant expressions with constant arguments; it can still run at runtime", "It may not take parameters", "It always returns a literal by value"], a: 1, why: "constexpr marks a function as eligible for constant evaluation; compile-time execution happens only in contexts that require it or when the compiler chooses to fold it." },
{ t: "const", d: 2, q: "When does writing through a pointer obtained via const_cast cause undefined behavior?", o: ["Always", "Never; const_cast legalizes the write", "When the referenced object was itself defined const", "Only for objects with virtual functions"], a: 2, why: "const_cast only removes the qualifier from the pointer or reference. If the underlying object was created const, modifying it is undefined behavior." },
{ t: "const", d: 1, q: "Best way to pass a large read-only object like std::string into a function?", o: ["By const reference", "By value, so the callee owns a copy", "By non-const reference", "By raw pointer to non-const"], a: 0, why: "A const reference avoids the copy and documents that the callee only reads. Pass by value only when the callee needs its own copy anyway." },
{ t: "const", d: 3, q: "A const member function uses a member declared as a raw pointer. What does const restrict?", o: ["Both the pointer and the pointed-to object", "Only the pointed-to object", "Nothing; pointers ignore const", "Only the pointer member itself; the pointee may still be mutated"], a: 3, why: "Constness is shallow: the pointer becomes const, but the object it points at is not part of this object, so writes through it compile. Deep constness needs discipline or wrappers." },
{ t: "const", d: 3, q: "A class overloads the index operator with const and non-const versions. Which is chosen for a non-const object?", o: ["The const version, being more restrictive", "The non-const version", "The call is ambiguous", "Whichever is declared first"], a: 1, why: "Overload resolution matches the implicit object argument: a non-const object prefers the non-const member. The const overload serves const access paths." },
{ t: "classes", d: 1, q: "When must a base class have a virtual destructor?", o: ["Whenever the class has any data members", "When derived objects are deleted through a pointer to the base", "Only when the class is abstract", "Always, or the code will not link"], a: 1, why: "Deleting a derived object via a base pointer with a non-virtual destructor is undefined behavior; a virtual destructor makes delete dispatch to the derived destructor." },
{ t: "classes", d: 2, q: "Which special member functions does the rule of five cover?", o: ["Default constructor plus four converting constructors", "Constructor, destructor, and three assignment forms", "The five constructors any class may declare", "Destructor, copy constructor, copy assignment, move constructor, move assignment"], a: 3, why: "If a class manages a resource so that one of these needs custom logic, all five usually need attention, or copies and moves will mishandle the resource." },
{ t: "classes", d: 1, q: "You add a constructor taking an int to a class. What happens to the implicit default constructor?", o: ["It is no longer generated; add a defaulted one back if needed", "Nothing; it is always available", "It becomes private", "It is generated but marked deleted"], a: 0, why: "Declaring any constructor suppresses the implicitly generated default constructor. Write it with = default to restore it." },
{ t: "classes", d: 2, q: "Main benefit of the override keyword on a derived virtual function?", o: ["It makes the function callable without an object", "It speeds up virtual dispatch", "The compiler errors if the function does not actually override a base virtual", "It seals the function against further overriding"], a: 2, why: "override turns silent signature mismatches, such as wrong const-ness or parameter types, into compile errors. Sealing is what final does." },
{ t: "classes", d: 2, q: "A base class constructor calls a virtual function overridden in the derived class. Which version executes?", o: ["The base class version", "The derived override", "Undefined behavior in every case", "Decided randomly at runtime"], a: 0, why: "During base construction the object's dynamic type is still the base class, so dispatch stops there. Only calling a pure virtual this way is undefined behavior." },
{ t: "classes", d: 3, q: "What is object slicing?", o: ["Splitting one object across two cache lines", "Passing a derived object by reference to a base parameter", "Copying a derived object into a base-typed value, discarding the derived parts", "Deleting a derived object through a base pointer"], a: 2, why: "Assigning or passing by value as the base type copies only the base subobject; derived data and overrides are lost. References and pointers avoid slicing." },
{ t: "classes", d: 1, q: "Declaring a pure virtual function in a class makes the class...", o: ["final", "abstract: it cannot be instantiated directly", "non-copyable", "implicitly virtual in all members"], a: 1, why: "A class with at least one pure virtual function is abstract; only derived classes that override all pure virtuals can be instantiated." },
{ t: "classes", d: 2, q: "In what order do constructor initializer list entries actually execute?", o: ["In the order written in the list", "Bases last, members first", "Unspecified order", "In member declaration order in the class, regardless of list order"], a: 3, why: "Members initialize in declaration order, after bases. A list written in a different order can silently read uninitialized members; good compilers warn about it." },
{ t: "classes", d: 3, q: "Best modern way to make a class non-copyable?", o: ["Declare copy operations private without definitions", "Mark the class final", "Give it a protected destructor", "Declare copy constructor and copy assignment as deleted with = delete"], a: 3, why: "= delete makes any copy attempt a clear compile-time error. The old private-and-undefined trick fails only at link time with worse diagnostics." },
{ t: "classes", d: 2, q: "How are virtual function calls typically implemented?", o: ["A runtime name lookup in a string table", "Each object carries a pointer to its class's table of function pointers", "Compiler-inserted switch statements at each call site", "The linker rewrites call addresses at load time"], a: 1, why: "Typical ABIs add a hidden vtable pointer to each polymorphic object; a virtual call indexes the vtable. The standard mandates the behavior, not this mechanism." },
{ t: "templates", d: 1, q: "Why are template definitions usually placed in header files?", o: ["The preprocessor expands templates", "Object files cannot contain template code", "Instantiation requires the full definition to be visible in the using translation unit", "Headers are compiled once, saving time"], a: 2, why: "The compiler generates code per instantiation, so it needs the definition wherever the template is used with a new type; hiding it in one cpp file causes link errors." },
{ t: "templates", d: 1, q: "f returns a const reference to a std::string. What is x in: auto x = f();", o: ["A std::string copy; auto drops the reference and top-level const", "A const reference to the original", "A dangling reference", "A const std::string"], a: 0, why: "Plain auto deduces by value like template deduction, stripping references and top-level const, so x is an independent copy. Add reference and const yourself to keep them." },
{ t: "templates", d: 2, q: "What is class template argument deduction (CTAD), added in C++17?", o: ["Automatic conversion between related specializations", "Deduction of member function return types", "Constructor arguments deduce the class template parameters, as in std::pair p(1, 2.5)", "A rule defaulting missing template arguments to int"], a: 2, why: "Since C++17 the compiler can infer class template arguments from a constructor call, removing the need for helpers like make_pair in many cases." },
{ t: "templates", d: 2, q: "Inside a template, what does if constexpr provide that a regular if does not?", o: ["Faster runtime branching", "Conditions on runtime values", "Use outside function bodies", "The untaken branch is discarded, so it may contain code invalid for that type"], a: 3, why: "With if constexpr the failing branch is not instantiated for that specialization, enabling compile-time dispatch without SFINAE or overload tricks." },
{ t: "templates", d: 3, q: "Why must you write typename before a dependent name, like the nested iterator type of a template parameter T?", o: ["Without it the compiler assumes a dependent qualified name is not a type", "typename introduces a new template parameter there", "It is purely optional style", "It defers name lookup to link time"], a: 0, why: "Names depending on a template parameter are parsed before instantiation; the grammar assumes they are not types, so typename is required to say the name is a type." },
{ t: "templates", d: 3, q: "A function template calls t.serialize(). You instantiate it with a type lacking that member. When does the error appear?", o: ["When the template is first parsed", "At instantiation with that concrete type", "At link time", "At runtime, via an exception"], a: 1, why: "Dependent expressions are checked at instantiation, so template type errors surface only when a specific type is plugged in, often with long diagnostics." },
{ t: "templates", d: 2, q: "std::array takes an element type and a size. What kind of parameter is the size?", o: ["A defaulted type parameter", "A constructor argument stored in the object", "A non-type template parameter, fixed at compile time", "A preprocessor constant"], a: 2, why: "The size is a non-type template parameter and part of the type itself: arrays of different sizes are unrelated types, and the size costs no runtime storage." },
{ t: "templates", d: 2, q: "What type is deduced in: auto v = { 1, 2, 3 }; ?", o: ["std::initializer_list of int", "std::vector of int", "A built-in array of three ints", "It does not compile"], a: 0, why: "Copy-initializing auto from a braced list is a special deduction rule producing std::initializer_list. Direct brace initialization follows different rules, so be careful." },
{ t: "concurrency", d: 1, q: "Which situation is a data race in C++?", o: ["Two threads reading the same const variable", "Two threads locking different mutexes", "Any access to a global from two threads", "Unsynchronized access to the same location by two threads where at least one writes"], a: 3, why: "Concurrent conflicting accesses without synchronization, at least one being a write, form a data race, and the standard makes that undefined behavior, not just stale reads." },
{ t: "concurrency", d: 1, q: "A lookup table is fully built before worker threads start, and they only read it. Synchronization needed?", o: ["Yes, a mutex around every read", "No; concurrent reads with no concurrent writer are race-free", "Yes, each entry must be atomic", "Only if the table exceeds one cache line"], a: 1, why: "Data races require a writer. Immutable data published before thread creation is safe to read concurrently; starting the threads provides the happens-before edge." },
{ t: "concurrency", d: 2, q: "counter is a std::atomic of int. Is incrementing it from multiple threads correct?", o: ["No; the read and write can interleave", "Yes; the increment is a single atomic read-modify-write operation", "Only while also holding a mutex", "Only on single-core CPUs"], a: 1, why: "Atomic increment is one indivisible read-modify-write, so no updates are lost. The same increment on a plain int would be a data race." },
{ t: "concurrency", d: 2, q: "Why should you wait on a std::condition_variable with a predicate (or in a loop)?", o: ["The predicate speeds up notification", "It prevents deadlocks automatically", "wait does not compile without one", "Wakeups can be spurious or stale, so the actual condition must be re-checked"], a: 3, why: "Condition variables may wake spuriously, and state can change between notify and wake-up. The predicate form re-checks the condition under the lock before proceeding." },
{ t: "concurrency", d: 3, q: "What does memory_order_relaxed guarantee on a std::atomic operation?", o: ["Atomicity only; no ordering of surrounding memory operations", "Atomicity plus acquire-release ordering", "Nothing; the value may tear", "A single total order visible to all threads"], a: 0, why: "Relaxed operations are indivisible and race-free on that variable but impose no inter-thread ordering, so they cannot safely publish other data by themselves." },
{ t: "concurrency", d: 2, q: "A std::thread object that is still joinable is destroyed. What happens?", o: ["The thread detaches automatically", "The destructor joins and waits", "std::terminate is called", "The thread pauses until a new handle adopts it"], a: 2, why: "Destroying a joinable std::thread calls std::terminate; you must join or detach first. C++20's jthread joins in its destructor instead." },
{ t: "concurrency", d: 2, q: "Two code paths must each hold the same two mutexes at once. Safest C++17 approach?", o: ["One std::scoped_lock acquiring both, using its deadlock-avoidance algorithm", "Lock them in whatever order each path likes", "A recursive_mutex locked twice", "Spin on try_lock for the second mutex forever"], a: 0, why: "Inconsistent lock ordering is the classic deadlock recipe. scoped_lock, like std::lock, acquires multiple mutexes with a deadlock-avoidance algorithm." },
{ t: "concurrency", d: 1, q: "One thread sets a plain bool stop flag; another polls it in a loop. No synchronization. Verdict?", o: ["Fine; bool writes are inherently atomic", "Fine if the bool is declared volatile", "A data race and undefined behavior; use std::atomic of bool", "A problem only on multi-socket servers"], a: 2, why: "An unsynchronized write plus read is a data race regardless of size; the compiler may even hoist the read out of the loop. An atomic bool fixes visibility and validity." },
{ t: "concurrency", d: 3, q: "Simplest correct thread-safe lazy singleton initialization since C++11?", o: ["Double-checked locking with a plain pointer", "A function-local static; the language guarantees thread-safe initialization", "An init function every caller remembers to call first", "A volatile pointer checked before and after locking"], a: 1, why: "C++11 magic statics: the first thread runs the initializer while others block until it completes. Classic double-checked locking with non-atomic pointers is broken." },
{ t: "concurrency", d: 2, q: "A struct with several fields must always be updated consistently and read by multiple threads. Right tool?", o: ["Make each field individually atomic", "Mark the struct volatile", "Nothing, if updates are rare", "A mutex guarding the whole struct to protect the multi-field invariant"], a: 3, why: "Atomics protect single objects, not invariants spanning fields; readers could observe half-updated combinations. A mutex makes the compound update atomic as a unit." },
{ t: "concurrency", d: 3, q: "What is special about the destructor of a future obtained from std::async with the async launch policy?", o: ["It detaches the task", "It cancels the task", "Nothing; future destructors never block", "It blocks until the associated task finishes"], a: 3, why: "Uniquely for std::async futures, the destructor waits on the shared state, so discarding the future makes the call effectively synchronous. A common surprise." },
{ t: "concurrency", d: 1, q: "What does calling join() on a std::thread do?", o: ["Forces the thread to terminate immediately", "Blocks the calling thread until that thread finishes", "Merges the two threads into one scheduler slot", "Yields the CPU once to the other thread"], a: 1, why: "join waits for the thread's completion and reclaims its resources; afterwards the thread object is no longer joinable." },
{ t: "memory", d: 1, q: "Key lifetime difference between stack and heap objects?", o: ["Heap allocation is faster", "Stack memory is shared across threads; heap is per-thread", "Stack objects die automatically at scope exit; heap objects live until freed", "Heap objects cannot contain pointers"], a: 2, why: "Automatic storage ends deterministically with the enclosing scope; dynamic storage persists until explicitly deallocated, which is why leaks are possible." },
{ t: "memory", d: 1, q: "Which is a classic memory leak?", o: ["new without a matching delete, e.g. skipped by an early return or exception", "Storing objects in a std::vector", "Holding objects in unique_ptr", "Large local arrays in a deep call stack"], a: 0, why: "Raw new demands a delete on every path. Early returns and exceptions are exactly how deletes get skipped, and exactly what RAII smart pointers solve." },
{ t: "memory", d: 2, q: "A function returns the address of one of its local variables. Using that pointer later is...", o: ["safe until the next function call", "safe; stack pages are never reused", "undefined behavior; the storage duration ended at return", "a guaranteed compile error"], a: 2, why: "The local's lifetime ends when the function returns, so the pointer dangles. It may seem to work until the stack is overwritten, making the bug intermittent." },
{ t: "memory", d: 2, q: "Memory allocated with array new is released with plain (non-array) delete. Verdict?", o: ["Fine; the forms are interchangeable", "Only the first element is freed; the rest leaks", "A guaranteed immediate crash", "Undefined behavior; array new must pair with array delete"], a: 3, why: "The two forms may use different bookkeeping, and plain delete runs at most one destructor; the standard makes mismatched pairs undefined behavior." },
{ t: "memory", d: 3, q: "A struct holds char a; double b; char c; and sizeof reports 24, not 10. Why?", o: ["Padding aligns b to 8 bytes and pads the tail so array elements stay aligned", "A hidden vtable pointer is added", "double members always force size 24", "char occupies 8 bytes inside structs"], a: 0, why: "Members get alignment padding: 7 bytes after a and 7 after c so consecutive array elements keep b aligned. Ordering members largest-first shrinks such structs." },
{ t: "memory", d: 2, q: "You keep a reference to element 0 of a vector, then push_back grows it past capacity. Using the reference is...", o: ["fine; references follow the element", "undefined behavior; the elements moved to new storage", "fine for trivially copyable element types", "a problem only in debug builds"], a: 1, why: "Reallocation transfers elements to a new buffer and frees the old one, so the reference dangles. Reserve capacity up front or use indices if the vector must grow." },
{ t: "memory", d: 1, q: "Dereferencing a null pointer in C++ is...", o: ["a guaranteed segmentation fault", "defined to return zero", "undefined behavior, even though many platforms happen to fault", "an exception you can catch"], a: 2, why: "The standard makes it undefined behavior: a fault is common but not promised, and the optimizer may assume it never happens, with surprising results." },
{ t: "memory", d: 2, q: "Overflow behavior: signed int versus unsigned int?", o: ["Signed overflow is undefined behavior; unsigned wraps modulo two to the width", "Both wrap around", "Both are undefined", "Signed wraps; unsigned saturates at the maximum"], a: 0, why: "Unsigned arithmetic is defined as modular. Signed overflow is undefined, and optimizers exploit that, e.g. assuming x plus 1 is greater than x for signed x." },
{ t: "memory", d: 3, q: "You constructed an object into a raw byte buffer with placement new. How do you end its lifetime?", o: ["delete the pointer normally", "free the buffer, which runs the destructor", "Do nothing; such objects are trivially destroyed", "Invoke the destructor explicitly; never delete through that pointer"], a: 3, why: "Placement new separates storage from lifetime: you call the destructor yourself, and the storage is released by whoever owns the buffer, not by delete." },
{ t: "memory", d: 2, q: "Reading a local int that was never assigned is...", o: ["guaranteed to give zero", "undefined behavior; the value is indeterminate", "implementation-defined but harmless", "fine if the result is only compared, not stored"], a: 1, why: "Automatic variables are not zeroed; using an indeterminate int value is undefined behavior. Static-storage variables, by contrast, are zero-initialized." },
{ t: "memory", d: 3, q: "Portable C++17 way to read a float's raw bit pattern as a 32-bit unsigned integer?", o: ["Cast the float's address to an unsigned int pointer and dereference", "std::memcpy the four bytes into the integer", "A union: write the float member, read the int member", "static_cast, which preserves the bit pattern"], a: 1, why: "memcpy between same-sized objects is the sanctioned C++17 method. Pointer casts violate strict aliasing, union punning is formally UB in C++, and static_cast converts the value." },
{ t: "memory", d: 1, q: "What does sizeof applied to a struct include?", o: ["Only the sum of the members' sizes", "Heap data owned by members, like string contents", "Static data members", "Members plus any internal and trailing alignment padding"], a: 3, why: "sizeof is the object's full footprint including padding, so arrays stride correctly. It never counts heap allocations owned via pointers, nor static members." },
{ t: "embedded-cpp", d: 2, q: "volatile versus std::atomic: which statement is correct?", o: ["volatile is for hardware register access; std::atomic is for inter-thread data", "volatile makes accesses atomic on 32-bit MCUs", "std::atomic is just volatile plus a compiler barrier macro", "They are interchangeable for flags shared with an ISR"], a: 0, why: "volatile only forbids the compiler from caching or eliding accesses; it gives no atomicity or ordering. atomic provides both, which shared-data code needs." },
{ t: "embedded-cpp", d: 2, q: "Why do many firmware coding standards forbid heap allocation after initialization?", o: ["The heap is read-only on most MCUs", "malloc is unavailable in C++", "Fragmentation and nondeterministic timing can cause rare in-field failures", "Heap access disables interrupts"], a: 2, why: "Long-running devices can fragment a small heap until an allocation fails unpredictably, and allocation time varies; static or pool allocation keeps behavior provable." },
{ t: "embedded-cpp", d: 2, q: "Sharing a flag between an ISR and the main loop in C++: appropriate mechanism?", o: ["A lock-free std::atomic, or briefly masking the interrupt; never a blocking mutex", "A std::mutex locked in both contexts", "A volatile int, which orders the accesses", "A condition_variable notified from the ISR"], a: 0, why: "An ISR cannot block: waiting on a mutex the main loop holds deadlocks the system. Lock-free atomics or short interrupt-masked critical sections are the standard tools." },
{ t: "embedded-cpp", d: 3, q: "A no-heap system must construct driver objects at startup inside a reserved static buffer. Which C++ feature does this?", o: ["operator new silently switches to static memory", "A std::vector with reserve called early", "Placement new into suitably aligned static storage, with explicit destructor calls", "Declaring the drivers constexpr"], a: 2, why: "Placement new constructs an object at a given address without allocating. The buffer must satisfy alignment, and teardown, if ever needed, is an explicit destructor call." },
{ t: "embedded-cpp", d: 2, q: "Best standard building block for a fixed-capacity, no-heap container in firmware?", o: ["std::vector, which stays off the heap when small", "std::array with a compile-time capacity plus a count of used slots", "std::deque configured statically", "std::string, which always uses its small buffer"], a: 1, why: "std::array lives wherever you place it, with zero dynamic allocation; adding a size counter yields a simple static vector. std::vector always heap-allocates its elements." },
{ t: "embedded-cpp", d: 3, q: "When is a std::atomic of a small struct safe to share with an ISR?", o: ["Always; atomics never lock", "Never; ISRs cannot use atomics", "Only with sequentially consistent ordering", "Only if it is lock-free; a lock-based fallback can deadlock against the ISR"], a: 3, why: "Oversized atomics may be implemented with an internal lock; an ISR interrupting the lock holder then spins forever. Check is_always_lock_free at compile time." },
{ t: "embedded-cpp", d: 1, q: "Why must a memory-mapped hardware register be accessed through a volatile pointer?", o: ["volatile makes each access atomic", "The bus requires volatile for aligned access", "volatile flushes the CPU cache after writes", "The compiler must not cache, reorder away, or delete the reads and writes"], a: 3, why: "Register accesses have side effects invisible to the compiler; volatile forces every access to happen as written. It does not provide atomicity or cache control." },
{ t: "embedded-cpp", d: 2, q: "Why are C++ exceptions commonly disabled on small embedded targets?", o: ["Exceptions require an operating system", "Unwind tables add flash size and throwing has unpredictable timing", "throw is undefined behavior on ARM", "Propagation disables interrupts"], a: 1, why: "Exception support costs code space for unwind machinery, and throw latency is hard to bound, which conflicts with real-time budgets; error codes or result types are typical." },
{ t: "idioms", d: 2, q: "A lookup function may legitimately find nothing. Idiomatic C++17 return type?", o: ["The value type with a sentinel like minus one", "A bool out-parameter and a reference", "std::optional of the value type", "Throwing an exception when absent"], a: 2, why: "optional expresses absence in the type system, forcing callers to handle the empty case; sentinels are ambiguous and exceptions are for errors, not expected outcomes." },
{ t: "idioms", d: 3, q: "Why is returning a std::string_view that refers to a local std::string dangerous?", o: ["The view is non-owning; it dangles once the local string is destroyed", "string_view copies lazily and may exhaust memory", "It fails to compile", "The view keeps the string alive but leaks it"], a: 0, why: "string_view is a pointer and length into someone else's buffer. When the owning string dies at function exit, the returned view refers to freed storage." },
{ t: "idioms", d: 2, q: "What does the declaration auto [key, value] = pairObj; do?", o: ["Declares a lambda with two captures", "Builds a std::tuple from the pair", "Copies the pair, then binds the names key and value to its members", "Creates references that never copy"], a: 2, why: "Structured bindings destructure aggregates, tuples, and arrays. With plain auto the whole object is copied first; bind by reference to avoid the copy." },
{ t: "idioms", d: 2, q: "What is std::span (C++20) conceptually?", o: ["An owning, resizable buffer", "A linked view over any container type", "A range that copies elements on first write", "A non-owning view over a contiguous sequence: a pointer plus a length"], a: 3, why: "span replaces pointer-plus-size parameter pairs: it views existing contiguous data such as arrays or vector contents without owning or copying, like string_view for characters." },
{ t: "idioms", d: 1, q: "opt is an empty std::optional. Difference between opt.value() and dereferencing it?", o: ["value() throws bad_optional_access; dereferencing empty is undefined behavior", "Both throw the same exception", "Both are undefined behavior", "Both return a default-constructed value"], a: 0, why: "value() is checked and throws when empty. The dereference operators are unchecked preconditions, mirroring the pointer interface, so test has_value first." },
{ t: "idioms", d: 3, q: "Why can you not always pass the data() of a std::string_view to a C API expecting a C string?", o: ["data() returns a heap copy that leaks", "The viewed characters are not guaranteed to be null-terminated", "string_view stores UTF-16 internally", "data() returns null for substrings"], a: 1, why: "A view can reference a substring mid-buffer, so no terminator follows its last character and the C call reads past the view. Copy to a std::string when termination is required." }
];
